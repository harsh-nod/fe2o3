{
  "schema": "exact-runtime-maintenance-transforms-v1",
  "operations": [
    {
      "file": "amd-dbgapi-one-stop-native-v1.h",
      "before": "  identity current_identity ();",
      "after": "  void entry_maintenance_current ();\n  void retain_entry_commit_targets ();\n  void retire_entry_maintenance () noexcept;\n  identity current_identity ();"
    },
    {
      "file": "amd-dbgapi-one-stop-native-v1.h",
      "before": "  static constexpr std::uint64_t snapshot_scalar_scratch = 1024;",
      "after": "  static constexpr std::uint64_t snapshot_scalar_scratch = 1024;\n  static constexpr std::uint64_t entry_maintenance_scalar_scratch = 256;"
    },
    {
      "file": "amd-dbgapi-one-stop-native-v1.h",
      "before": "  path m_path=path::none;",
      "after": "  enum class entry_epoch : std::uint8_t { unused,entry_commit,armed,retired };\n  target_ops_ref m_entry_process_ref,m_entry_top_ref;\n  entry_epoch m_entry_epoch=entry_epoch::unused;\n  bool m_entry_maintenance_inflight=false;\n  path m_path=path::none;"
    },
    {
      "file": "amd-dbgapi-one-stop-native-v1.h",
      "before": "  return sizeof (native_adapter) + constant_and_scratch + snapshot_scalar_scratch\n",
      "after": "  return sizeof (native_adapter) + constant_and_scratch + snapshot_scalar_scratch\n    + entry_maintenance_scalar_scratch\n"
    },
    {
      "file": "amd-dbgapi-one-stop-native-v1.h",
      "before": "static_assert (sizeof (native_adapter) + 4096 + 1024 + limits::client_output_bytes + 256\n",
      "after": "static_assert (sizeof (native_adapter) + 4096 + 1024 + 256 + limits::client_output_bytes + 256\n"
    },
    {
      "file": "amd-dbgapi-one-stop-native-resume-v1.inc",
      "before": "namespace amd_owned_one_stop_v1 {\n",
      "after": "namespace amd_owned_one_stop_v1 {\nvoid native_adapter::entry_maintenance_current () {\n  debit (counter::work,128); // Fixed added checks; roster debits remain separate.\n  require (m_inferior!=nullptr && m_inferior_ref.get ()==m_inferior\n           && current_inferior ()==m_inferior && m_host!=nullptr\n           && m_host_ref.get ()==m_host && m_entry_process_ref.get ()!=nullptr\n           && m_entry_top_ref.get ()!=nullptr,failure::owner_changed);\n  auto *const proc=m_inferior->process_target ();\n  auto *const top=m_inferior->top_target ();\n  require (proc!=nullptr && proc==m_entry_process_ref.get ()\n           && proc==get_native_target () && top==m_entry_top_ref.get ()\n           && m_inferior->target_is_pushed (proc)\n           && m_inferior->target_is_pushed (top)\n           && (top==proc || (top->stratum ()==thread_stratum\n                            && m_inferior->find_target_beneath (top)==proc)),\n           failure::owner_changed);\n  require (m_owner.check_owner (current_identity ()),failure::owner_changed);\n  require (m_owner.state ()==phase::host_running && !m_owner.m_runtime\n           && !m_owner.m_callback && info ().runtime_state==AMD_DBGAPI_RUNTIME_STATE_UNLOADED\n           && !m_inferior->target_is_pushed (&the_amd_dbgapi_target)\n           && !m_command_continue && !m_exit_requested && !m_infrun_origin\n           && m_callback_progress==callback_progress::none\n           && m_gpu_thread==nullptr && m_gpu_thread_ref.get ()==nullptr,\n           failure::commit);\n  require (m_host->state==THREAD_RUNNING && m_host->executing () && m_host->resumed ()\n           && !m_host->has_pending_waitstatus () && proc->threads_executing\n           && proc->commit_resumed_state && !proc->has_resumed_with_pending_wait_status (),\n           failure::commit);\n  std::size_t host=0,gpu=0; thread_roster (host,gpu);\n  require (host==1 && gpu==0,failure::resume_scope);\n}\nvoid native_adapter::retain_entry_commit_targets () {\n  debit (counter::work,32); // Retention/scalar setup before any commit effect.\n  require (m_entry_epoch==entry_epoch::unused && !m_entry_maintenance_inflight\n           && m_entry_process_ref.get ()==nullptr && m_entry_top_ref.get ()==nullptr\n           && m_inferior!=nullptr && m_inferior_ref.get ()==m_inferior\n           && current_inferior ()==m_inferior,failure::commit);\n  auto *const proc=m_inferior->process_target ();\n  auto *const top=m_inferior->top_target ();\n  require (proc!=nullptr && top!=nullptr && proc==get_native_target ()\n           && m_inferior->target_is_pushed (proc)\n           && m_inferior->target_is_pushed (top),failure::owner_changed);\n  // Same real ref policy used by target_detach; no raw-pointer permit.\n  m_entry_process_ref=target_ops_ref::new_reference (proc);\n  m_entry_top_ref=target_ops_ref::new_reference (top);\n  m_entry_epoch=entry_epoch::entry_commit; // Never re-armed from serialized state.\n  entry_maintenance_current ();\n}\nvoid native_adapter::retire_entry_maintenance () noexcept {\n  if (m_entry_epoch==entry_epoch::unused || m_entry_epoch==entry_epoch::retired) return;\n  if (!m_owner.invalid ()) m_owner.debit (counter::work,32);\n  if (m_entry_maintenance_inflight\n      || (m_entry_epoch==entry_epoch::entry_commit && m_generic_commit))\n    m_owner.poison (failure::commit); // Reentry cannot acquire a resume permit.\n  m_entry_epoch=entry_epoch::retired; // Monotonic; no reset/reselection.\n  // Never drop the last ref or run target close from this noexcept path.\n  // During an effect, or after ownership/stack drift, retain the bounded refs\n  // through debugger teardown. They grant no authority once retired/poisoned.\n  if (!m_entry_maintenance_inflight && !m_generic_commit\n      && m_inferior!=nullptr && m_inferior_ref.get ()==m_inferior\n      && current_inferior ()==m_inferior && m_entry_process_ref.get ()!=nullptr\n      && m_entry_top_ref.get ()!=nullptr\n      && m_inferior->target_is_pushed (m_entry_process_ref.get ())\n      && m_inferior->target_is_pushed (m_entry_top_ref.get ())) {\n    m_entry_top_ref.reset (nullptr);\n    m_entry_process_ref.reset (nullptr);\n  }\n}\n"
    },
    {
      "file": "amd-dbgapi-one-stop-native-resume-v1.inc",
      "before": "void native_adapter::before_generic_commit () {\n  if (!selected ()) return;\n  require (m_path!=path::none && m_generic_returned && !m_generic_commit,\n           failure::commit);\n  if (m_path==path::publication || m_path==path::completion)\n    require (m_owner.commit_started (current_identity ()),failure::commit);\n  m_generic_commit=true; // Before beneath/forward-progress effects.\n}\nvoid native_adapter::after_amd_commit () {\n  if (!selected ()) return;\n  require (m_generic_commit && !m_amd_commit_returned && m_path!=path::entry\n           && m_amd_body_returned,failure::commit);\n  m_amd_commit_returned=true;\n}\nvoid native_adapter::after_generic_commit () {\n  if (!selected ()) return;\n  require (m_generic_commit && m_generic_returned\n           && (m_path==path::entry ? !m_amd_commit_returned : m_amd_commit_returned),\n           failure::commit);\n  if (m_path==path::publication || m_path==path::completion)\n    require (m_owner.commit_returned (),failure::commit);\n  if (m_path==path::callback_continue) m_callback_progress=callback_progress::none;\n  m_path=path::none; m_generic_returned=m_generic_commit=false;\n  m_amd_body_returned=m_amd_commit_returned=false;\n  flush ();\n}\n",
      "after": "void native_adapter::before_generic_commit () {\n  if (!selected ()) return;\n  if (m_path==path::none) {\n    require (m_entry_epoch==entry_epoch::armed && !m_entry_maintenance_inflight\n             && !m_generic_returned && !m_generic_commit\n             && !m_amd_body_returned && !m_amd_commit_returned,failure::commit);\n    entry_maintenance_current ();\n    m_entry_maintenance_inflight=true; // Consume before the one existing commit.\n    return; // No target_resume credit, owner phase change, ACK, or output.\n  }\n  require (m_path!=path::none && m_generic_returned && !m_generic_commit,\n           failure::commit);\n  if (m_path==path::entry) retain_entry_commit_targets ();\n  if (m_path==path::publication || m_path==path::completion)\n    require (m_owner.commit_started (current_identity ()),failure::commit);\n  m_generic_commit=true; // Before beneath/forward-progress effects.\n}\nvoid native_adapter::after_amd_commit () {\n  if (!selected ()) return;\n  require (m_generic_commit && !m_amd_commit_returned && m_path!=path::entry\n           && m_amd_body_returned,failure::commit);\n  m_amd_commit_returned=true;\n}\nvoid native_adapter::after_generic_commit () {\n  if (!selected ()) return;\n  if (m_entry_maintenance_inflight) {\n    require (m_entry_epoch==entry_epoch::armed && m_path==path::none\n             && !m_generic_returned && !m_generic_commit\n             && !m_amd_body_returned && !m_amd_commit_returned,failure::commit);\n    entry_maintenance_current ();\n    m_entry_maintenance_inflight=false;\n    return; // Do not repeat an owner transition row or change stop/packet counts.\n  }\n  require (m_generic_commit && m_generic_returned\n           && (m_path==path::entry ? !m_amd_commit_returned : m_amd_commit_returned),\n           failure::commit);\n  if (m_path==path::publication || m_path==path::completion)\n    require (m_owner.commit_returned (),failure::commit);\n  if (m_path==path::callback_continue) m_callback_progress=callback_progress::none;\n  const bool entry=m_path==path::entry;\n  m_path=path::none; m_generic_returned=m_generic_commit=false;\n  m_amd_body_returned=m_amd_commit_returned=false;\n  flush ();\n  if (entry) {\n    require (m_entry_epoch==entry_epoch::entry_commit,failure::commit);\n    entry_maintenance_current ();\n    m_entry_epoch=entry_epoch::armed; // Only after that actual first commit/flush.\n  }\n}\n"
    },
    {
      "file": "amd-dbgapi-one-stop-native-resume-v1.inc",
      "before": "void native_adapter::before_generic_resume (ptid_t scope,int step,gdb_signal signal) {\n  if (!selected ()) return;\n",
      "after": "void native_adapter::before_generic_resume (ptid_t scope,int step,gdb_signal signal) {\n  if (!selected ()) return;\n  retire_entry_maintenance ();\n"
    },
    {
      "file": "amd-dbgapi-one-stop-native-resume-v1.inc",
      "before": "void native_adapter::mi_input (const char *text) {\n  if (!selected ()) return;\n",
      "after": "void native_adapter::mi_input (const char *text) {\n  if (!selected ()) return;\n  retire_entry_maintenance ();\n"
    },
    {
      "file": "amd-dbgapi-one-stop-native-events-v1.inc",
      "before": "void native_adapter::callback_begin (amd_dbgapi_inferior_info &actual,breakpoint *bp,\n                                    amd_dbgapi_breakpoint_id_t id,thread_info *thread) {\n  if (!selected ()) return;\n  require (&actual==&info () && thread==m_host && thread==inferior_thread ()\n           && m_path==path::none && m_callback_progress==callback_progress::none\n           && bp!=nullptr && bp->pspace==m_inferior->pspace\n           && bp->has_single_location () && bp->disposition==disp_donttouch,\n           failure::callback_identity);\n  const auto found=actual.breakpoint_map.find (id.handle);\n  require (found!=actual.breakpoint_map.end () && found->second==bp\n           && bp->first_loc ().pspace==m_inferior->pspace\n           && bp->first_loc ().address!=0\n           && regcache_read_pc (get_thread_regcache (thread))==bp->first_loc ().address,\n           failure::callback_identity);\n  const auto object=reinterpret_cast<std::uintptr_t> (bp);\n  if (m_callback_object==0) {\n    require (m_owner.state ()==phase::host_running,failure::callback_identity);\n    m_callback_object=object; m_callback_pc=bp->first_loc ().address;\n    m_callback_number=static_cast<std::uint64_t> (bp->number);\n  } else {\n    require (object==m_callback_object && bp->first_loc ().address==m_callback_pc\n             && static_cast<std::uint64_t> (bp->number)==m_callback_number,\n             failure::callback_identity);\n  }\n  if (m_owner.state ()==phase::awaiting_target_completion) {\n    read_runtime_root (m_owner.m_root_address);\n    require (m_root_read.breakpoint==m_callback_pc\n             && m_owner.terminal_callback_begin\n                  (current_identity (),object,id.handle,thread->global_num,\n                   m_owner.m_root_address,m_root_read),failure::callback_identity);\n  } else {\n    require (m_owner.callback_begin (current_identity (),object,id.handle,thread->global_num),\n             failure::callback_identity);\n  }",
      "after": "void native_adapter::callback_begin (amd_dbgapi_inferior_info &actual,breakpoint *bp,\n                                    amd_dbgapi_breakpoint_id_t id,thread_info *thread) {\n  if (!selected ()) return;\n  retire_entry_maintenance ();\n  require (&actual==&info () && thread==m_host && thread==inferior_thread ()\n           && m_path==path::none && m_callback_progress==callback_progress::none\n           && bp!=nullptr && bp->pspace==m_inferior->pspace\n           && bp->has_single_location () && bp->disposition==disp_donttouch,\n           failure::callback_identity);\n  const auto found=actual.breakpoint_map.find (id.handle);\n  require (found!=actual.breakpoint_map.end () && found->second==bp\n           && bp->first_loc ().pspace==m_inferior->pspace\n           && bp->first_loc ().address!=0\n           && regcache_read_pc (get_thread_regcache (thread))==bp->first_loc ().address,\n           failure::callback_identity);\n  const auto object=reinterpret_cast<std::uintptr_t> (bp);\n  if (m_callback_object==0) {\n    require (m_owner.state ()==phase::host_running,failure::callback_identity);\n    m_callback_object=object; m_callback_pc=bp->first_loc ().address;\n    m_callback_number=static_cast<std::uint64_t> (bp->number);\n  } else {\n    require (object==m_callback_object && bp->first_loc ().address==m_callback_pc\n             && static_cast<std::uint64_t> (bp->number)==m_callback_number,\n             failure::callback_identity);\n  }\n  if (m_owner.state ()==phase::awaiting_target_completion) {\n    read_runtime_root (m_owner.m_root_address);\n    require (m_root_read.breakpoint==m_callback_pc\n             && m_owner.terminal_callback_begin\n                  (current_identity (),object,id.handle,thread->global_num,\n                   m_owner.m_root_address,m_root_read),failure::callback_identity);\n  } else {\n    require (m_owner.callback_begin (current_identity (),object,id.handle,thread->global_num),\n             failure::callback_identity);\n  }"
    },
    {
      "file": "amd-dbgapi-one-stop-native-events-v1.inc",
      "before": "void native_adapter::runtime_ack (amd_dbgapi_inferior_info &actual,amd_dbgapi_event_id_t event,\n                                 amd_dbgapi_runtime_state_t state,amd_dbgapi_status_t status) {\n  if (!selected ()) return;\n  require (&actual==&info () && actual.runtime_state==state\n           && m_path==path::none && m_callback_progress==callback_progress::none,\n           failure::owner_changed);\n  if (state==AMD_DBGAPI_RUNTIME_STATE_LOADED_SUCCESS) {\n    require (m_owner.runtime_loaded (current_identity (),event.handle,status),failure::acknowledgement);\n  } else if (state==AMD_DBGAPI_RUNTIME_STATE_UNLOADED) {\n    require (m_owner.runtime_unloaded (current_identity (),event.handle,status),failure::acknowledgement);\n  } else reject (failure::native_result);\n  flush ();",
      "after": "void native_adapter::runtime_ack (amd_dbgapi_inferior_info &actual,amd_dbgapi_event_id_t event,\n                                 amd_dbgapi_runtime_state_t state,amd_dbgapi_status_t status) {\n  if (!selected ()) return;\n  retire_entry_maintenance ();\n  require (&actual==&info () && actual.runtime_state==state\n           && m_path==path::none && m_callback_progress==callback_progress::none,\n           failure::owner_changed);\n  if (state==AMD_DBGAPI_RUNTIME_STATE_LOADED_SUCCESS) {\n    require (m_owner.runtime_loaded (current_identity (),event.handle,status),failure::acknowledgement);\n  } else if (state==AMD_DBGAPI_RUNTIME_STATE_UNLOADED) {\n    require (m_owner.runtime_unloaded (current_identity (),event.handle,status),failure::acknowledgement);\n  } else reject (failure::native_result);\n  flush ();"
    },
    {
      "file": "amd-dbgapi-one-stop-native-events-v1.inc",
      "before": "void native_adapter::after_host_or_gpu_stop (thread_info *thread) {\n  if (!selected ()) return;\n  if (m_owner.state ()==phase::lifecycle_disappeared && thread==nullptr) return;\n  // Lifecycle disappearance is not a claim that the separate MI exit was normal.\n  require (thread!=nullptr && thread==inferior_thread (),failure::stop_changed);\n  if (m_owner.state ()==phase::provisional_checkpoint) {\n    require (thread==m_host\n             && m_owner.host_stop_output_completed\n                  (current_identity (),regcache_read_pc (get_thread_regcache (thread))),\n             failure::checkpoint_changed);\n  } else if (m_owner.state ()==phase::provisional_gpu_stop) {\n    require (thread==m_gpu_thread,failure::stop_changed);\n    m_candidate_stop=query_stop (thread,true);\n    require (m_owner.gpu_stop_output_completed (current_identity (),m_candidate_stop),\n             failure::stop_changed);\n    confirm_physical_snapshot (); // Uses the unchanged second full native tuple.\n  } else reject (failure::order);\n  // MI *stopped alone never means checkpoint retirement. Do not flush here.",
      "after": "void native_adapter::after_host_or_gpu_stop (thread_info *thread) {\n  if (!selected ()) return;\n  retire_entry_maintenance ();\n  if (m_owner.state ()==phase::lifecycle_disappeared && thread==nullptr) return;\n  // Lifecycle disappearance is not a claim that the separate MI exit was normal.\n  require (thread!=nullptr && thread==inferior_thread (),failure::stop_changed);\n  if (m_owner.state ()==phase::provisional_checkpoint) {\n    require (thread==m_host\n             && m_owner.host_stop_output_completed\n                  (current_identity (),regcache_read_pc (get_thread_regcache (thread))),\n             failure::checkpoint_changed);\n  } else if (m_owner.state ()==phase::provisional_gpu_stop) {\n    require (thread==m_gpu_thread,failure::stop_changed);\n    m_candidate_stop=query_stop (thread,true);\n    require (m_owner.gpu_stop_output_completed (current_identity (),m_candidate_stop),\n             failure::stop_changed);\n    confirm_physical_snapshot (); // Uses the unchanged second full native tuple.\n  } else reject (failure::order);\n  // MI *stopped alone never means checkpoint retirement. Do not flush here."
    },
    {
      "file": "amd-dbgapi-one-stop-native-events-v1.inc",
      "before": "void native_adapter::disappearing (inferior *actual) {\n  if (!selected () || m_owner.invalid ()) return;\n  // exit_inferior clears threads before this observer, but has not cleared PID.\n  // Retained strong references prevent address reuse; do not pretend a live\n  // stopped host thread still exists or query target memory here.\n  const bool same_owner=actual==m_inferior && m_info!=nullptr\n    && amd_dbgapi_inferior_data.get (actual)==m_info && m_info->inf==actual\n    && actual->pspace==reinterpret_cast<program_space *> (m_owner.m_identity.program_space)\n    && actual->num==static_cast<int> (m_owner.m_identity.inferior_number)\n    && actual->pid==static_cast<int> (m_owner.m_identity.pid)\n    && m_info->process_id.handle==m_owner.m_identity.process\n    && m_inferior_ref.get ()==actual && m_host_ref.get ()==m_host;\n  if (!same_owner || !m_owner.lifecycle_disappeared (m_owner.m_identity)) {\n    m_owner.poison (failure::lifecycle);\n    return; // Never interrupt ordinary known-owner teardown with a thrown error.\n  }\n  flush ();",
      "after": "void native_adapter::disappearing (inferior *actual) {\n  if (!selected () || m_owner.invalid ()) return;\n  retire_entry_maintenance ();\n  // exit_inferior clears threads before this observer, but has not cleared PID.\n  // Retained strong references prevent address reuse; do not pretend a live\n  // stopped host thread still exists or query target memory here.\n  const bool same_owner=actual==m_inferior && m_info!=nullptr\n    && amd_dbgapi_inferior_data.get (actual)==m_info && m_info->inf==actual\n    && actual->pspace==reinterpret_cast<program_space *> (m_owner.m_identity.program_space)\n    && actual->num==static_cast<int> (m_owner.m_identity.inferior_number)\n    && actual->pid==static_cast<int> (m_owner.m_identity.pid)\n    && m_info->process_id.handle==m_owner.m_identity.process\n    && m_inferior_ref.get ()==actual && m_host_ref.get ()==m_host;\n  if (!same_owner || !m_owner.lifecycle_disappeared (m_owner.m_identity)) {\n    m_owner.poison (failure::lifecycle);\n    return; // Never interrupt ordinary known-owner teardown with a thrown error.\n  }\n  flush ();"
    },
    {
      "file": "amd-dbgapi-one-stop-native-events-v1.inc",
      "before": "  if (selected ()) { m_snapshot.revoke (snapshot_issue::owner_changed); m_owner.poison (why); }\n",
      "after": "  if (selected ()) { m_snapshot.revoke (snapshot_issue::owner_changed); m_owner.poison (why); retire_entry_maintenance (); }\n"
    },
    {
      "file": "amd-dbgapi-one-stop-native-events-v1.inc",
      "before": "  if (selected ()) { m_snapshot.revoke (snapshot_issue::native_error); m_owner.poison (failure::native_result); }\n",
      "after": "  if (selected ()) { m_snapshot.revoke (snapshot_issue::native_error); m_owner.poison (failure::native_result); retire_entry_maintenance (); }\n"
    },
    {
      "file": "amd-dbgapi-one-stop-native-io-v1.inc",
      "before": "  m_owner.poison (why);\n",
      "after": "  m_owner.poison (why);\n  retire_entry_maintenance ();\n"
    }
  ],
  "reverse_exact": true
}
