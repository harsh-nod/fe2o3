# Fixed physical-snapshot V2 consumer — unbound

This is a separate unpublished Cargo workspace, not an in-place V1 protocol update. The package has 25 selected Rust/Cargo/license leaves, including a diagnostics-only successor to the historical V2 staging. The original V1 package remains unchanged apart from a link to this sibling. No GPL debugger implementation is copied or linked into this MIT OR Apache-2.0 package.

The native PROFILE is None. There is no executable, PID, path, argv, JSON or environment override that enables it. runtime-bindings.json contains null historical placeholders only; Rust does not read it. No controller/debugger/target invocation is provided here. Hardcoded target/artifact/source constants are private qualification-fixture identities, not portable deployment locations.

## What changes in V2

The closed domains are fe2o3-owned-one-stop-v2, fe2o3-gfx950-one-stop-controller-observation-v2 and fe2o3-one-stop-native-peer-observation-v2. V2 refuses V1 or mixed producer rows. The actual old V1 implementation is separately tested for eventual refusal of a complete V2 transcript; it may ignore an unknown notify first, so immediate rejection is not claimed.

At the source-owned, confirmed and retired current stop, the producer can eventually report ACTUAL s12/DWARF44 four bytes and ACTUAL272 bytes of output. The consumer checks the retained owned inferior start/PID, exact MI GPU thread/PC, source/profile layout and same producer tuple. All 64-bit scalar selectors serialize losslessly as decimal strings. Unavailable register/memory/short-memory states are typed absence; they never serialize a fabricated zero capture. The independent fixed XOR-lane/canary oracle validates received bytes, never populates them.

This same-stop272B sample is separate from the later target's complete4096B output/canary validation. The target report is not a second raw-memory recapture. JSON fields are historical observations, not native handles, source owners, stop tokens or a launch permit. Public construction of mock Peer implementations cannot prove those actual owners.

The fixed17-command MI2 flow, original60-second start/deadline,192KiB line,8MiB stream and8192-record ceilings remain. No extra read/evaluation/attach command, queue input or resume path exists. Exact command matching and post-observation/serialization deadline checks remain. Failure cleanup attempts to drain both actual EOFs and reap its direct debugger child under its original separate5-second bound. If setup fails before reader startup, EOF remains unobserved and that cleanup deadline can expire; the new diagnostics do not change or qualify this path. Those facts do not prove whole-family cleanup, no foreign actor, isolation or sampler exclusion.

Setup failures retain a fixed stage/substage and only facts already observed by that check: child PID/initial stamp, wait status, acquired debugger custody, actual reader-start mask and cmdline length/equality. Unknown observations remain absent. The first failure is copied before unchanged cleanup and printed only on stderr; the original Refusal, successful protocol/publication, MI command roster and all limits are unchanged. These diagnostic values cannot enable the unbound PROFILE or replace actual outer-family cleanup evidence. No startup output is fabricated when pipe readers have not started.

## Read-only source check and CPU-only qualification

The explicit verifier reads exactly25 selected leaves:21 Rust files, Cargo.toml/lock and two licenses. It retains the old32KiB manifest,128KiB file and512KiB aggregate limits. It checks canonical JSON, fixed path roster, strict UTF8, length/hash, regular non-symlink files and before/after directory/file identities. This is consistency against the checked-in manifest, not authentication of a coordinated edited manifest, full tree, binary, startup or native authority.

Set package_dir to this canonical absolute package directory. Read-only: node "$package_dir/verify-source.mjs" "$package_dir". CPU-only package controls: node --test "$package_dir/tests/package-files-tests.mjs". Root may separately run Cargo --offline --locked test/clippy/build against this manifest with a FRESH target directory. Do not run the resulting native binary. There is no acquisition/build/launch automation here.

Historical qualified source had58 V2 library tests +30 V2 native-transport CPU tests, strict Clippy/build, and ONE separate old-V1 compatibility test (89 combined). Exact receipt is in historical-evidence.json. Those results do not qualify relocation or new package helpers. tests/v1-compatibility.patch is the exact appended old-parser test, for a root-created temporary COPY of the unchanged parent; never patch the published V1 package in place. Its pre/post pins are retained in compatibility.json.

No new native/debugger/startup, dispatch/stop, physical capture, source/ABI/kernel equivalence, hardware or V4 milestone result is asserted. Overall accepted exits remain6/18. The separately disabled [GPL producer](../../rocgdb-one-stop-native-adapters-v1/physical-v2/README.md) needs its own full source/build/startup and same-client native qualification; nothing transfers from older debugger ELF acceptance.
