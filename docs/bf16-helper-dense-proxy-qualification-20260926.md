# BF16 helper dense propagation and borrowed ranked candidate — 2026-09-26

This checkpoint advances #280 M4 and #282 U4 without closing either.
Accepted broad exits remain **M1/V1/V2/U1/U2/U3 (6/18)**.

## Implemented and genuinely exercised

Source preparation now charges the original cumulative account for the real
enum-dominance analysis, scalar/allocation/provenance/constant preparation and
exclusive-owner carrier traversal. Shared legacy algorithms keep their prior
predicates and local limits. Exact new capacities, relocations, scans, projection
spines and live frame/error storage are accounted before the corresponding work;
the adapter never substitutes a fresh budget or refunds another scope's bytes.
The generic enum adapter includes large associated-error frame controls.

C2 runs the actual closed acyclic source CFG through initial and repeated dense
propagation and final replay. The existing transfer semantics are shared with
the legacy state adapter. Each pass queries the sole actual defined call exactly
once; all three genuine visits authenticate the caller. Source Move operands are
consumed only after complete call authentication. The returned ordinary
**[f32; 4] array does not acquire accumulator provenance**.

The final visit uses C3's actual caller Call, helper Matrix and helper Return,
with four function-qualified component rows. Identity and Swap01 preserve their
different component order. Transported Return operands need not have the raw
Matrix result IDs. Genuine controls observe that exact relation, callback
success/error/panic, retained callback surplus and ignored work/storage/floor
violations, under unchanged limits and the original account.

A new private borrowed ranked proxy maps that live C3 association to one
TensorLayout recipe candidate: the exact zero-filled gfx942 BF16/F32 m16n16k16
Wave64 contract, UniformSubgroup, 64 lanes, four logical arguments and the
qualified result binding. Its own work/storage, partial failure, panic, sticky
denial, floor and ledger controls are tested. The genuine hook keeps all final
callback controls INSIDE the proxy scope, then unwinds through C3, dense replay,
facts and source preparation. The candidate borrows its real occurrence; a
copied operation or digest is not readiness or compilation authority.

This is a genuine combined source/preparation/C1/C2/C3/proxy observation, not just
a synthetic positive. It is still DURING the Final callback: later blocks and
outer postflight checks can fail. No result is admitted to ordinary ranked
compilation, and no existing pending/RawEmpty guard was removed.

## Completed CPU qualifications on mi350

Each receipt binds its recorded HEAD plus exact dirty source census, not an
arbitrary later checkout. Counts from separate gates overlap.

| Gate | Actual passing tests | Receipt SHA-256 |
| --- | --- | --- |
| Preparation R2 | 666 model; 2,411 backend; 12 closure-pin controls | `785634ba1de17bc8931e592c3b09ff22970821bc42ac13a2fff8ee7695bc6150` |
| Final callbacks / fresh R7 | 2,412 backend; rebuilt extractor/backend; five fresh Rust sessions | `6797954faed028e07f995fb4f29c48df32af558bd7259879e578c3487d2251e5` |
| Borrowed proxy / fresh R8 | 404 cargo-fe2o3 binary; 2,424 backend; rebuilt extractor/backend; five fresh Rust sessions | `1a947a2c01aa5ac4d51eefface719f2b7b62354679401de07da95b222bd17c76` |

All listed gates completed with zero failures. Backend runs exclude 189 ignored
tests; the genuine helper test was separately selected with --ignored and
passed. The binary run excludes five ignored tests and is NOT the entire CLI
integration suite. Two child-process self-tests are overlapping executions,
not additional distinct parent tests.

R8 receipt: 59,176 bytes. Executed source census: 8,361 files / 119,541,432 bytes /
`30382d7977e52e9c937c386b2ecd489ab05c4ae80accc1012e9b6f22ffc19564`.
Fresh R8 parent report: 281,039 bytes /
`90afe14b280f2e3cfb20ae28124e9c58e14b3df6c0a2523b5196f682a6c239a2`.
Identity and Swap01 each retain 18 numerical positives and 16 request refusals;
wrong launch and callback error/panic sessions also pass.

Lossless R6→R7 comparison permits only generation paths/dependency digests and
25,443,061 additional work units for Identity/error/panic, or 25,443,214 for
Swap01. R7→R8 permits only the same generation changes and 8,768 additional
work units in affected sessions. Numerical bytes, masks, refusals, storage and
the 1,632,943,151-byte logical peak are unchanged. This is logical accounting,
not RSS. Limits remain 2 GiB storage and 2^54 cumulative work.

Root comparison audits:
`947c589d8d2fc191b1c371f36cc4e56b4c7c72c5f9c494bce35d7c5ece548e02`
(R7) and
`7e760e73d4bbe9a79e49b721fab14b8860eec2de2de0171cedc7f4ab9b1d02c9`
(R8). Independent proxy/hook source review:
`36231c2d0f47821be3789baf99a9a7ebdf08cef7aa8762f73005e4b9d87a94cf`.

## Retained failures and CI correction

The first preparation gate exposed a test assertion requiring Debug on an
intentionally opaque ledger identity. The assertion now compares equality
without expanding that API; the failed gate remains retained. A first proxy
request incorrectly selected --lib on the binary-only cargo-fe2o3 package and
stopped before tests. Its successor's broader CLI integration tests rejected
the runner's explicit RUSTC environment at their intended environment guard
(10 failures); it did not reach the new backend tests. The qualified R3 gate
selects the complete cargo-fe2o3 binary tests, not those integration tests.
No complete CLI-suite or strict backend-Clippy pass is claimed; the earlier
non-strict lint run retained existing warnings.

[CI run 36222772392](https://github.com/harsh-nod/fe2o3/actions/runs/36222772392)
failed two workspace macro-closure pin tests. Independent reconstruction of all
58 tracked macro-tree files showed that only four already-committed nested
fixture lockfiles changed through d355dd0b6; no macro source or manifest changed.
The reviewed LOCAL tree pin is now
`65aa1e5e26c40735f10562a847d46dcff5835cb43ad9355002d8a83c93eb38e2`.
The external-source pin and exact complete-tree checks are unchanged. All 12
focused closure controls and then all 404 binary tests passed. A new remote
CI result must be evaluated separately after publication.

## Remaining implementation

- A source-owned final candidate must not be exposed until dense replay,
  facts and preparation postflights all succeed on the original account.
- Complete root projection must retain actual input reads, output store,
  Option/bounds conditions, CFG, source mappings and all resource charges.
  One TensorLayout row cannot replace the source root's full recipe.
- CFG expansion/reordering must emit explicit source-call to final ranked
  coordinates; source block indices are not ranked block indices.
- Distinct checked nominal correspondence and retained output custody must
  precede formal/source-memory/target verification and ordinary LLVM handoff.
- Broader mutated-source capability controls, final foreign-ledger replacement,
  guarded-source postflight and private-facts nonescape qualifications remain
  separate; existing facts-level tests do not stand in for them.
- Edited tiled promotion, native helper execution, machine/hardware equivalence
  and the full shared curriculum remain open.

Normal helper continuation still deliberately refuses. CPU numerical success
and candidate construction do not establish LLVM emission, artifact publication
or GPU launch. This checkpoint changes no broad milestone checkbox.
