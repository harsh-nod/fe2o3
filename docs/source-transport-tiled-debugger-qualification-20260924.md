# Source roles, typed transport and debugger packaging — 2026-09-24

This record separates genuine frontend runs, static native observations and
CPU-only debugger infrastructure. It does not close another broad milestone:
**M1/V1/V2/U1/U2/U3 (6/18)** remain accepted.

## Actual source roles and native data transport

The fresh composition transport ladder completed 26 actual extractor/frontend
children: thirteen CLI, ten normal-output and three argument-role sessions.
Its source-owned callbacks retain the original cumulative resource ledger and
actual SSA argument/call roles through serialization. The five ABI components
retain offsets 0/8/16/20/24; input rows are bounded to 64 x 16 bytes.
The 128 normal CPU cases and 96 role CPU cases overlap existing configurations;
54 role and nine suffix mutation refusals are not extra successful source runs.

The source ladder receipt is
`logs/phase28-resume-r14-compiler-composition-transport-actual-r2/receipt.json`,
34,301 bytes, SHA-256
`d3dd809f4179593f8197eb52a5e6f873ea8ffff07052e03d79728803df7141e0`.
Its source census is
`3c76d94c2093aed142def371b23f554dc5da179f9024db137d1f6e51a8ba9298`.
The retained 246,046-byte ladder report has SHA-256
`703175f99606d31a2ecd37926b9d42982d863a6f8cc2e2667a36ca41e8dd5158`.
An independent artifact audit checked fifteen canonical/LLVM/handoff joins and
the retained terminal streams and dependency pins.

The separate private typed machine analysis runs on actual decoded LLVM MC
instructions, CFG and register units. It retains one move-only account and
reservation: 65,536 bytes of logical storage and 280,576 work units. It does not
reset the incoming account or refund consumed work. The reservation lasts
through serialization and releases exactly back to its incoming storage floor.

All six fresh O0/O3 copy/preserve/edit cases passed their finite DATA relation,
byte-equal default-versus-typed complete effect traces, late original-account
refusals and cumulative-account refusals. The receipt is
`logs/phase28-resume-r14-compiler-transport-native-actual-r2/receipt.json`,
135,301 bytes, SHA-256
`80a7a71d30d830aeaa9fdbcf2fb252c78dbb2cfe22a111eb4af2f45ef2720991`.
The 22,521-byte matrix report has SHA-256
`246462bcd4b74d11270ee9d4851dd4e4869202d6e6da04325e7de5c41925e67d`.

At O0 the relation is conditional on the actual helper live-ins; it does not
prove the complete caller-to-helper ABI. At O3 it follows the selected kernarg
loads/wait and data roles. Neither establishes address/predicate correctness,
whole-kernel functional equivalence, runtime preconditions or GPU execution.
The implementation is private-macro gated; it does not silently replace the
ordinary production analyzer or authorize protected publication.

A newly rebuilt legacy observer separately passed all fourteen older static
cases, with byte-identical HSACOs to the preceding legacy run. This regression
uses the exact retained historical R7 source/artifacts in a separately pinned
read-only view, not a fresh execution of today's frontend. Its receipt is
`logs/phase28-resume-r14-compiler-transport-legacy-actual-r2/receipt.json`,
207,460 bytes, SHA-256
`fe1080c97effc4a25085441c5537fdb06efe4f1946f954c0b146afb006a3cdcf`.
The current-source six-case matrix above is a distinct qualification.

## Borrowed BF16 tiled inspection: core qualified, shape work remains

The opt-in pre-ranked API joins actual nominal Context/Lane/A/B/Zero/Result
producers, source spans, twelve input components, four MFMA result definitions
and their real outside uses. Its callback borrows the original live owner,
binding map and cumulative phase budget. JSON/digests cannot reconstruct it.
Later mandatory ranked, formal and target checks remain unchanged.

The finite fixture is a Wave64/WG64, one-workgroup BF16 fragment with one
result-component output use, **not a GEMM or numerical correctness result**.
The measured root has twenty blocks and thirty-two locals. Whole-root scans
are bounded at thirty-two blocks while sparse source mappings remain sixteen.
Scratch is prepaid on the original ledger and released to its exact floor.

The actual four-session core ladder now passes: direct inspection, wrong launch,
callback error and callback panic. The direct callback retained the original
ledger, observed the real matrix and joined all four result definitions to their
actual uses. Unused result components are permitted; missing or reordered
definitions are not. Wrong-launch input refuses before callback execution.
Error and panic controls preserve consumed work and the incoming storage floor.

The completed R12 gate passed 62 lowerer and 18 frontend controls plus the
four-session parent. Its receipt is
`logs/phase28-resume-r14-compiler-tiled-source-core-actual-r12/receipt.json`,
162,011 bytes, SHA-256
`c02f9da928a778a7771218193c622d15ee2aec8ef24d84ded472704367eb1af8`.
Its source census is
`221c29bdbcc3eada1833a038b8442f9043f74cb2aaa2b17baea4ef24f0322831`.
The direct callback retained twenty blocks and 323 canonical operations; its
phase consumed 584,907 work units. This is pre-ranked inspection only, not
normal ranked/formal/target continuation or an artifact-publication authority.

The declaration seal admits exactly the unique root and, when present, the
compiler's exact bodyless trap declaration. Every trap call must have current
typed origin evidence and the exact unreachable shape. Arbitrary imports or
extra declarations still refuse. Result-use scanning enumerates genuine
operands and terminators under its existing 128-use cap; it does not synthesize
uses to make an unused component appear live.

Historical failed gates remain retained: source preparation/scan bounds,
transparent nominal borrow handling, root/function sealing, declaration-test
diagnostics, an overly strict result-liveness condition and a test iterator
compile error. No failed receipt is rewritten as successful qualification.
The temporary root/function diagnostic has been removed.

The separate shape ladder R1 failed correctly on its first single-predecessor
case: the Rust spelling did not produce an observed admitted SSA alias.
Its receipt is 95,450 bytes, SHA-256
`793d132c6e12ad75f5d835d948c1da3627c9aa3c32371ff321e6483ae78064d0`.
This does not qualify alias, phi, loop or retained-memory coverage. The shape
ladder, source-bound helper transport, source promotion, fresh edited-source compilation,
numerical oracle, final-code/resource comparison and public tiled command remain
unfinished. The core ladder alone does not complete U4.

## Same-owner BF16 normal continuation

The separate genuine two-session normal ladder now passes direct compilation
and wrong-launch refusal. After inspecting the live pre-ranked region, it
consumes the returned original compilation through ranked checks, target-neutral
attachment, formal-memory admission, target lowering and the existing inert V2
worker handoff. It checks the full original V12 identity at the target boundary;
no JSON, copied graph or decoded handoff reconstructs a compiler source owner.

The direct target retains twenty correspondence blocks, three formal allocations,
nine formal accesses and eight ranked discharged bounds. One runtime bounds
requirement and two runtime alias requirements remain. They are not claimed
discharged by a host launch. The ordinary compiler LLVM is 52,795 bytes; the
descriptor-bound worker LLVM is 61,355 bytes; the inert V2 handoff is 61,654
bytes. Each is separately retained and read back. One expected MFMA intrinsic
call is present in the ordinary LLVM. The worker handoff grants no compiler,
worker or launch authority.

Four pure observation controls and the completed two-session parent passed in
`logs/phase28-resume-r14-compiler-tiled-normal-continuation-actual-r1/receipt.json`,
180,348 bytes, SHA-256
`ce2b72e8d05a6d5850e91bc39ee6912448cf78603d33dabe96cd892de3ad181e`.
The source census for that gate is
`1d72adaf5ee7ff6da050aa8e856ee5ce8480ecb41d8776aab1968dbd13884659`.
The first phase uses its original ledger and ends at its established boundary;
later stages retain their existing accounting. This is not a single
compiler-wide meter. Source shape coverage, numerical simulation, tiled editing,
final machine equivalence and GPU execution remain unqualified.

## Reproducible disabled debugger packages

`tools/gfx950-one-stop-controller-v1` publishes the standalone MIT/Apache Rust
controller source. It passed 43 library and 29 native-adapter CPU tests,
20 package checks, strict Clippy and a build. Its source-owned runtime profile
remains `None`; no debugger or target was executed. The receipt is
`logs/phase28-resume-r14-compiler-one-stop-controller-package-cpu-r1/receipt.json`,
78,495 bytes, SHA-256
`064e5970c0a02db45ba524d15d6ffe9787421bd43a7ccf83128e323208580bc3`.
The package verifier checks source consistency, not runtime permission.

`tools/rocgdb-one-stop-native-adapters-v1` separately publishes the GPL debugger
patch and exact standalone postimages, with its activation gate false. Its
source verifiers, 47 Node checks and strict inert C++ fixture passed in
`logs/phase28-resume-r14-compiler-one-stop-gpl-and-tiled-focused-r1/receipt.json`,
150,404 bytes, SHA-256
`91f48e98efad3377315bf2456cb523b1f2ec608ad7edb0c5cfcf1650a2b0f03e`.
The package includes no full upstream tree, automatic acquisition or activation.

A distinct private selection-enabled debugger candidate built successfully;
33 source-placement/presentation controls and actual object-layout checks
passed. It has not been launched and is not the disabled published package.
Private family-scope source passed 30 Rust and 48 Node CPU controls; its
execution bindings remain null. The source-only startup MI2 transport
passed 54 Rust and 20 Node controls, with two cleanup-record controls separately.
Its current runtime bindings remain null; no startup command has been run.

A separate physical-snapshot proposal and corrected loaded-closure consumer
passed 26 inert C++ groups and 31 Node checks (four placement and 27 consumer
checks). The consumer's positive baseline now passes before family/cgroup
mutation refusals; the earlier failed baseline remains retained. Receipt:
`logs/phase28-resume-r14-compiler-one-stop-consumer-and-snapshot-cpu-r2/receipt.json`,
149,099 bytes, SHA-256
`73f8da4631862298441f752bc0511a76b47ba2171d4767957b2fa51fc3b979b5`.
That snapshot proposal has not been compiled into a new debugger or activated.

The unchanged static-evidence decoder also accepted the actual retained R5
build records: 136 evidence pins, 149 distinct files and 210,078,984 bytes
rehashed. Its result explicitly keeps startup/native qualification false.
Receipt:
`logs/phase28-resume-r14-compiler-one-stop-r5-static-decoder-r2/receipt.json`,
263,191 bytes, SHA-256
`5fbef228e6eb761055c3816e1ff7a3302748f498f941caed45d9801d1400d192`.
These two private proposals and static observations are not part of the
published disabled packages' runtime capabilities.

A static debugger build, mock stream, source hash or prior empty-queue run is
not a live GPU stop. Same-client startup/ACK, checkpoint consumption, target
execution, current-stop sampling, register/memory data, resume, output/canaries
and complete family cleanup still need their own fresh integrated qualification.
V4 remains open; no live route or support claim is enabled.

## Merged regression checkpoint

The merged Pliron/lowerer/backend all-target suites and unsafe-source inventory
passed 5,810 test executions in 44 result groups (256 ignored), followed by the
backend/extractor build. Receipt
`logs/phase28-resume-r14-compiler-merged-source-and-build-r4/receipt.json`
is 127,890 bytes, SHA-256
`e9528497b39d0d1420136d8aa511da2ff4e38404acb50defff7e8f2d0b74cfc6`.
Its source census is
`2371f34e1c4af91ba2cded8666c4edfe6b677c3b89f6a9bafbf635ef3e44c094`.
This gate includes the unused-result correction and predates the separate
normal-continuation test leaves and subsequent record updates; it is
not retroactively relabeled as testing a later source tree. Counts overlap other
gates; this is not whole-workspace strict lint or hardware qualification.
