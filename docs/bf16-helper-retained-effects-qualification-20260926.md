# Complete source-indexed Final effects — qualified CPU checkpoint

Date: 2026-09-26. This follows the [shared ordinary recipe checkpoint](bf16-shared-root-recipe-qualification-20260926.md) and [source assertion checkpoint](bf16-helper-root-assertion-qualification-20260926.md). It does not complete the nominal root recipe or admit normal helper compilation. Broad accepted exits remain M1/V1/V2/U1/U2/U3 (6/18).

## Implemented boundary

A distinct original-ledger entry retains every source-block row of the actual completed dense Final table: layout, global_read, transpose_workgroup and read_view, including defaults and unreached rows. Fixed TensorLayout payloads are copied field by field; unsupported layout variants refuse. The tensor-only candidate API is unchanged.

The same immutable source owner, inventory, exact Call, canonical facts, real consumer, three dense passes and authenticated Final Call/Matrix/Return candidate are used. The exact block-count buffer and generic frames are prepaid before allocation. No fourth pass, detached caller-supplied effect table, replacement positive ledger or heap-owned blanket clone is introduced.

Capture stays private until all inner source/facts/dense postflights and candidate rejoin complete. The immutable loan then exposes candidate, source function and full effects table. The enclosing entry postflight follows the external callback. Partial buffers, callback captures and panic payloads drop before own-only refunds; callback-owned surplus survives. These are bounded logical-accounting checks, not allocator/RSS or native-stack measurements.

## Completed gates

| Gate | Observed result | Receipt bytes / SHA-256 |
| --- | --- | --- |
| compiler-retained-final-effects-regression-r1 | 331 model tests; 2,557 backend tests, 189 ignored; backend/extractor build and whitespace check | 50,796 / 54181d44d9ebff7a89f61a3e4daa82dc41ad9d62315037d0c5449259b1a9f508 |
| compiler-retained-final-effects-genuine-r1 | Five fresh actual Rust sessions in R15; unchanged original limits | 271,741 / b7d6f6e11b995dd3ac55d1d078048b281f40c0aefba0b8785884a9fb96e96298 |
| compiler-retained-final-effects-lossless-comparison-r1 | 28 pure comparison controls and completed R14-to-R15 lossless comparison | 91,670 / c1f5b7d8af43aad8c224b1a805a2a9769d47cba3f0c24cd4f41b8cbe4aafec75 |

Root independently checked clean exits, unchanged before/after source/input/tool snapshots, selected inputs and actual stream pins. The compiled source census before documentation additions is 8,410 files / 120,188,425 bytes / SHA-256 751020c97da7c36a9c3f418b3bd484c31062d81c0779170929cba5e16b9932f2. Baseline backend warnings remain; no whole-backend strict-Clippy pass is claimed.

The 13 new unit controls cover all four payload copies, defaults/order, unsupported layouts, malformed/incomplete/repeated capture, exact capacities, arithmetic/resource boundaries, partial-copy denials, surplus/error/panic custody and generic frame coverage. They do not fabricate admitted source owners.

## Actual-source evidence and limits

R15 uses Identity, Swap01, wrong-launch, callback-error and callback-panic sessions: 36 positive CPU numerical cases, 32 request refusals, two unchanged normal refusals, two callback controls and one wrong-launch refusal. R15 observation.json is 281,064 bytes / SHA-256 82c289410d064875b6a00a17aa308422144cceee74a32ad2701b9255a194e25f.

The genuine observer checks the actual owner/function/Call/Matrix/Return/four-component associations. It derives two BF16 fragment-load source rows from real intrinsic declarations and requires global_read presence exactly there; the fixture has no layout, transpose_workgroup or read_view entries. It does NOT independently derive and compare AllocationContract payloads. Nonempty transpose/read-view payload equality is synthetic component coverage, not positive real-source coverage.

The roots still contain 19 blocks and zero assertions. No positive actual Assert coverage is inferred.

## Exact comparison and diagnostic multiplicity

Both dependency directories were independently enumerated and all 918 files rehashed: 459 ordered roles and 352,670,356 bytes each, no symlinks or extra files. The lossless core preserves its 138 explicit allowed paths and all other keys/order/types/numeric lexemes. Exactly 132 values changed: 96 cumulative-work fields, 25 generation paths, six dependency digests and five artifact digests.

Numerical outputs, masks, refusal payloads, authority/normal flags, storage and peak values remain exact. Logical phase/reverification peak stays 1,632,943,151; callback storage stays 816,796,628 before / 816,797,844 after. No storage/peak exception, phase increase or probe-cap increase was used.

Each affected stderr has four assertion rows at lines 17/41/43/45 and five retained-effect rows at 16/40/42/44/46. Four pairs are followed by exactly one retained-only row. Source ordering identifies that final row as the one-short-storage probe: the early observer completes, but the later route refuses before assertion output. The completed parent checks require a storage error, no work denial and incoming-floor restoration. The exact later failing allocation was not instrumented. Wrong-launch has neither family. The comparator preserves every duplicate and rejects missing, extra or misplaced rows; no diagnostic row grants whole-route success.

## Work reconciliation

Uniform cumulative-work increments are 25,727,151 for Identity/error/panic and 25,727,219 for Swap01. Four new original-ledger API/observer pairs comprise one standalone observation and three success/error/panic controls.

Explicit control precharges plus callback work are 3*(4*8192+128) + 3*(8*1024*1024+4*8192+256) + 3*17 = 25,363,635. They already prepay the new negative-only independent-ledger probes. Subtraction leaves four inferred per-pair costs of 90,879 / 90,896; these are consistency inferences, not instrumented per-function costs. The original outer six boundary probes remain prepaid by 6*PROBE_WORK+64. Repeated stderr rows do not multiply original-ledger work again.

## Remaining implementation

The retained table is source-indexed, not a ranked block map, access certificate or complete kernel recipe. Still needed: the strict original-meter root adapter, authenticated checked-reference origins, complete conditional output writes and source-expression continuation, exact candidate placement through CFG expansion, assertion overlay, mandatory recipe verification and qualified normal continuation. No nominal LLVM/native/GPU continuation, physical debugger capture, public activation or edited-source promotion is established by this checkpoint.
