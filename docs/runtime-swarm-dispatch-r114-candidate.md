# Runtime Swarm Dispatch: R114 Candidate

Historical planning snapshot. The [post-qualification dispatch](runtime-swarm-dispatch-r114.md)
supersedes its candidate status and immediate assignments; the observations below
remain as recorded at the planning checkpoint.

Planning-only refresh after three read-only, source-grounded worker handoffs.
The accepted checkpoint remains R113
`d85d6d7dc4f3b065dce9dc6505eac065c48cb0fa`; this dispatch does not accept R114,
C1, settlement, native execution, formal proofs or performance.
[Issue #182](https://github.com/harsh-nod/fe2o3/issues/182) remains open, checked
through the GitHub API with `updatedAt` `2026-09-12T10:51:18Z`.
See the [complete packet map](runtime-swarm-next-packets.md) for retained history
and the [detailed work orders](runtime-a1-a2-swarm-current.md) for contracts.

## Current State

| Work | Observed State | Not Yet Established |
| --- | --- | --- |
| R113 / V2 | Locally accepted executable-model membership and whole-roster Begin | Settlement, production journal, authenticated proof and native correspondence |
| R114 / N4-R1 | Uncommitted implementation candidate: active-control custody, borrowed cleanup, deferred live transport and secondary-panic retention; 32 focused pristine-abort tests passed | Remaining test oracles, full/auxiliary checks, compiled negatives, immutable qualification and publication |
| C1 | Isolated candidate: nine focused tests, 724 runtime-library tests and strict all-feature/all-target Clippy passed | Integration and full immutable packet qualification |
| V3 | Reviewed settlement design draft | Frozen contract and implementation |

R114's latest preliminary run is `r114-wired-pristine-tests.json`, with source
map `07e344b44fcffd94ba440272d61b2a49455ba2487fdf98ea5b88dd22fa936c8b`
and R113 as its recorded source HEAD. Its raw log and before/after inventories
remain local under `/home/harsh/.codex-tmp`, not an accepted committed archive.
The earlier compile failures remain retained. No tests, solver runs or hardware
jobs were launched by this planning refresh.

## Swarm Ownership

The three review turns have returned; their implementation queues below are
assigned work, not unattended running jobs. Primary owns edits, shared wiring,
integration, serialized builds, proof/hardware scheduling and publication.

| Owner | Immediate Assignment | Following Queue |
| --- | --- | --- |
| Native: `native_replacement_handoff` | Close R114's remaining oracles and review its qualification | N4-R2 controls, data cleanup, applicable N4-L/live and N4-Q/queue teardown, then N5 DATA-ADOPT |
| Admission: `submission_identity_handoff` | Qualify and integrate C1; review independent C2/C3 coverage | Joint I2 ISSUE, C4 COMPLETE, C5 typed future/join, C6 generated GRAPH/DRAIN |
| Resources: `r102_evidence_review` | Freeze V3 settlement, then review its implementation and cost tests | V4 proofs, V5/V6 journal/hooks, V7 complete writers/recovery, V8 leases; M1-M4 memory bounds |
| Primary | Integrate one reviewed packet at a time; own Q1/Q2/Q3 and release gate #277 | Signed publication to both topic remotes; later A3-A7 integration |

## Native First Exit

R114's source ownership is `fe2o3-kfd/src/shared_memory/control_cleanup.rs`,
the borrowed lower cores in `shared_memory.rs`, and pristine-abort ownership in
`queue_dispatch_binding/pristine_abort.rs` and `queue_live/pristine_abort.rs`.
These crate-relative paths identify candidate source, not accepted changes.

Finish these explicit test-review gaps before qualification:

- Revision headroom: assert the target native record, retained VA and `Active`
  phase after the successful two-revision case; zero/one headroom retain the
  expected mapped/unmapped record without a VA refund.
- Live transport: exercise an active Mapped control, not only Unmapped,
  NativeDisposed and completed cleanup.
- Identity/order: compare against the pre-cleanup kernarg/reverse-code roster,
  including untouched suffix and completed prefix, not only a post-failure snapshot.
- Direct-session finishing: dynamically observe retention before returned errors
  and resumed panics through the actual finishing helper; a source-order guard
  does not qualify that behavior.
- Incomplete callback: returning `Ok` without completing cleanup must retain
  the active control and reject; a retry must not invoke cleanup again.

Then run applicable full/focused/auxiliary gates, decisive compiled negatives,
exact source restoration and independent evidence review. Lower scripted native
leaves and injected facade settlement remain distinct from live Linux/KFD success.

After R114 acceptance, N4-R2 reuses its borrowed cleanup contract for
ordinary/returning controls, rooting the full owner before validation and reserving return capacity
before disposal. Preserve forward-code order and exact returned data. The data
extension covers every supported host/device and initialized/uninitialized
variant, exact retained charges and no repeated free/refund. N4-L and N4-Q follow
the contracts required by each route, preserving owners through model retake and
all teardown prefixes. N5 requires the cleanup paths used by its supported
routes, not every possible destruction mode.

## Admission First Exit

C1 integrates the isolated `fe2o3-runtime/src/context/tests/submission_identity_tests.rs`
and its narrow test wiring/cancel-entry counter without replacing production
validators. Qualify the 80 coordinate/ingress rejection cells, valid controls,
genuine cached completion, backend-ID reuse, destroyed streams and precedence.
Rejected operations preserve supplied handles, retained owners and pre-existing
callbacks before any backend entry. Rejected callback registration drops its
new callback without invoking it. Preserve the original failed compile and
preliminary results.

C2 and C3 can be reviewed independently; their proposed files remain absent:

- C2: `authorized_execution/tests/generated_identity.rs`. Every descriptor
  coordinate in both directions; genuine control transfer followed by immutable
  source use; later artifact/authority/currentness substitution must retain custody.
- C3: `async_engine/tests/owned_tests/preparation_tests/completion_tests.rs`.
  Two-owner isolation, Stop-before-disposal, A-success/B-failure/C-retained
  retirement, no retry or second reply, latest-waker and panicking-wake behavior.

Those two paths are relative to `crates/fe2o3-runtime/src`. Reuse existing
reservation/adoption fixtures; no dummy native completion adapter. Generated
production preparation still installs no adoption hooks. Existing ordinary
typed async launches, graph/drain machinery and decoders are extended, not rebuilt.

## Resources First Exit

Freeze the [V3 draft](runtime-context-version-settlement-v1.md), then extend the
existing journal and tests. Success commits admitted epochs as lineage;
NoEffect preserves lineage and burned epochs; both release exactly the retained
members and writer. Unknown retains its complete chain and is sticky.

Validate reference/phase, applicable evidence and the complete touched chain
before mutation. Success/NoEffect additionally validate return headroom and
scratch; Unknown requires neither. Reuse all seven vectors, with O(k) touched
work and no commit-time growth or unrelated-arena scans. Require independent
reference traces, full arena auditing, rejection/replay/corruption snapshots,
NoEffect gaps, MAX exhaustion, Unknown capacity retention and fixed-k work tests.
Preserve all 23 accepted journal tests. Model evidence remains inert, not
authenticated production authority.

V4 proof work can start from accepted V1/V2. M1 aggregate-domain design, M2 native
cost inventory and M3 host-image ceilings can also start independently. V5/V6
integrate the production journal and initial hooks; V7 covers all writers and
explicit Unknown recovery; V8 adds exact input leases. M4 closes total retained
memory only after concrete owner, journal, lease and cache accounting is joined.

## Integration And Acceptance

1. Advance N4-R1, C1 and V3 independently. Review C2/C3 and M1-M3 without waiting
   for their later production joins. Serialize edits to shared modules and builds.
2. Required N3/N4 paths feed N5. N5 plus C1/C2/preissue-C3 feed joint I2 ISSUE,
   with the mutation-hook policy specified first. C4 COMPLETE then enables typed
   output and generated GRAPH/DRAIN integration.
3. A first non-reusing ISSUE does not wait for V7/V8. Journal-enabled execution
   still needs V5/V6. Cross-run reuse requires complete V7/V8 and exclusive graph
   reservation; kernel reuse additionally needs admitted compiler effects.
4. Q1 authenticates property-specific proofs and actual Rust/model correspondence.
   Q2 supplies native depth, pressure, teardown, bounded-memory and device-timeline
   overlap evidence. Q3 freezes comparable KFD/HIP/HSA workloads and full-output
   checks before tuning; retain raw repetitions, bandwidth, latency and resource use.
5. Rotate these same lanes through A3 local multi-GPU, A4 authenticated two-host
   control, A5 distributed transfers/collectives, A6 fault qualification and A7
   performance/production closure. A1/A2 alone cannot close #182.

Worker V3/capsule and compiler semantic-to-machine authority remain external
production gates. Fixtures cannot supply their concrete backend or owned proof
artifacts. Primary also owns the separate [#277 release audit/CI gate](https://github.com/harsh-nod/fe2o3/issues/277);
its external teardown report does not qualify this topic's candidate. Preserve
R103's default-concurrency musl failures for investigation rather than replacing
them with focused passes.

Publish this dispatch separately from unfinished runtime source. Before starting
R114's immutable campaign above the planning commit, pin a new runner/campaign
to its actual HEAD. Preserve the R113-parent preliminary chain unchanged; never
rewrite its recorded source identity or count it as the new campaign.
MI300X remains Primary-scheduled with task-owned staging/processes and cleanup;
disruptive fault tests need an isolated window on the shared machine.
