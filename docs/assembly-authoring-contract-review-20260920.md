# M0 / V0 / U0 review refresh — 2026-09-20

Status: **proposal; owner acceptance remains outstanding**. This refresh
supplements [the decision/fixture/budget packet](assembly-authoring-contract-review-20260919.md)
and [integration contract](assembly-authoring-integration-contract-v1.md).
It corrects temporal implementation status, not the original acceptance
criteria, target inventory or later milestones. No new schema/policy identifier,
production authority, accepted SLO or shared path ownership is created.

Assessment input: clean local compiler `75fc3304846e5ef6b4ebc094d90acc5024e3fe42`;
published site `baec79a49dfdcb78cfbc8db70233b8ccaf16bd3c`.
Canonical compiler main is `d10f49bfedc26848285d20ec1399c193b3340f47`;
mirror main remains `0b0e6c0d22f4aa5b10ddae702575eb85c7250ca7`.
The local compiler has not incorporated d10f49bf or been published on either
main. Both canonical commits d10f49bf and inherited 7dfbe5cc lack the author's
exact DCO trailer; no disposition, new exception or history rewrite is inferred.

## Evidence now available for the existing decisions

| Decision / fixture | Qualified evidence since the earlier packet | Boundary still requiring acceptance or implementation |
| --- | --- | --- |
| D2 / F7 whole-body mechanism | [Whole-body Function-shell transport](whole-body-transport-qualification-v1.md) passed normal worker/link/metadata/independent decode for naked and ordinary functions at O0/O3 | The module-assembly-only failure remains historical. A working bounded Function mechanism now exists; owner selection is still required. The tested body has no useful input/output; source-fed ABI, branches/helpers/resources remain M2. |
| D3 / F2–F3 source promotion | Actual typed V8 source owner, new-file candidate and fresh MIR32/KIR17 source path; [default low-register native qualification](evidence/default-source-native-20260919.md) and separately pinned edited native observations | Default native evidence is no longer missing. Release-active headless source selection, dependency placement and publication API remain unapproved. |
| D4 / F4 persistent intent | [Retained recipe qualification](evidence/recipes-cursor-generated-refusals-20260919.md): two generations, seven replays, 15 callbacks/210 whole-kernel simulations, six exact refusals, real source edits and regeneration | The old description 'unrun codec' is superseded for this private implementation. Public schedule placement, fixed production policy, numerical/resource applicability and exact-versus-advisory outcomes are not supplied by it. |
| D5 / F5 source/capture identity | [Actual source/catalog/capture join](evidence/source-candidate-debug-join-20260919.md), fresh variant captures and actual stale-consumer refusals | No public V17 source-map transport or common final-artifact/allocator lineage is manufactured. |
| D5 / F6 runtime/lifecycle | [Cursor qualification](evidence/inspector-cursor-qualification-20260919.md) and [lifecycle/console qualification](evidence/lifecycle-console-20260919.md) | Actual create/release and repeated dynamic source execution exist. Public same-capture origin/resource selection and actual allocation reuse/generation remain missing. The paired-sink method is a normal simulator addition, not test-only, and its review remains pending. |
| D5–D6 / F8 consumer lessons | Published local resource importer and instruction-edit/debugger lab; current-host lab checks 768 output words, 96 guard bytes, two debugger sessions and two exact frontend refusals | Public fixture revision and measured host-tool revision are distinct. This is not a clean-public-host reproduction, source-promotion API, complete small+tiled workflow or curriculum-pin change. |

Each linked qualification retains its own exact source/tool/receipt domains.
Do not combine independently successful source, native and capture observations
into a new end-to-end artifact receipt. M1/V1/U1 remain the three qualified
original exits; fifteen original milestones remain open.

## Concrete responses needed

The [five-decision headless proposal](https://github.com/harsh-nod/fe2o3/issues/271#issuecomment-5739212828)
is still outside the repository. The
[compiler owner's response](https://github.com/harsh-nod/fe2o3/issues/271#issuecomment-5739281003)
acknowledges it but explicitly does not approve its normal dependency/publication
API. Please approve or correct each existing decision separately:

1. D3: fresh live HIR/semantic owner selection versus historical U1 selectors.
   The proposed narrow source entry must not rebind V6/KIR11 observations to
   a V8 owner or trust user paths/spans as compiler custody.
2. D3: normal dependency placement for inert source formatting and file custody;
   move the statically reviewed shared core, pending owner acceptance, into
   release-active use only through the accepted compiler-owned entry. Explicitly
   decide its public Rust API/lifecycle and normal non-test qualification.
   No standalone report becomes an owner.
3. D3/D6: new-file publication outcomes, including the conservative possibility
   that publication created a candidate before a later I/O failure. No original
   overwrite, automatic continuation, rollback deletion or blind retry.
4. D2: accept/correct the measured Function-shell transport and exact ABI/
   finalizer obligations. A future source-fed body must distinguish compiler
   ABI setup from authored instructions and verify the whole decoded entry.
5. D4: identify the existing schedule-intent/fixed-policy adapter and exact/
   advisory applicability semantics. The private codec is not a permanent API.
6. D5: review the paired simulator sinks and move-owned same-capture lifecycle
   owner before public query wiring. No monotone allocation ID, generation 0,
   stack depth or static site substitutes for real reuse/activation identity.

These responses can be bounded by existing profile/cell and need not wait for
all matrix, memory or tiled implementations. Conversely, they cannot delete
those requirements from later milestone exits.

## Measurements and unchanged hard limits

The site's [production-preview baseline](https://github.com/harsh-nod/fe2o3-kernels/blob/baec79a49dfdcb78cfbc8db70233b8ccaf16bd3c/docs/authoring-ui-production-baseline-20260919.md)
now supplies 20 actual browser samples with first/warm open, selection and
reverse/repeat observations plus heap/RSS checkpoints. Its measured source
commit is b3272c21, not the later publishing site commit. This supersedes the
old 'no current production-navigation measurement' statement, not accepted
latency budgets or measurements for newly added panels.

Hard bounds remain distinct from proposed performance SLOs: authoring pages
64, regions 64 operations/256 values, original source/report bounds; resource
queries 256 items/scans, 64 visible access rows and 256 visible memory bytes;
each importer retains its own tighter cap. Task supervision remains two Cargo
jobs, 20 GiB combined charged storage, 40 GiB free disk and 64 GiB available RAM.
No root-wide disk cleanup, foreign worker termination or limit widening is
authorized by these measurements. New latency/RSS claims need exact new inputs,
cold/warm distinctions and all retained samples; logical capacity is not RSS.

## Current parallel scope, not additional qualification

The [bounded continuation scope](https://github.com/harsh-nod/fe2o3/issues/271#issuecomment-5751876408)
covers a new source-fed diagnostic whole-body example/native test and a
site-only target-bound LDS model. At this refresh they are drafts, not passes.
Neither touches Source257/258, #275 call/memory/ABI, Policy8/history, shared
production/descriptor/worker roots, the Pliron pin or protected/default entry
points. Selected CPU ranges cannot establish native LDS transactions, physical
base, bank conflicts or timing; model assumptions must remain explicit.

All three original contract checklists remain unchecked pending the named
owners' explicit decisions, exact fixture/profile acceptance and budget policy.

## Dated continuation result — 2026-09-20

The initial draft-only snapshot above is now supplemented by
[source-fed body qualification](evidence/source-body-abi-20260920.md):
fresh source, 96 CPU cases, useful slice/value ABI, O0/O3 normal worker/link
and complete-entry decoding, 17 LLVM negative controls and two actual-source
refusals. The old no-input/output transport fixture remains a separate
observation; these source-fed results do not rewrite its receipt or close M2.

The site's selected-access LDS bank model is now published on main at
`d353a2f92bae692dd5d54a91be3f350a6ce8b4d8`, with 442 Vitest/5 lab controls
and 86 browser tests. The
[guide](https://github.com/harsh-nod/fe2o3-kernels/blob/d353a2f92bae692dd5d54a91be3f350a6ce8b4d8/docs/lds-bank-analysis-v1.md)
keeps assumed base residue, target model and CPU observations distinct from
physical placement, native transactions, conflicts or timing. V2/V4 remain open.

The [updated publication coordination](https://github.com/harsh-nod/fe2o3/issues/271#issuecomment-5734666719)
identifies an independently qualified, remote-specific route: normally merge
signed work into current canonical main, and port only signed authoring changes
onto mirror main without importing origin-only unsigned ancestry. This is not
a DCO waiver, permission to forge trailers, or a completed publication claim.
Each candidate must preserve peer changes, pass its exact introduced-range
checks and be pushed without force; old source receipts keep their original
input domains. Public source-promotion and paired-sink acceptance remain
outstanding independently of publication.
