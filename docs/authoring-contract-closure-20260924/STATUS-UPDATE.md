# Minimal status-refresh proposals

These are **unapplied prose proposals** against compiler5a2d4043c64cdf09ab798587ce54fc51f0d3f24d, `docs/assembly-authoring-implementation-status.md` (87,408 bytes, SHA256 edc6068db0fc8f24284c8b5ebf2c38b807bb02cefd90d417bc49b977f00341a0). They do not change C/S, issue checkboxes, FE2O3_PIN or maturity defaults. Root should merge only the selected prose hunks after reconciling later edits; do not replace the whole current file.

## 1. Replace only the three stale summary rows

Keep the surrounding table's “landed evidence versus remaining acceptance” distinction and preserve the six accepted exits.

### M0 row (published line867)

Proposed replacement:

| M0: contracts and ownership | The bounded scalar/ordered source slices, actual helper composition, public source promotion and V20/V21/V22 complete-entry source/normal/static observations now supply concrete architecture, extraction, ABI/resource and emission evidence. The catalog covers CDNA3/CDNA4 metadata; executable coverage remains profile-specific. See the dated qualification records and proposed consolidated M0/V0/U0 matrix. | Accept the exact current supported/pending target/operation/bug and shared observation contracts with all required owners; resolve measured generation/validation/inspection budgets and preserve disjoint ownership. Later gfx950/memory/matrix/production cells remain explicit dependencies, not presumed absent evidence for already qualified prototypes or automatic broad acceptance. |

This corrects the obsolete implication that only the whole-body prototype rejection exists. It does not claim M0 or every whole-kernel requirement passed.

### V0 row (published line879)

Proposed replacement:

| V0: contract and inventory | Real ordinary helper/loop/storage-reuse observations and exact source-variable/SSA/memory captures, typed V20 local CPU live/recorded and V21/V22 recorded profiles now identify concrete available and unavailable fields. Historical resource/browser timing measurements and current hard import/session caps are retained separately. | Obtain #215/#216/compiler/tutorial acceptance of the consolidated backend/capture/selection/resource matrix and exact schema/fixture handoffs. Agree applicable measured load, heap, interaction/paging/reverse budgets; old timings are not measurements of current large imports or hardware capture. |

Do not replace “measured UI budgets” with only ledger quotas or test counts. A rendered page does not supply missing producer/currentness authority.

### U0 row (published line890)

Proposed replacement:

| U0: editing and ownership contract | Exact source selectors/stale checks, actual create-new public promotion and fresh candidate admission, bounded composition/helper source actions and named two-order recipes now provide concrete positive/refused editing and invalidation evidence. CPU snapshots and reverse remain observational. | Accept the complete per-level action/target/family matrix, source extraction/insertion/specialization ownership, fixed-policy recipe applicability and snapshot/mutation boundaries with the required owners; agree authoring budgets and representative independent fixtures. Preserve pending tiled/general-target actions and no raw intermediate resume authority. |

Do not describe the already published narrow promotion API or qualified two-order recipe as wholly missing. Do not broaden them to arbitrary lifting, transformation or tiled edit support.

## 2. Mark older handoff prose historical

At published line896, propose renaming:

`## Concrete dependencies and next owner handoffs`

to:

`## Historical phase-three dependencies and owner handoffs`

Add immediately below:

> This is the phase-three handoff snapshot. Later dated qualification sections above supersede its implementation-status statements where explicitly recorded, but do not erase unresolved owner contracts, historical failures or original milestone scope. Use the current consolidated supported/pending matrix and explicit acceptance records for new decisions.

This preserves exact old coordination rather than editing historical failure/lease evidence. In particular, the older recipe-pending prose must not override later accepted U3 evidence.

## 3. Link/update placement

If this packet is accepted for publication as a proposal, root may add one link near the three rows to the newly published consolidated review and budget/evidence record. Do not insert a private absolute path, a link to a nonpublished donor, or a statement that owners have accepted it. Preserve the historical September19/20/22 reviews byte-for-byte; add a new dated successor instead.

No new milestone checkbox or accepted-count update is part of this patch proposal. A future M0, V0 or U0 exit requires its own documented decision after the matching DECISIONS checklist is satisfied.
