# R109 Live Device Insertion Evidence

## Scope

Local source/test acceptance for N3-L1 initialized-device append, explicit
insertion and remembered-hole replacement. The production-used sequencer keeps
the original R107 initialization root outside the model loan, reserves bounded
identity storage before effects, retains completed output through failed retake
and commits metadata before extraction. The selected facade restores the
auxiliary before deferred parent transport, including swallowed errors.

The [contract](../../runtime-live-device-insertion-custody-v1.md) describes the
architecture, error precedence and verification boundaries. Exactly twelve
non-doc files differ from accepted R108
`9171bd68d920681e524d8eda5a29053a8fd8ae88`; the publication parent is planning-only
`17d0be5476a4f71c473a619903c62aa316a96fc7`.

This is not live Linux/GPU qualification, authenticated formal correspondence,
aggregate-memory closure or a HIP/HSA performance result. No SSH, GPU or solver
run was performed for this acceptance.

## Accepted Checks

| Check | Result |
| --- | --- |
| Frozen source campaign | 17/17 gates; source unchanged |
| Auxiliary campaign | 10/10 gates; exact named rosters retained |
| GNU tests | 2,619 passed, 5 ignored, 0 failed; 48 harnesses |
| Musl tests | 2,619 passed, 5 ignored, 0 failed; 48 harnesses |
| Frozen and restored insertion suites | 14/14 each |
| Frozen and restored initializer suites | 11/11 each |
| Frozen and restored model-loan suites | 4/4 each |
| Compiled behavioral negatives | 11/11 compile and fail exact planned assertions |
| Evidence-clock contract tests | 16/16; eight exact helper hashes pinned |
| Source restoration | All 5,673 non-doc path/hash identities match |
| Final collector | Exit 0; closed transcript binds exact collector and summary |

The source and auxiliary commands match accepted R108. GNU/musl passing-name
multisets add exactly fourteen new test functions. Three unchanged documentation
fences in `queue_live.rs` move by two lines; the collector permits only those
three exact name relocations after comparing their fence bytes. Compiler/linter,
documentation, host/macros, Python, format/whitespace, dependency/policy,
production metadata/audit and standalone lockfile checks remain intact. Proof
inventory is not a solver rerun.
Raw Cargo logs retain their original trailing blank lines and are excluded from
the edited-source/documentation whitespace check, not normalized after capture.

Ten new functions exercise the shared sequencer with the actual lower initializer
and one original scripted memory engine containing a constructed primary and
auxiliary. The native/currentness matrix has 34 faults at three ordinals,
with independent exact progress, identity, charge and untouched-owner assertions.
Other cases cover capacity 15-to-16/full rejection, real release-created holes,
ordering, reserve/entry failures, retake outcomes, panic precedence and commit
entry. Four functions cover concrete preflight/missing-engine facade behavior
and source-routing guards. They do not establish native success through the
concrete Linux facade.

Primary detached metadata is fixture-local; auxiliary metadata is owned by the
actual constructed lane. A later ordinal relocates that same auxiliary behind
a vacancy, not an extra constructed lane. New composition uses configured
accounts; accepted lower initializer regressions retain both account modes.
Dynamic commit-entry custody and source-guarded full commit order are distinct
evidence. Stable vector capacity is not global allocator instrumentation.

The eleven negatives cover omitted reservation, seventeenth admission, ignored
retake error, dropped or still-usable completed custody, missing or cleared
terminal transport, incorrect process-wide poison, early extraction, append
instead of insertion and ignored release holes. Each fails its named behavioral
assertion, not a source-routing guard or compile failure. The original source is
restored and all hashes checked after every mutation.

## Preserved Rejections

The fourteen preliminary runs remain retained, including an initial composition
compile failure and a 13-pass/1-fail expanded matrix with an incorrect expected
dispatch-global poison marker. The reviewed matrix fixes that oracle to preserve
the existing ordinary missing-engine error policy; it does not change production
policy to make the test pass. The displaced session attributes and module-path
wiring are repaired in the accepted source.

The first mutation correctly failed the reservation test but its strict checker
rejected the reported filename. Rust reports the lexical module path containing
`construction_primary/../construction_auxiliary/`; the original checker expected
the canonical filename. The original helpers, manifest, run/log/source map,
restoration and reproducible checker failure remain unchanged and unaccepted.

Fresh `r109-exact-clock-*` helpers were reviewed, tested and pinned before the
accepted eleven-mutation campaign. All recipes, test names, messages and line
numbers match the original, with only the exact reported path substituted.
The final collector verifies the original manifest against its original run's
hash and verifies every originally pinned helper, historical artifact and
contract-test artifact. It does not silently replace the original evidence.

The evidence clock records actual qualifying UTC samples with a separate
ten-second monotonic wait budget and 1,001-sample bound. Raw child-close
timestamps are not rewritten; regressions still reject. Each event binds its
exact predecessor bytes, source identity and expected outcome. No runtime test
deadline or concurrency setting was relaxed.

## Provenance And Next Work

[`test-summary.json`](test-summary.json) indexes all 193 exact raw artifacts.
The fresh manifest pins eight helpers and 126 historical artifacts and fixes
26 run/restoration/validation entries before execution. Archive creation requires
the already closed successful collector transcript to match exactly. Independent
read-only review checked all archived hashes and the qualification rosters.

Native next takes N3-L2 coherent insertion, then N3-L3 uninitialized insertion;
N4 cleanup remains independently reviewable. Admission C1 and Resources V2 can
advance independently. Generated adoption/publication/completion, authenticated
proofs, native reliability and matched performance retain separate exits on the
[current swarm map](../../runtime-swarm-next-packets.md). This packet does not
close A1/A2, issue #182 or full HIP/HSA parity.
