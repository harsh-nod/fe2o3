# R95 Auxiliary Construction Custody: Local Evidence

Source parent: signed planning checkpoint
`cb29dc6216cdf57c76d1f952264ceed9e257c40a`. Implementation began above
`d464dc442c903e6915fe6ed11dcdfff8fee5db94`; the preceding accepted runtime
source is R94 `363ce6b79938def9365016c34f020a00493c1f87`.

This record covers the [auxiliary construction contract](../../runtime-auxiliary-queue-construction-custody-v1.md),
not the complete NATIVE-2B.5 shared-engine/platform matrix. It adds no live KFD,
new Verus theorem, generated execution or performance acceptance.

## Accepted Source Scope

- One externally rooted auxiliary construction scope retains returned data,
  preparation/control/platform prefixes, resource authority and completed lane.
- Terminal settlement retains the exact original parent, including its engine,
  accounts and completion/dependency owners, without replacement authority.
- Actual opening currentness runs inside settlement before model loan and
  preparation; pure journal-capacity rejection remains observation-free.
- Checked installation retains the completed owner until an exclusive exact
  vacancy and the creation gate permit nonallocating, nonfallible installation.

Seven new auxiliary tests cover eleven rejected vacancy plans, three successful
append/reuse transfers, exact ledger storage/records and dependency epochs,
terminal shell/Drop, two completed-lane failure modes, nine operation/retake
combinations, three opening outcomes and successful installation. One new queue
test and an extended existing test check the 253/254/255 history boundary,
retained-queue headroom, poison precedence and absence of backend observations.

The parent fixtures contain actual queue/ledger structures but no native engine.
The operation/retake matrix uses a scripted loan with an actual charged R88
completion token. Opening coverage includes actual missing-engine rejection and
injected panic. These are composed CPU tests, not complete auxiliary CREATE or
doorbell acceptance. Dependency epoch/storage preservation does not claim an
active target-use workload was exercised. Existing primary integration coverage
does not become auxiliary integration coverage.

## Gates

The authoritative final records are `raw/r95-accepted-source-gate.json` and
`raw/r95-auxiliary-results.json`; parsed totals are in `test-summary.json`.

| Check | Result |
| --- | --- |
| GNU runtime all-target suites | 2,516 passed, 5 ignored, 48 harnesses |
| musl runtime all-target suites | 2,516 passed, 5 ignored, 48 harnesses |
| GNU runtime/host documentation | 108 passed |
| musl runtime documentation / host documentation | 91 / 16 passed |
| GNU host / musl host | 258 passed, 4 ignored / 141 passed |
| Generated macro fixtures | 7 passed |
| KFD library harness in full runtime suites | 936 passed |
| Focused auxiliary construction | 7 passed |
| Full source gates / auxiliary checks | 17 / 12 passed |
| Compiled negative mutations | 8 named tests rejected after compilation |
| Frozen non-documentation identities | 5,647 unchanged and restored |

Filtered suites overlap the full suites and are not additional unique tests.
The source gates include both Clippy profiles, Python checker/runner tests,
format/whitespace, dependency policy/tests, CI test-gate checks and standalone
lockfiles. Auxiliary checks include existing primary, ordinary, lower-handoff,
Linux-helper, initialization, transition, preparation and bind regressions,
negative-proof inventory, production metadata and the pure-Rust closure audit.

Model, resource-accounting, completion-crate and Cargo inputs remain unchanged
from R73 `fb1e27e66cee27bb11f0b7c08f2d5994c5d168da`. The 686-file negative
inventory audit is not a solver rerun or new executable-refinement proof.

## Mutations And Attempts

The accepted `v3-` campaign separately removes exact slot identity, reserved
append capacity, completed-lane custody, returned completion-token custody,
original-parent transfer, original-parent poisoning, rooted opening and
retained-queue journal headroom. Every case compiles, fails exactly its named
test with exit 101 and its expected assertion, changes one intended source file,
and has a reproducible patch/hash. All source identities are restored before
the final positive gates.

The first slot mutation exposed a whitespace bug in the patch helper; its
restoration hash check stopped before accepting the candidate. A later
backward-hunk restoration was rejected by apply_patch without writing. Both
tooling corrections are described in `raw/r95-mutation-harness-notes.md`.
The preliminary descriptor/logs and corrected helpers are retained separately;
tooling failures are not counted as successful negative tests. A later
context-free completed-owner insertion failed compilation; that failed
`v3-completed-owner` attempt is retained but excluded. The corrected helper
anchors insertions and verifies the intended mutated file before execution;
the accepted replacement is `v3b-completed-owner`.

The earlier `r95-final` attempt passed all seventeen gates with 2,514 tests per
target, but preceded the opening/poison/guard corrections. It is preliminary,
not final-source acceptance. Initial compilation, six-test focused runs, the
934-test KFD run and later corrected runs remain retained with exact inventories.
The `r95-corrected` attempt passed its first eleven gates, including both
2,516-test suites, but stopped at a single assertion-line formatting difference.
The source was formatted and the eight compiled mutations rerun as `v3-`;
the earlier `v2-` campaign is preliminary. The final `r95-accepted` attempt and
accepted mutation identity check supersede those attempts for source acceptance.

Three read-only workers reviewed native ordering, runtime custody and evidence
scope. Primary owns implementation, integration and all executions. No SSH,
remote staging or GPU workload was started for this packet.

## Remaining Gates

NATIVE-2B.5 must exercise the production-used sequence with the original
preparation/shared foundation/engine and exact platform owners, including every
prefix, CREATE, ID recovery/collision, both doorbell checks and gate/cleanup
failure. NATIVE-2C replacement/insertion follows. Native generated adoption,
unreturned callback-internal custody, aggregate budgets, protected compiler
integration, new adapter proofs and matched HIP/HSA performance remain open.

`source-files.sha256` identifies the changed source/document files;
`retained-files.sha256` covers this record and raw artifacts. No source or timing
result in this packet establishes complete A1/A2, issue #182, or HIP/HSA parity.
