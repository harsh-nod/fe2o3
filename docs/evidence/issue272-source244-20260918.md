# Issue 272: Source244 Attention Inventory Bindings

This is partial M0 inventory work. All full M0-M7 milestones remain open;
`qualifiedPairCount=0`, `requiredPairCount=null`, `inventoryComplete=false` and
`qualified=false` remain unchanged. No compiler, simulator or GPU qualification
is inferred from source bindings or mocked toolchain tests.

## Exact Change

The reviewed worker commit `e4ad7f056aad24ed333114670d734ec381213293` is integrated
as `f9a7d4d8cac4efa01116af7a60eaddafd03dca23` after published source243. Only the
manifest, its contract documentation and two existing test files change. No
Rust source body, compiler code, schema, website or qualification gate changes.

Two existing GPT-OSS candidates now have distinct positive, unqualified fixture
identities despite sharing a megakernel symbol:

| Fixture | Existing Feature | Display Binding |
| --- | --- | --- |
| `gfx950-gpt-oss-pipelined-attention` | `kernel-gpt-oss-decode-pipelined-attention` | GPT-OSS lesson tab 5, function-name byte offset 1508 |
| `gfx950-gpt-oss-scalar-attention` | `kernel-gpt-oss-decode-scalar-attention` | GPT-OSS lesson tab 6, function-name byte offset 3041 |

The bindings retain exact whole-file bytes, package/lock/source contracts,
feature selections and attributed function occurrences. Source or feature
substitution, another same-named occurrence, and changed offsets reject.
Existing CPU-reference coverage is extended with pending obligations. Two
simulation suites are added as pending, with no request/oracle pins or receipts.
Compiler-rejected display labels do not reclassify these valid candidates as
required negatives.

All 48 previous fixtures and 58 previous identities remain unchanged. The
original-47 recovery digest remains
`db1d9a0d5c5aca3b9c76a4713ddd3b22417e41ee24667efae11773dfccdc4215`.
Only the GPT-OSS lesson-level source gap is cleared; the historical
`gemm-proof-plan` source gap remains.

The existing derivation now reports 50 fixtures (11 gfx942, 39 gfx950), 60 known
identities, 121 display occurrences, 56 pending display bindings, three required
negative cases and 62 pending qualification suites. These are inventory counts,
not a final CPU/GPU pair denominator. SIMT/tile variants remain pending and no
qualified pair is added.

## Validation

Primary validation ran on the exact integrated commit with clean-tree checks
before and after. The existing fake-toolchain matrix harness ran all 82 manifest,
12 occurrence and 24 identity tests. A separate CPU-suite planning test passed:
**119 Python tests total**, plus the shell harness. Repeated worker/focus runs
are not added again. The CPU test checks obligation plans, not numerical results.

Shell syntax, manifest validation, workspace/Pliron dependency policies, hygiene,
DCO and whitespace checks also passed. The harness uses fake Cargo/LLVM tools;
it is not an actual compiler build, simulation or hardware run. The source243
Rust validation remains separately identified and is not relabelled as a fresh
source244 Rust run.

Evidence root: `/home/harsh/work/fe2o3-issue272-source244-evidence-20260918`.

| Record | SHA-256 |
| --- | --- |
| Updated manifest | `b0c267a5b5aa18895af662e946952aac34bf24d6935d6b65b04cba9d8389f538` |
| Primary validation record | `0d246adbe5853c628737f976a9c67973f9378a329fee72d7b5f244cb3ccbd97a` |
| Matrix and Python transcript | `c92897c1f5d250353a3fb6b2d57b800f22a8aa79fc89424380af8f2839252f4a` |

The native inventory worker completed its implementation and tests. Two optional
read-only external Codex subprocess attempts failed authentication before doing
work; they are terminal and were not represented as successful reviews. No Qwen
worker was used. Primary review covered the actual four-file patch. Test scratch
was removed and the worker was closed; source worktrees and reports remain.

## Separate Runtime Experiment

Source243's isolated runtime is available, but its first actual-source protected
helper test failed with `TimedOut`, both at two and eight container CPUs. Fresh
fixture compilation succeeded; the proof executor did not produce a passing
result. No GPU ran. A private source245 supervisor-polling experiment is separate
from this inventory packet and must be validated before any claim of a fix.
