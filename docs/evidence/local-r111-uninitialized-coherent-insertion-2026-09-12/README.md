# R111 Uninitialized Coherent Insertion Evidence

## Scope

Local source/test acceptance for N3-L3-C uninitialized coherent insertion and
required-hole replacement. The two existing direct-session APIs reuse shared
settlement and lower allocation/map custody. The actual completed mapped owner
stays outside the model loan through retake and ledger commit; earlier admitted
prefixes stay in their original lower custody. No copy, readback, initialized
authority, selected-lane facade API or generated publication is added.

The [contract](../../runtime-uninitialized-coherent-insertion-custody-v1.md)
defines the boundary. Exactly eight non-documentation files differ from accepted
R110 `356523e6ea8c61c70c6812762aeaab33506de6e2`, also the publication parent.
Issue #182 and A1/A2 remain open. No SSH, GPU or solver job was run; this is not
native execution, authenticated formal correspondence, aggregate-memory closure
or HIP/HSA performance qualification.

## Accepted Checks

| Check | Result |
| --- | --- |
| Source campaign | 17/17 gates |
| Auxiliary campaign | 10/10 gates; exact R110 rosters retained |
| GNU tests | 2,657 passed, 5 ignored, 0 failed; 48 harnesses |
| Musl tests | 2,657 passed, 5 ignored, 0 failed; 48 harnesses |
| Frozen/restored allocation-only insertion | 19/19 each |
| Frozen/restored initialized coherent insertion | 19/19 each |
| Frozen/restored device insertion | 14/14 each |
| Frozen/restored coherent initializer | 10/10 each |
| Frozen/restored borrowed initialization | 8/8 each, KFD only |
| Frozen/restored model loan | 4/4 each |
| Compiled behavioral negatives | 14/14 fail exact planned assertions |
| Evidence-clock contract | 16/16 tests; eight immutable helper pins |
| Source restoration | All 5,676 non-doc identities match |
| Collector and independent archive audit | Pass; closed transcript and exact raw hashes |

GNU/musl name/status multisets equal R110 plus nineteen new functions, with no
removed or newly ignored tests. All doctest names and fence bytes are unchanged.
Host/macros, strict Clippy, Python, format/whitespace, dependency, CI, lockfile and
production closure checks pass. Proof-inventory checking is not solver execution.

The new roster contains twelve constructed-engine tests, three root tests,
three direct-session tests and one wiring guard. Oracles compare actual owners,
native arguments/progress, model projections, ledger order and retained charges.
Successful extents 1/17/4097 have explicit 4096/4096/8192 backing charges. Scripted
backing zeros confer no initialization authority. Cases cover lower failures,
retake/commit failure, capacity, validation precedence, release-created holes,
explicit insertion, one-shot roots and first-panic/retake-error precedence.

Constructed native leaves are scripted and composition uses configured accounts;
direct-root controls cover both accounting modes. Primary detached metadata is
fixture-local; later ordinals relocate the same actual auxiliary behind a vacancy.
They do not qualify extra native lanes. Public coverage exercises two direct
APIs' preflight/missing-engine failures, not facade additions or native success.
Dynamic commit-entry custody and source-guarded full commit ordering are distinct.

All fourteen negatives compile and run exactly one named test, then fail at the
planned behavioral assertion rather than a compiler error or source guard. Two
exercise reused model-loan substrate. Every mutation is followed by exact source
restoration; all six positive suites then pass on the restored source.

## Preserved Attempts

All eighteen preliminary attempts and their 54 log/record/source artifacts are
preserved. Two builds ran no tests: E0405 exposed a missing test-trait import;
E0599 exposed bool markers incorrectly treated as Options. A later strict-Clippy
run rejected an unused model-oracle helper; its existing wrapper was restored
without suppression. These three failed attempts are not passing qualification.

The preliminary root/public run passed seven tests. The first constructed run
passed nineteen but is excluded: its start at `2026-09-12T21:46:28.701Z`
overlapped the formatter's `21:46:18.189Z` to `21:46:41.235Z` interval by
12.534 seconds. Equal endpoint hashes do not establish continuously immutable
source. The exclusion does not prove that compilation consumed mixed versions.
Original timestamps, snapshots and results remain unchanged.

## Clock And Provenance

[`test-summary.json`](test-summary.json) indexes 217 exact raw artifacts. The
manifest pins 129 historical artifacts and eight helpers and plans 35 causal
events. The closed collector transcript binds the exact summary and collector
SHA `9d879159b247621f8b8980e9c4768b757aba53a89b8f0e7a3235d28a2b739869`.
Independent review confirmed every archived name/hash against the originals.

The source campaign's raw UTC interval is 3,024.820 seconds; its monotonic elapsed
duration is 3,182.442 seconds. Preserve both; do not equate them or infer a clock
cause or benchmark result. Later clock-floor checks use actual observations,
bounded monotonic waits and exact predecessor hashes. They do not rewrite time.
Source hashing excludes docs and verifies snapshots, not continuous immutability.

The outer 7,200-second and individual 1,800-second deadlines are unchanged from
R110, as are four build jobs and four test threads. No stack override was added.
Raw bytes, including trailing blank lines, remain intact; only this raw subtree
is excluded from the edited-source/documentation whitespace check.

Native next takes N3-L3-D device allocation/map custody; lower cleanup remains
independently ready. Admission C1 and Resources V2 remain independent. Generated
adoption/execution, proofs, native reliability and matched performance retain
separate exits on the [current swarm map](../../runtime-swarm-next-packets.md).
