# Source250: Exact First-Fill Tutorial Binding

This change closes a displayed-source binding gap, not a production milestone.
The qualified count remains zero. No protected proof or GPU run occurred.

## Source Contract

The first-fill tab now displays the complete published `examples/fill/src/lib.rs`
from compiler commit `a58e4bc7da39c22de24881229315418e1217039b`: 308 UTF-8
bytes with SHA-256
`827ea368df5dd7f429792e0f8a21df79d4d5508525061a844c190da25de54213`.
There is no hidden namespace line or excerpt-to-file digest substitution.

The existing restricted source selector accepts a bare leading inner `no_std`
attribute only at the declared Cargo library root. It still refuses unsupported
attributes, modules, macros and source selections; this is not a Rust parser or
authentication of compiler execution. Selection is by exact declared inputs,
not a kernel name or tutorial-specific branch.

The existing occurrence at byte offset 148 binds to
`fixture:gfx942-fill-simulation:fill`. Known identities remain 60, displayed
declarations 121 and compiler fixture selections 50. Pending display bindings
decrease from 56 to 55. SIMT/tile variants and all required stage evidence remain
pending; the historical 47-fixture figure is not a newly qualified denominator.
`requiredPairCount` remains null and `inventoryComplete` remains false.

## Validation

The worker's complete Python runs passed 86 manifest, 34 identity and 12
occurrence tests. The primary independently passed 34 identity, 12 occurrence
and three focused first-fill tests. A primary repeat of the complete manifest
suite was deliberately interrupted before the wrapper's five-minute timeout;
that run is retained as interrupted, not counted as a pass.

The isolated website patch was replayed onto published main `55bd450` without
copying any original dirty website worktree. The exact runtime projection at
site commit `81dfb69ce673d945053fb90e0450897f9f9ea15a` contains all 56 lessons
and 306 tabs. The full compiler manifest comparison passes, including the
per-occurrence census; `runtimeCensusValidated` is true, `qualified` is false,
and `qualifiedPairCount` is zero. The runtime projection SHA-256 is
`cfdb6b5b4d5507f879a107707df7dbcdfa08d805d90812110d076f69bb6354ee`.

An earlier isolated website baseline had only 36 lessons and correctly refused
the exhaustive comparison. That historical failure is not the current site
status. It and an initial CLI invocation with an invalid argument are retained.

Website validation passed all 386 tests across 30 files with
`npm test -- --maxWorkers=2 --testTimeout=15000`, plus typecheck, ESLint and the
production build. Default five-second test limits timed out in existing LDS UI
tests, including on a serial retry. Those failures are retained; the successful
command is not described as a default-timeout pass. The first run also caught
the whole-file-tab roster's expected seventh-to-eighth entry change, which was
updated to include the now explicitly whole-file fill source.

The website's pinned evidence validator also passes against compiler code
commit `1c0e99b1405896a8d0a5792bba0f592c9584a04c`, whose manifest SHA-256 is
`228dc407e4d5a0f9861de9d6e1e13bc62333f12b856082778a45053d3398c51d`.
It checked 45 commits, 109 evidence records and 112 source tabs, together with
the current local artifacts, getting-started bindings and debugger captures.
The site changes after the runtime snapshot only add the matching compiler pin
and extend the whole-file regression roster; displayed curriculum bytes are
unchanged. This report is a subsequent documentation-only compiler change.

## Evidence

Task logs and runtime projections are retained in
`/home/harsh/work/fe2o3-issue272-fill-binding-evidence-20260919.4aqWRm`.
Compiler changes are separate from Source249's compiled Rust validation and
Source251's conditional-analysis implementation. No generated report is launch
authority, and no failed or incomplete run has been promoted into qualification.
