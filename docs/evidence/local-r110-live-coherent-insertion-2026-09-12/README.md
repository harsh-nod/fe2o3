# R110 Live Coherent Insertion Evidence

## Scope

Local source/test acceptance for N3-L2 initialized coherent insertion and
remembered-hole replacement, including existing owned and borrowed entrypoints.
The production-used settlement retains the actual mapped owner outside the
model loan through retake and ledger commit. Preparation reuses R106's lower
initializer; failed lower prefixes keep their original pending/terminal custody.
Selected-lane facades restore the original lane before deferred parent transport.

The [contract](../../runtime-live-coherent-insertion-custody-v1.md) describes
ownership and error precedence. Exactly ten non-documentation files differ from
accepted R109 `a9e1c73bb9b752610e58be9cd96861962e140787`, also the publication
parent. Existing initialized-device behavior remains covered independently.

This is not successful Linux/GPU execution, authenticated formal correspondence,
aggregate-memory closure or HIP/HSA performance qualification. No SSH, GPU or
solver job was run for this acceptance. Issue #182 and A1/A2 remain open.

## Accepted Checks

| Check | Result |
| --- | --- |
| Frozen source campaign | 17/17 gates; exact source unchanged |
| Auxiliary campaign | 10/10 gates; exact R109 rosters retained |
| GNU tests | 2,638 passed, 5 ignored, 0 failed; 48 harnesses |
| Musl tests | 2,638 passed, 5 ignored, 0 failed; 48 harnesses |
| Frozen and restored coherent insertion | 19/19 each |
| Frozen and restored device insertion | 14/14 each |
| Frozen and restored coherent initializer | 10/10 each |
| Frozen and restored borrowed initialization | 8/8 each, KFD only |
| Frozen and restored model loan | 4/4 each |
| Compiled behavioral negatives | 10/10 compile and fail exact planned assertions |
| Evidence-clock contract tests | 16/16; eight immutable helper pins |
| Source restoration | All 5,675 non-doc path/hash identities match |
| Final collector | Exit 0; closed transcript binds the exact collector and summary |

GNU/musl name/status multisets are exactly R109 plus nineteen new test functions,
with no removed or newly ignored tests. All doctest names and fence bytes are
unchanged. Host/macros, strict Clippy, Python, format/whitespace, dependencies,
CI gate, lockfiles and production metadata/audit checks remain intact. The
proof-inventory check does not execute a solver.

The new roster comprises twelve constructed-engine tests, three shared-memory
root tests, three concrete session/facade tests and one source-routing guard.
Independent auditors compare exact owners, source/retained bytes, native prefix
and mapping arguments, ledger order/capacity, resource charges, original model
and independently derived lifecycle projections. Cases cover capacity, real
release-created holes, explicit ordinals, currentness/native/copy/map failures,
projection failures, failed retake, commit-entry panic and first-panic precedence.

The constructed backend is scripted. Primary detached metadata is fixture-local;
a later ordinal relocates one real auxiliary behind a vacancy, not an additional
constructed queue. Concrete facade coverage exercises preflight/missing-engine
failure, not successful Linux initialization. Composition uses configured
accounts; lower initializer/root tests retain both modes. Dynamic commit-entry
custody and source-guarded full commit ordering are distinct evidence.

The negatives remove completed retention, extract before commit entry, permit
repreparation, overwrite earlier terminal custody, append without a required
hole, override an explicit index with a hole, admit a seventeenth entry, skip
index validation, suppress retake failure and replace the first panic. The last
two exercise reused model-loan substrate. Each fails its exact dynamic assertion,
not a compile error or source guard. All source identities are restored after
every mutation, followed by all five restored suites.

## Preserved Attempts

All fifteen preliminary attempts remain intact, including six rejected runs:
namespace/private-field compilation errors, strict lint findings, an incorrect
checkpoint-inequality assumption, incorrect public preflight error expectations,
and a final formatting check. The matrix attempts retain 14-pass/2-fail and
18-pass/1-fail outcomes. Corrections preserve existing production error precedence;
they do not change that policy to satisfy a test. The final 19-test campaign is
separate from these mixed-source attempts.

The unused collector was corrected during review to derive the eight borrowed
KFD tests from KFD namespaces, excluding two runtime-crate tests. It was then
tested and pinned before any mutation; no used helper was edited retroactively.

The outer multi-gate envelope increased from 1,800 to 7,200 seconds before the
freeze because the expanded serialized source campaign exceeds the old total
budget. Every individual gate still has a 1,800-second timeout. Build jobs and
test threads remain four, and no stack override was added. The evidence clock
uses actual UTC observations, bounded monotonic waits and exact predecessor
hashes; it rejects raw child-finish regressions rather than rewriting them.

## Provenance And Next Work

[`test-summary.json`](test-summary.json) indexes 183 exact raw artifacts. The
manifest pins eight helpers and 114 historical artifacts and plans 26 mutation,
restoration, restored-suite and collector events before execution. Archive
creation requires the closed successful collector transcript to match exactly.
Raw logs preserve original bytes, including trailing blank lines; only this raw
subtree is excluded from the edited-source/documentation whitespace check.

Native next takes N3-L3-C uninitialized coherent insertion, then N3-L3-D device
allocation/map custody; lower cleanup remains independently ready. Admission C1
and Resources V2 remain independent. Generated adoption/execution, authenticated
proofs, native reliability and matched performance retain their own exits on the
[current swarm map](../../runtime-swarm-next-packets.md).
