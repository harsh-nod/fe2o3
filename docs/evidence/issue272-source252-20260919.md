# Source252: Reference Signature and Same-Session Custody

Status: tested compiler prerequisite. This is not milestone completion,
native replay, a semantic-equivalence proof, or a kernel qualification result.
All whole #272 milestones remain open; the end-to-end qualified count remains zero.

## Exact Candidates

- Base: `ebc4fd944ca69684a3c3a4b85b6c720d17d5374d`.
- Initial implementation E: `03ecdd15afb6b7ed272ee47dadf74fd9559f6f99`.
- Authentication-module extraction F: `c201c5183966aa96734b657fb722fef5b93c21e8`.
- Cleanup G: `8b2f55f9f6611766e60c8422a921050ef508f38e`.
- G tree: `dfce7ec09d258fc8d40f39b407e07673c6204f8b`.
- G frozen input: 5,572 files; roster SHA-256
  `15acd60a54dcc595ec3c271f91f1af7de7729c22fee1e4c41bdc5e0f140364e3`.

G's frozen source matches its committed Git blobs and executable modes. This
record is a subsequent documentation-only change. It does not claim integration
or requalification on newer peer main history.

## Purpose

The production importer previously cloned the collector's reference binding.
This change retains the live kernel and reference instances, exact instantiated
Rust signatures, registration identity, and ordered function occurrence. Import
re-extracts the binding from those compiler inputs and compares the complete
result before handing it to the existing production path.

The logical signature projection preserves scalar tags (including `usize` versus
`u64` and `isize` versus `i64`), ordered kernel arguments and leading point
coordinates, reference mutability, scalar/slice shape, represented regions, and
authenticated nominal output-carrier distinctions. Its borrowed relation view
does not allocate another mapping. It is descriptive data, not provider authority.
The existing effect-IR digest format is unchanged.

The projection is intentionally not a complete Rust signature. Kernel return
types and output-carrier index-space arguments remain in the retained raw rustc
signature and instance. Only erased/static regions and the existing semantic
argument-count bound are represented; unsupported inputs fail closed.

## Production Boundary

The importer rejects missing, extra, reordered, changed-role, substituted-instance,
changed-name, changed-signature, and changed-binding inputs. Comparison covers
both complete effect trees, helper identities/operands/summaries, predicates,
loop summaries, and the separately retained observable-output lists. Equality
does not rely solely on cached digests.

Initial extraction, capture, replay, and comparison use the existing cumulative
source-work budget, which continues into source preflight. New allocation debits
describe requested logical payload and copying work. They are not a canonical
live/peak-storage receipt, a physical allocator/RSS bound, or a bound on rustc
query internals. Existing local expression and symbolic limits remain relevant.

The raw-source census checks borrowed HIR/MIR/type records before the existing
identity hashes. It does not bound provider authentication, interning, opaque
identity lookups, referenced allocations, diagnostic formatting, or internal
hashing instructions. Its depth limit is 256; coroutine/coverage metadata, MIR
inline assembly and pattern types are refused rather than traversed without
accounting. These are explicit admission restrictions, not new supported syntax.

Bounded loop extraction uses the inherited ledger too. State copies, map keys,
predicates, CFG/dominator work, summaries and recursive constant folding are
charged before the corresponding work. The constant-folder checks the complete
expression tree even when evaluation would short-circuit. Conservative charging
can reject an input earlier under the unchanged work cap. Ordinary opt0 `while`
MIR may contain unused `!` locals: their declarations are admitted, but executable
place access to those uninhabited values remains rejected.

This does not transport a complete CPU/helper source closure to the independent
native checker. A helper summary and body digest are not the missing helper
body/instance preimage. No V6/V2 wire, new proof authority, old clean-report
conversion, policy activation, finalizer, or generated-host contract is added.
The conditional-stage and independent-replay integrations remain separate work.

## Validation

Candidate E passed 3,658 library tests, with zero failures and 36 ignored:

| Package | Passed | Ignored |
| --- | ---: | ---: |
| `fe2o3-host` | 90 | 0 |
| `fe2o3-lower-mir-kernel` | 969 | 0 |
| `fe2o3-pliron` | 1,319 | 0 |
| `fe2o3-verifier` | 146 | 5 |
| `rustc-codegen-fe2o3` | 1,134 | 31 |

After the module extraction, F independently passed all 1,134 backend tests and
six explicitly invoked reference-binding source test parents. Their 26 compiler
invocations cover scalar/aggregate/multiple/write-only outputs, loops, helpers,
dynamic loops and required invalid-reference refusals. Positive cases stop at
the existing missing-local-proof-runtime boundary; no proof execution or GPU
authority is inferred. The six source parents completed in 339.75 seconds.

G independently passed all 1,134 backend tests after removing an unused getter,
deriving loop-switch fields from the actual header, pairing edge endpoints and
cleaning up test-only lint findings. G also passed:

- The same six reference-binding source parents and 26 compiler invocations,
  independently rerun in 323.28 seconds.
- Two conditional-output source parents across three configurations in 258.80
  seconds: reference fill on gfx942 and gfx950, plus the unannotated refusal.
  These query the actual canonical/source/ranked correspondence and stop before
  proof execution; they do not discharge the full production obligations.
- All 91 documentation tests: 66 lowerer and 25 PLIRON.
- Warning-mode all-target Clippy for the five listed packages. The new reference
  lint findings were resolved; inherited warnings remain. This is not strict
  Clippy or a green whole-repository CI claim.

The E, F and G suites overlap. Their counts are not added as unique tests, and
the broad E suite is not relabeled as an independently rerun broad G suite.

G formatting, both dependency policies, source-manifest validation, hygiene,
both repository DCO checks and whitespace checks pass. The manifest validator
reports 50 fixtures with `qualified=false`, pending semantic oracles/policy
verification and an unmatched `gemm-proof-plan` curriculum source binding. This
is content/source-contract validation, not completion of the original 47 kernels
or the expanded corpus.

Earlier failed runs are retained: two initial disk-reserve refusals before
compilation; B's two pinned-rustc API errors; C's ordinary opt0 loop refused for
unused `!` locals; and E's large-file-growth hygiene failure. No work cap, required
positive or semantic expectation was weakened. The queued E source jobs were
cancelled while waiting for the build lock, before compilation, and replaced by
later-candidate runs. Native workers implemented and cross-reviewed disjoint
leaves; the primary owns integration, guarded serial builds and publication.

Evidence directory:
`/home/harsh/work/fe2o3-issue272-source252-evidence-20260919`.

Prior Source251 evidence remains attributed to its original candidates and is
not relabeled as Source252 qualification.

The owned RAM build cache is retained for active work. All source-test temporary
directories were removed and the scratch directory was checked empty after the
terminal runs. Unreachable UV cache pruning reclaimed 1.1 GiB without force or
changes to source, reports, toolchains or active compiler targets. A read-only MI350 inspection
confirmed the existing protected image
`sha256:aae172a55c7dba4a96c2841320e3c5570037cc6b2febf7e05ded398fc1001742`.
No container, remote scratch directory, provisioning change or GPU job was
created for this batch. Image availability is not a proof result.

## Next Boundary

[Proposal5738854011](https://github.com/harsh-nod/fe2o3/issues/271#issuecomment-5738854011)
describes a session-owned, move-only pending conditional analysis with exact
recipe-occurrence/live-operation correspondence and the complete residual roster.
It cannot convert to the old clean lowering path. Lowerer-owned translation,
canonical/source/ranked composition, independent CPU/helper source replay,
final-native-output preservation and protected host discharge remain required.
The proposal is not frozen wire/API authority or production activation.
