# Source255: Source Translation Before Clean Ranked Admission

This is a checked prerequisite, not end-to-end qualification. All whole #272
M0-M7 milestones remain open. Qualification remains 0/47 for the original goal
and false for all 50 current manifest fixtures. No protected proof, artifact or
GPU execution is credited to this change.

Scope follows lease
[5739662746](https://github.com/harsh-nod/fe2o3/issues/271#issuecomment-5739662746)
and the shared-checker repair approval
[5739839977](https://github.com/harsh-nod/fe2o3/issues/271#issuecomment-5739839977).
These permit independently qualified feature-branch publication, not main,
default activation or qualification of root's separately composed tree.

## Exact Candidate

- Base: `7dba0c1a483fde093a814840ba11b1e2a6191e0c` (Source254).
- Implementation: `ab30812cc03ffe1bb8c3f80d3a53972dd89df63e`.
- Compact custody-error follow-up and final code candidate H:
  `7469a8219995e4cea5d300f76a83d28a59776b14`.
- Final code tree: `b48188357ca4751b933f7e9f8af6d42643271dbd`.
- Frozen H roster: 5,584 files, SHA-256
  `80ae83106202b442b1bce611116d60cc8f23a5000567b57204c4948cf9d6cb34`.

The source roster matches committed blobs and executable modes. This report is
a later documentation-only change and does not relabel another code candidate.

## Implemented Invariants

The existing translation checker now borrows the recipe directly. Its old clean
adapters, mandatory checks, attachment and receipt gates remain in place. The
new `check_conditional_source_translation_v1` query accepts the genuine retained
source/SSA/canonical owner and a genuine frozen pending ranked owner. It checks
actual recipe custody, root and transparent Result-body selection, symbol,
launch rank/layout, source rows, represented effects, scalar values, CFG
relation, synchronization and tensor contracts through the shared checker.

The pending recipe accessor authenticates its retained session/stage/root
metadata. It exposes no mutable recipe, context, session recovery or promotion.
The query result borrows both owners and returns descriptive counts, not a clean
lowering input, refinement receipt or artifact/launch permission. Equal bytes
from a foreign pending owner or a detached recipe do not satisfy custody.
Unsupported UnitLocal helper policy still refuses.

A new negative exposed a real shared-checker omission: an extra unrepresented
ranked write was accepted because the old checker only reconciled supplied
ordinary source rows. The repaired core walks every actual non-private memory
operation and requires exactly one ordinary or independently validated generated
attribution. It covers all six current memory-operation variants, resolves the
actual view's address space, reuses the generated validator's existing location
set, and reports missing or overlapping attribution at exact recipe coordinates.
No second graph, additional walk allocation or replacement checker is introduced.

Private producer omissions retain their existing checks. This is complete
non-private attribution, not all-memory, indexed-address or complete operational
equivalence. A successful query leaves all pending pipeline obligations intact.

## Resource Boundary

The caller must already reserve retained source-owner storage. Association and
native-helper expansion consume the same caller ledger; the shared correlation
checker retains its separate `max_operations * 64` cap, including the new walk.
No budget resets, larger limits or retries are introduced.

Existing source-row/correlation allocations, initial recipe/arena construction,
root printing and allocator/RSS usage are not fully covered by this query.
Custody failures use a compact error with no added allocation or lint suppression;
the pending accessor still exposes its detailed session error directly.

## Validation

Final H passed all five library suites: 3,690 tests, zero failures, 36 ignored.
The breakdown is host90, lowerer981, PLIRON1339, verifier146/5ignored and
backend1134/31ignored. Its exact-source, formatting, both dependency policies,
manifest, hygiene, both-repository DCO and whitespace checks passed. The
feature-enabled ranked-pipeline integration suite passed all 53 tests and both
documentation suites passed all 103 tests. Five-package all-target Clippy passed
in warning mode, with no diagnostic on an added code line. Inherited warnings
remain; this is not strict warning-free Clippy or complete workspace CI.
Both ranked-source parents passed, covering gfx942/gfx950 reference-fill
preparation and unannotated-source refusal. All six reference-binding parents
passed across 26 compiler invocations. Every final gate exited zero, and the
complete 5,584-file frozen source roster was unchanged afterward.

The earlier G candidate at `ab30812cc` passed 3,690 library tests, with zero
failures and 36 ignored; 53 feature-enabled ranked-pipeline integration tests;
103 documentation tests; both ranked-source parents; and all six reference-source
parents. Its Clippy command exited zero but reported four added-line diagnostics
at three locations for a large nested error. H removes that payload. G's results
are historical, not final H qualification.

The new tests cover genuine pending owners, foreign/equal-byte recipes, separate
source and pending lifetimes, Result root/body distinction, root/launch/row
substitutions, allocation and value drift, removed/extra effects, direct/nested
helpers, UnitLocal refusal, retained storage and exact/one-short helper budgets.
The old clean path independently rejects an otherwise valid extra global read.
Existing genuine generated scan/reduction and conservative/private regressions
remain mandatory. A dedicated genuine ordinary/generated-overlap fixture remains
a test gap; the exact-one check is present and independently source-reviewed.

The ordinary-source observer stops before proof execution and artifact creation.
Reference-source positives reach the locally unavailable proof-runtime boundary;
some mutation controls also allow that boundary. Those cases do not demonstrate
semantic rejection by a real prover. Named ABI/source refusals remain required.
Manifest validation still reports pending semantic oracles and policy verification
for every fixture, plus the pending `gemm-proof-plan` source binding. A valid
manifest does not qualify any kernel.

## Failure History

Development failures are retained in the raw evidence. A and B failed compilation
on fixture dependency/call-site adjustments and missing public documentation.
C hit invalid launch geometry in a new fixture; D lacked two test imports.
E exposed the real unrepresented-write gap. F passed 3,688 library tests but
failed two new fixtures: duplicate writes were already rejected by ownership,
and Write/Global allocation effects are not constructor-admitted. Corrected
fixtures use an extra valid read and Read/Global allocation without weakening
compiler checks. G's added-line lint findings led to the separately qualified H.

## Next Production Boundary

The source transaction does not yet consume this pending query. The approved
next scope, [5739906056](https://github.com/harsh-nod/fe2o3/issues/271#issuecomment-5739906056),
factors the actual protected proof/import/bind/stage sequence and observes its
exact construction before clean compilation. It may not remove requests,
fabricate contracts or construct an earlier graph. Even a successful test-only
observation must remain `Incomplete`, preserving residual safety failures.

Full conditional discharge, independent CPU/helper ABI and effect replay,
residual safety checks, aggregate proof, optimized/native-output custody and
safe host completion remain required. No backend activation, host/runtime,
policy, wire/schema, dependency, main or tutorial-site change is included here.
Tests use Pliron `7ebf6e6638c2a3bcec179423993b01211a9689b4`, not root's newer pin.

## Operations

Native read-only reviewers checked custody, helper budgets, shared attribution,
test validity and the next integration boundary. Primary owns edits, builds,
integration and Git. No Qwen worker ran. A worker's bounded SSH inspection found
mi350 reachable and the pinned linux/amd64 Verus image present; it started no
container, proof, GPU job, build, service or remote scratch.

Builds use the retained private RAM cache with locked/offline inputs, pinned
nightly, two build jobs, one test thread, timeouts and unchanged resource reserves.
Six immediately rechecked inactive Cargo `deps` directories were removed,
freeing 997,715,968 allocated bytes. Source, Git, reports, toolchains, runtimes,
models and active jobs/caches were preserved. Restricted process entries are
recorded; unrestricted process visibility is not claimed.

Raw evidence: `/home/harsh/work/fe2o3-issue272-source255-evidence-20260919`.
Prior evidence archives remain unchanged. All validation processes are terminal
and private per-test scratch is empty. The bounded RAM build cache is retained
for the active goal.
