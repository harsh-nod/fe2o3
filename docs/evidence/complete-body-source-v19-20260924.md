# Actual-source complete-body continuation — 2026-09-24 UTC

The bounded Rust-source profile now passes actual-source observation, ordinary
LLVM export and ordinary worker/descriptor handoff for a one-block kernel and a
four-block selector diamond. The final source ladder passed **21 sessions,
384 CPU cases and 15 exact refusals**. This is a compiler-owned ABI/termination
profile, **not** fully author-owned entrypoint ABI/termination or #280 M2
completion. It does not accept additional milestone exits.

The seven public source commands and two actual-source CPU/debugger commands
also pass. Separate native qualification compiles the exact unchanged executable
LLVM prefix for both source kernels at O0 and O3, with 138 mutation refusals.
The earlier [native renderer matrix](complete-body-native-abi-20260923.md)
remains separate synthetic-input evidence. No GPU dispatch, native functional
execution, physical capture, protected finalization or launch authority is
established here.

## Implemented source and checked continuation

The live importer selects the new trusted complete-body terminal only from the
actual retained rustc instance/provider and exact signature. Coordinated terminal
144, intrinsic 92, CombinedV6 and the production-compiler-intrinsic/v6 domain lead
to explicit MIR36 admission; older profiles remain closed. Current source body,
root, call occurrence, launch, ABI, actual operands and physical role bindings
are retained and rechecked. Neither diagnostic bytes nor a caller-created plan
can manufacture source custody.

The sole source materializer produces actual KIR19 blocks, authored steps, SSA
definitions, merge parameters and edge arguments in one immutable executable
graph, with explicit source expansion/elimination correspondence. There is no
V12 projection, second executable CFG or LLVM-text-to-source conversion.
The normal driver takes this source-specific continuation and preserves the
same checked owner through canonical emission and descriptor preparation.

The ordinary root ABI planner remains authoritative. Exact typed source
ownership, promoted GpuKernel ABI, unchanged source-signature identity and
pointer/length/scalar layouts are preserved. The existing ranked safety pipeline
receives a bounded analysis projection with the actual SSA/control/address
dependencies. Typed formal analysis recognizes only the verified authored
u32 operations as memory-free and analyzes the real compiler-owned tail.
This is not a numeric functional-equivalence theorem for the authored arithmetic;
unsupported refinement bindings still refuse.

Each declared scope has the exact gfx942:xnack-, Wave64 and complete-body profile
capabilities. One owned cumulative resource ledger crosses materialization,
checked replay, emission and descriptor preparation; retained receipts and
nonzero floors remain explicit. The added projection reserves a reviewed
256-KiB payload envelope and bounds its vectors/text. Existing rustc, SSA,
ranked-session, formal and descriptor allocation domains remain separately
bounded; these figures are not process RSS or host-OOM guarantees.

The author supplies bounded u32 arithmetic, distinct v8..v63 roles and forward
control flow. The compiler still owns ABI/index setup, the selector boundary,
guarded global store, waits/EXEC restoration and termination. The current source
shape is one block or one selector diamond, one root/no helpers, workgroup
required=max64x1x1 and an explicit finite max_grid. LLVM IR is not bypassed:
the checked canonical emitter places the bounded body in a side-effecting
inline-assembly unit.

## Successful actual-source evidence

The final gate froze HEAD ce6b769d83fd82918ceee23ca6a5756bdeeb91c5 plus
7,208 working-tree files / 107,560,580 bytes, source SHA-256
217a30aed5f579381a9461007bf1f6cf729ab146cab7918f27538e01d245fee2.
HEAD alone did not contain these pending source changes. Before/after source
and selected tool/input checks matched. Later documentation is outside that
tested census, and file hashes are observations, not portable source custody.

The two positive source fixtures each pass three separate live-source sessions:
checked observation, normal LLVM export and normal handoff export. Observation
borrows the genuine checked target for simulation and then consumes that SAME
target through normal worker preparation. The public-driver executable LLVM
and handoff outputs are compared byte-for-byte with the same-source observations;
the Rust qualification strictly decodes the handoff and descriptor.

| Actual source | Authored blocks / steps | CPU cases | Same-owner entry / canonical kernel ID |
| --- | --- | ---: | --- |
| assembly_one | 1 / 1 | 192 | assembly_one |
| assembly_diamond | 4 / 2 | 192 | assembly_diamond |

Source export bytes, sole canonical function/kernel entry, typed emission
identity and sole descriptor entry agree. Descriptor kernel IDs are
708dde27746a708692b39d3c004bd10580e26a0002b62d071e3653cef873e736 and
1f856fc1b83707933092274bcee12344a8c1178ac6e7f3539f426f67449e609b respectively.
These are retained test observations, not standalone authenticated tokens.

CPU cases cover four independent input triples, selectors 0/1/u32::MAX, grids
64/128 and lengths 0/1/63/64/65/127/128/129. Independent Rust expectations check
both diamond outcomes, written values, initialization, two-sided canaries,
untouched in-view elements beyond the grid and immutable requests.

Formal memory bounds remain conservative LaunchEnvelope requirements:
the actual max_grid=[2,1,1] source envelope requires at least 512 output bytes.
Passing a shorter guarded CPU case does not relax this condition or authorize
such a native launch. The tail contains direct GEP(index), not an invented
Select(predicate,index,0) value, and is not relabeled SliceBounded.

Five negatives run in all three modes, accounting for 15 exact refusals:

| Source mutation | Required boundary |
| --- | --- |
| required=max128x1x1 | Complete-body exact64 launch contract |
| Scratch v7 | Reserved compiler register prefix |
| Pass b in a's exact input slot | Source argument transport |
| Only one diamond arm defines output | Output undefined at merge label 4 |
| Omit finite max_grid | Dynamic source envelope refused |

Every negative checks its precise diagnostic and absence of output. A crash,
timeout, unrelated macro error or later unsupported operation is not success.

The lowerer complete_body filter passed **39 tests** in actual-r6, before the
subsequent fixture-only wrong-launch correction; actual-r7 runs the source
ladder itself. The filter covers exact source-coordinate/transport, marker
ABI/layout, materializer/resource and ranked-edge controls. The two backend
raw-to-semantic coordinate controls also
passed. These are scoped filters, not a full backend regression or strict
whole-workspace lint claim. The earlier public-consumer CPU gate passed its
5 simulator / 5 debugger V19 controls, 4 extractor controls, 1 exporter control,
and full simulator/debugger libraries (75 / 176 passed, debugger one ignored);
those filters overlap the full suites and must not be added as unique tests.

## Successful public Cargo and logical-debugger commands

The ordinary binaries were rebuilt, then all seven source commands and both
source-debugger commands passed in a separate gate against the same source
census. These are actual public commands, not private test callback substitutes.
The source commands run both normal LLVM and handoff export: two positive
programs and five exact refusal cases, each with unchanged source pins.

The two debug commands each export actual-source diagnostic KIR19 and run three
independent CPU requests plus three short logical-debugger sessions. Selectors
0/1/u32::MAX produce 19/19/19 for one and 19/23/23 for diamond. Direct inspection
of the six retained result files checked all 128 values, initialization of all
520 bytes and both trailing canary words. Direct inspection of all six debug
response streams checked revisions 0/0/1/2/2 and event cursors 0/0/1/0/0, with
unchanged configuration identity and explicit simulated observations.

The discovered register_values, source_variable_values and hardware_wave_state
capabilities remain unavailable. The observed wave/lane state is expressly
logical_visualization. These six public requests are separate from, not an
increase to, the 384-case maintainer ladder; the short public replay does not
claim to exercise every debugger operation or replace focused SSA/branch tests.
All exported bytes and receipts remain diagnostic observations without compiler,
source, protected-resume, artifact or launch authority.

Inner receipts are under
phase28-complete-body-public-{source,debug}-CASE-r1/receipt.json:

| Command / case | Receipt bytes / SHA-256 |
| --- | --- |
| source / one | 4386 / f2c95657e5fe236b8a6f6d707601903c1e42cb34865c5d59b86b34aec53fdcdc |
| source / diamond | 4414 / 83918bdf55d7011574d1f212406c7a165342881ab8b9f1f7a8a01c605a3ca890 |
| source / wrong-launch | 4031 / 040c7d889f222cd29293152dd5db67814c18e8ba49b299b94a0f3093da631971 |
| source / reserved-register | 4100 / 40f441b36edfa853fd5441ae92c4566b9331d5a32688db239593630f303c1897 |
| source / foreign-input | 4070 / eb257309b97eec4adbd482d742f04e58eae2a856184a96306c865037514420f0 |
| source / undefined-merge | 4058 / a192fbb111ed5830b1ff9fbf7691e75db64753dd1ee255444daf0e37108514d9 |
| source / dynamic-grid | 4045 / 22ecef16ef747409884d37acd152cf13ba01aecb26b5702cd1689621d5e3fef0 |
| debug / one | 13010 / 68e5d4048077b0a54dda498a0c767cb28682a307dfc25658c30caf37a2043ae3 |
| debug / diamond | 13114 / 34b90de169998146cd2ddba3d25de8a29752118f4f4456958d99f3276db76064 |

The public diagnostic bytes are pinned separately from private source-owner
qualification outputs: one is 1,019 bytes /
1bf48d77302689bf514f4236ede0a408cc1bfbd5be9cc2ab7fc1c8458af02965;
diamond is 1,185 bytes /
b1104bbb9496044a10776fed27c00d34354a7ff41291ed7a2c5d1bdcfcdc160a.
Raw byte hashes and canonical identity hashes are distinct fields; neither is
portable source ownership.

## Native compilation of unchanged actual-source executable LLVM

A separate native test harness consumed the pinned final actual-source
observation and its retained canonical bytes, LLVM, worker text, handoff and
descriptor. It checked the same-owner source/canonical/descriptor entry
assertions and ordinary driver byte equality, then compiled the exact
canonical executable LLVM at O0 and O3 under the unchanged assembly_one and
assembly_diamond entry symbols. No text renaming or plan regeneration was used.

The compiled input is the unchanged executable prefix of worker.ll, **not**
the descriptor-bearing worker.ll module. The retained Rust observer separately
strictly decodes and byte-joins the handoff and descriptor. The native fixture
still has synthetic worker identity fields and observation-only entry-symbol
selection; neither the fixture nor the harness reconstitutes source custody
from bytes or admits the protected finalizer. This is not a normal native
worker/finalizer end-to-end result.

All four compilation cases passed post-link target/export/unresolved-symbol/
metadata checks for gfx942:xnack-, Wave64 and required/max64 workgroup size.
Each retains 32 explicit kernarg bytes and 288 total bytes; O0 reports 25
metadata arguments (19 hidden), O3 reports 19 (13 hidden). The native selector
load is ready from kernarg byte offset 28 into s22. This checks the selected
ABI and encoded structure, not native numerical execution.

| Source / optimization | HSACO bytes / SHA-256 | Negative mutation checks |
| --- | --- | ---: |
| assembly_one / O0 | 5720 / b97f27dc600707ae0d9604c48dfe6f74103458c83466e1a0dd80920617911db1 | 32 |
| assembly_one / O3 | 5400 / ee938d6e6d4e5a58c19a4e39aee3518bebab35bbf85ae3a6547a659cdd7b9a9f | 32 |
| assembly_diamond / O0 | 5824 / acb1d69a91d64d0a871edf541760ea285b6f05c518a454d63a4fbf3b6e14784d | 37 |
| assembly_diamond / O3 | 5440 / 48156355db65012979a156f1e6dd20c1b470f4af18b973af9e8a7fd81189b9bc | 37 |

The **138** negative checks comprise LLVM/native/metadata/descriptor/selector
mutations: 19/6/3/2/2 per one case and 22/8/3/2/2 per diamond case. Every retained
mutation result is refused. Separately, the four calls repeat 64 entry-syntax
cases (16 per call) and 12 entry-relation/default cases (three per call);
these are not 76 distinct mutation schemas. The legacy native-symbol regression
also passed its separate ten cases and 352 mutation refusals.

The actual-source native summary is
phase28-complete-body-actual-source-native-r1/summary.json:
5,155 bytes / f7a23e988f29451f9e173ec87c9e8cde018a7a4d6ab6c0c3ddc990d77eb51a0e.
The harness run.mjs is 11,049 bytes /
795cb26174ac2b9409d9bc21b039fe74af38597493ad0735d2b51f286c142973;
its 17-input manifest is 4,506 bytes /
537c08f795c29628a107480cb21447e15168dfd2d9dd1a555233a1786802f925.
Those pins include the source observer and fixture, not just selected LLVM.
The four raw report and HSACO byte hashes were independently checked after
the gate. No GPU dispatch, native functional execution, source authentication
grant, protected finalization or launch authority is claimed.

## Failures retained and corrected

All failed gates remain failed observations, even when they reached a useful
earlier stage. The implementation was not qualified from synthetic unit tests
alone.

1. Initial compilation exposed u64 work constants supplied to a usize ledger,
   a dev-only model crate imported by normal backend code, and newly exhaustive
   simulator error cases. Corrections use the existing ledger type and normal
   dialect-amdgcn facade, without adding a normal dependency or lint allowance.
2. The fixture initially omitted max_grid. It now authors the explicit finite
   bound; a separate omitted-grid negative preserves the honest dynamic refusal.
   The compiler never substitutes a guessed 128-lane envelope.
3. The first real source attempts swallowed the annotation cause. The bounded
   existing diagnostic path exposed it: roots carry a promoted GpuKernel ABI,
   not the marker's Rust ABI. Only the correct promoted root ABI is accepted;
   the original source signature remains independently retained.
4. Review found raw rustc locals/blocks had been mistaken for identity-sorted
   semantic coordinates. The producer now retains and checks the exact
   raw-to-semantic/reverse mapping. The lowerer derives return/argument roles,
   transports their mapped IDs, and joins full block identities. Permuted IDs
   and substituted/duplicate/missing role controls cover the original mistake.
5. The next actual run reached ordinary ABI planning and refused the slice:
   its legacy descriptor census knew only older accessors. A narrow new
   fallback recognizes the actual attached complete-body marker, exact first
   source type/ownership/ABI and field layout, then reuses all ordinary root
   pointer/length/Pair checks. Generic descriptors and old scalar call lowering
   remain closed.
6. That fallback initially required V35 nominal Usize despite MIR36's separate
   ordinary-u64 representation. A bounded cfg(test)-only observation of the
   real owner confirmed matching Pair/ExclusiveOwner, [ptr,length,phantom]
   fields at [0,8,16], mutable raw64 pointer to u32, and Ordinary/u64 length.
   The strict MIR36 predicate now accepts only its exact representation, not
   both schemas. Trusted source wrapper/marker ABI establishes usize lineage.
   The temporary observer was removed; its failed diagnostic run is retained.
7. The one-block source passed, then the diamond exposed a genuine ranked SSA
   error: a merge-defined invocation index crossed to the analysis store block
   without an argument. The existing IndexLessThanArgs edge now carries that
   exact value, and the store consumes its one block argument. The ranked rule
   is unchanged; direct cross-block and missing-edge controls still refuse.
   The reviewed retained vector bound increases from 42 to 43 slots.
8. Both positives then passed, but the wrong-launch negative was intercepted
   by the earlier general typed macro's required/max equality rule. It now
   authors required=max128, valid for that general rule and invalid for the
   complete-body exact64 rule. The expected diagnostic was not weakened.
9. One early lowerer filter matched zero tests; it is not counted. The corrected
   filter ran 14 transport controls and later all 39 complete_body controls.
   A genuinely unused source() getter was removed without suppressing warnings;
   its retained BodyBinding provenance still participates in exact replay.

## Retained receipts

Paths are under
/home/harmenon/fe2o3-authoring-280-282-mi350.4VZ42zNr/logs/.
Each gate row abbreviates the leading phase28-resume- directory prefix.
The phase prefix matters: r6 compile checks and r7 source attempts are distinct.
Failure rows are not successful qualification.

| Gate / receipt.json | Result | Bytes / SHA-256 |
| --- | --- | --- |
| r6-compiler-source-v19-check-r1 | Failed (retained) | 12189 / b7228dd5221921e9bdc9b335dacb5d4a51ad022c12a888d71583bd1e0b3f45ef |
| r6-compiler-source-v19-check-r2 | Failed (retained) | 12577 / de2f45361facc60696f010d904f1ff5ed1e5fccff9ebfb1dc54a4474571bcb6a |
| r6-compiler-source-v19-check-r3 | Passed scoped commands | 20745 / 8326e5406f7ee6f0d63878dcabe3972c4cf90ec82f3e48ff95f0cfe607698ed5 |
| r7-compiler-source-v19-unit-build-r1 | Passed scoped commands | 22480 / c0cbf51dc9f01582a305d714ba151c5fbeb5373f9da5ccf3d6f23b594c83e4e2 |
| r7-compiler-source-v19-actual-r1 | Failed (retained) | 13458 / 3060dd6cdc85ed3aea50f4f1f73cbd9e39dc39aa0c7a979effa8768fdbf670d4 |
| r7-compiler-source-v19-actual-r2 | Failed (retained) | 13265 / 328dd5d3b0083b59a70db6d046637a408ac9b8af5fd979f1d0fc98691e7327f5 |
| r7-compiler-source-v19-actual-r3 | Failed (retained) | 14462 / 767554b289234b1bceb66af71b9c60442bad743dc0cb6da6be7e2a099785021d |
| r7-compiler-source-v19-actual-r4 | Failed (retained) | 13922 / 48bae4b65f2cc53477bcb7b9ddc7b695f02a9ae9149b179483ad14f0012abf76 |
| r7-compiler-source-v19-abi-observation-r1 | Failed (retained) | 13319 / b354a8543d67cb1176f97ff412ba0361854ff3ecb1e105bd0b92d61a99fb18f1 |
| r7-compiler-source-v19-actual-r5 | Failed (retained) | 13922 / fd3518b151f711be6ec7927611bf950cd24cede57e81b0006a0564b20a78e252 |
| r7-compiler-source-v19-actual-r6 | Failed (retained) | 13922 / 0f4d72885c78900fb24f4604a8ba6cf6981f3638313b6f298b3c62ea12aebf3a |
| r7-compiler-source-v19-actual-r7 | Passed scoped commands | 21623 / 209cfe79d19ef1ddffefe5147119687d3cf198f19c23b61c7c5948ac3de8cbe9 |
| r7-compiler-public-v19-cpu-r1 | Passed scoped commands | 23422 / e038a9e9db7b43cf9ed68b7a88725bed3e58a9b336b4ce55a682d4337b8d9635 |
| r7-compiler-source-v19-public-r1 | Passed seven source / two debug commands | 28269 / 7f371f02546baf4bceabd5a6e967f226cc06c9f098a861e482b23c7e715a49fc |
| r7-compiler-native-source-symbol-regression-r2 | Passed ten legacy native cases | 95640 / 295fc9d25dc489561e0f99f0d296d9fb935226a94217478ca4bac6529159eccd |
| r7-compiler-actual-source-native-v19-r1 | Passed four unchanged-source native compilation cases | 119755 / 45fa03e647fe675917756601e04673dd66e34cc390379a17cd8134412949bc13 |

The final observation file is
phase28-complete-body-source-actual-r7/observation.json:
77,983 bytes, SHA-256
33eaf977a1b04ef2864a0d0d7fb11f96ad009f6982c54a1d4548111923daa17b.

## Remaining milestone boundaries

- Public commands, CPU observations and the native executable-prefix check
  establish only their exact documented scopes. The normal protected native
  worker, finalized artifact and runtime launch chain are not qualified here.
- Native numerical execution and hardware observation remain separate from
  LLVM/LLD compilation, structural checks and simulated reference values.
- #280 M2 is still not fully author-owned complete entrypoint ABI/termination,
  broad helper/mixed-source coverage or general authoring support.
- M6 protected finalizer, generated-host, native functional reference and
  hardware qualification are not supplied by an inert extraction handoff.
- #281 V4 physical same-stop wave/register capture is not CPU replay.
  The independent gfx950 no-queue registration observation is not a run of
  these gfx942 kernels and does not transfer hardware evidence to them.

See the [source walkthrough](../complete-body-source-v19.md) and
[diagnostic CPU/debugger walkthrough](../complete-body-debug-v19.md).

## Final merged-tree regression and publication preparation

The compiler candidates were fast-forwarded from ce6b769d83fd82918ceee23ca6a5756bdeeb91c5
to concurrent main 430b9e3f4c83520c3bf6634dcddd19374d7bd178. All 145 pending paths
were preserved byte-for-byte; none overlapped the 35 peer paths. The peer KFD
transaction tests now observe copied transition state rather than dereference
a saved pointer across mutable publication. Both compiler candidates have
identical pending source bytes.

Broad regression exposed two stale test expectations, not relaxed production
validation: the old V35 unknown-version control now distinguishes known V36
from unknown V37, and the simulator CLI help golden includes the explicit
diagnostic V19 switch. Their failed gates remain retained.

Five allocation-free cleanup edits remove a duplicate backend module
registration, make a test-only getter test-only, and use equivalent bounded
iterator/let-chain forms. The current post-cleanup regression passes:

- MIR all targets: 609 passed, one ignored.
- Lowerer library: 1,564 passed, none ignored.
- Backend library: 1,945 passed, 129 ignored. Six duplicate copies of existing
  transport tests disappeared with the redundant module; unique controls remain.
- The earlier broad consumer gate passed 3,261 tests, 11 ignored, across 82
  suites for the debugger, simulator, analysis, optimizer and pliron consumers,
  followed by a backend all-target check. These scopes overlap other gates;
  counts must not be added as a unique-test total.
- Peer-merged default/engineering KFD library, no-queue example and strict
  all-target Clippy gates passed independently.
- All 133 changed Rust files passed pinned rustfmt checking; all five subsequent
  cleanup postimages were checked again without source writes.

The post-cleanup public gate rebuilt the ordinary binaries and reran all seven
source commands and both source-debug commands successfully. Its four positive
LLVM/handoff hashes and two diagnostic KIR hashes exactly match the earlier
public run; all six CPU/debug selector relations still match. The earlier
384-case private ladder and native observations remain separately dated
evidence, not relabelled as this rerun.

The final core/public/lint gates froze HEAD 430b9e3 plus 7,218 source files /
107,707,762 bytes, SHA-256
e2bbbebe14025bc592fce12f55e07d40cda2593ffea91791a8e298cf24b6c4b1.
This documentation was appended afterward and is not part of that census.
The publication whitespace check also removed one extra final LF from the
debug-command script; its Node syntax check passed with no executable change.

### Lint is not globally clean

The non-strict eight-package all-target inventory completed with 463 diagnostic
records / 343 unique diagnostics, down from 479 / 354. The 11 targeted new
diagnostics are gone. Fourteen new result_large_err warnings remain in
production_complete_body_ranked_v19.rs and production_complete_body_checks_v19.rs.
They retain the full typed ranked/pass/refinement failure reports. Replacing
them with stage-only errors would discard diagnostics; unmetered boxing would
change the resource contract. A bounded diagnostic-storage redesign remains
separate work. No warning suppression or strict whole-compiler lint pass is
claimed. Unchanged diagnostic spans alone also do not establish unchanged
warning magnitude for shared types enlarged by this work.

| Retained r7 gate | Result | Receipt bytes / SHA-256 |
| --- | --- | --- |
| compiler-v19-regression-core-r1 | Failed at stale MIR test | 13369 / 08be08ac3051b9b7662b160f2f323ff53dad56576bee8e03ef2d51b4212b7efe |
| compiler-v19-regression-core-r2 | Failed later at stale CLI help golden | 14585 / 8d7256f7e545a335e93cc13dc7cd14d04c4110bc982862cfdce9d73bad2ae8ed |
| compiler-v19-regression-consumers-r1 | Passed | 24258 / e80a9c5e24dcb0a9ca2b1d4c174d04858877be34330e58178b77ff48832e4e5a |
| compiler-v19-merged-kfd-r1 | Passed | 21066 / a73739f02ec21f5907cfc31ff803f4fbd20d9029018d08d101f4e730fbd44cf5 |
| compiler-v19-postlint-core-r1 | Passed | 23552 / d4386cfbe80cead18ff4477de4d264663abd44346ae3ae5fb2d0b84d4ea991e0 |
| compiler-source-v19-public-r2 | Passed | 28850 / 1a809fcf8721e88a737ab297a617054a06624cebd9b755cf22027fa21041c869 |
| compiler-v19-lint-inventory-r3 | Inventory only, not strict lint | 21916 / b8dcc1b61ffe1115039496a6127a08d35545f9918a7b44afce97a8d980864003 |

The original accepted exits remain M1/V1/V2/U1/U2/U3 (6/18).
M2/M5/V3 advance, but the remaining author-owned entry ABI, memory/synchronization,
target coverage, physical hardware, protected finalization and curriculum
requirements are not narrowed or declared complete.

### Source-size policy correction

The first publication policy gate refused the driver module's increase from
1,187 to 1,209 lines (limit 1,200). That failed receipt is retained:
r7-compiler-v19-publication-policy-r1, 12,529 bytes /
d732e03962ec43512eaf3d9b5dd868af2597c97f90430e9adb4129a015e9bf3a.

The three unchanged live-session LLVM/handoff output functions were extracted
into amdgpu_outputs_v1.rs (196 lines); the callback driver is now 1,023 lines.
No target checks, output rules, complete-body dispatch or protected-V3 refusal
were removed. The backend library passed again (1,945 passed, 129 ignored),
then fresh ordinary LLVM/handoff commands passed both positive source kernels
and the exact wrong-launch refusal. The four positive output hashes match the
previous public run.

That gate froze HEAD a6820d41ad5326b6a3019626ef83372ef5b7d2b2 plus 7,219 files /
107,714,241 bytes, SHA-256
a2c95ea4d6465c86f7bb284c8d93468c8530130773bfa29ca1846de1a25aa238.
Its receipt is r7-compiler-v19-output-module-regression-r1, 24,591 bytes /
feef60ffe821accfca348cf14eefe82c539b7077549139fe6373402b0dd45024.
This note was appended afterward.
