# Source235: Preserved Workgroup Provider

This checkpoint preserves an authenticated ordinary provider boundary. It does
not admit execution capabilities, prove CPU/GPU equivalence, qualify a tutorial
pair, authorize a protected artifact, or launch a GPU kernel. M0 remains partial,
M1 active/incomplete, and M2-M7 incomplete.

## Source

- Previous integration: `db132062461ff1579caedfbb93e49e99578b607a`.
- Published main integrated by normal signed-off merge:
  `0f8343cabba2d3c9820fe4e6eddff11e1a238ef2`.
- Merge: `d5be542f9630b0cf57f2b030159b5bc1305e49d3`.
- Implementation: `d6485e8434c843cefa34b107433f63c7de9b5853`.
- Frozen candidate C: 5,343 files, exact roster SHA-256
  `13adfee7a386ed14beb8fe22d9d75817472bba153b4e7a4776e46e5521f5c398`.

This evidence document is subsequent documentation, not part of the tested
source roster. Later main `03123cb902ee26be88cf8cc1fd4492f5898dfe90` is not
part of this checkpoint. No peer unpublished work was imported.

## Change

`KernelContext::with_workgroup` retains its ordinary body through MIR with
`#[inline(never)]`. Existing reviewed-source authentication recognizes
`ExecutionWithWorkgroup`, diagnostic item `fe2o3_device_with_workgroup_v1`, and
exact compiler path `fe2o3_device::execution::{impl#2}::with_workgroup`.
The existing reviewed-helper predicate traverses the body; terminal
classification returns `None`. No intrinsic, wire tag, scope receipt, source
census exemption or lifecycle admission is added.

The complete reviewed device source closures were measured again:

- Canonical: `46f15615e7f9012f31449976f974526045d77273c93b2ea7416ae15a5df373c2`.
- Cargo-vendored: `a8fa402e84d774819ea28c8153e845f27e0eeddbf33219850b2eb111ec670565`.

The source test authenticates the live diagnostic DefId, derives its existing
item-definition identity, and compares that identity to the actual semantic
body containing WorkgroupDerive. It asserts a separate Defined provider call
and normal exit, retains repeated-process determinism and distinct source
identities, and leaves the exact V29 materialization refusal unchanged.

The literal observation follows only unique explicit return writers through
actual Defined call destinations. It is a bounded, work-charged diagnostic,
not an evaluator or proof of CFG reachability, alias freedom, output transport
or functional equivalence. In particular, a discarded callback result is not
the kernel's output.

## Validation

Candidate C passes the six-case rooted source-safety parent test (230.60 seconds).
It includes an unsafe callback in an ordinary generic helper and a
separately defined unsafe callback factory compiled at O3/MIR2. Both require
collector diagnostics and the rooted user-callback chain, not a macro rejection
or a later materialization refusal.

| Gate | Result |
| --- | --- |
| Backend library | 833 passed, 9 default ignores |
| Workgroup source matrix | 1 parent passed, 40 observations |
| Rooted source safety | 1 parent passed, 6 cases |
| Actual frontend and importer spoof/control gate | 1 parent passed, 4 cases |
| Callback-free source control | 1 parent passed, 16 observations |
| Device API UI | 1 parent passed, 183 compile-pass/fail cases |
| Merged lowerer library and semantic integration | 728 + 136 passed |

Total: 1,702 passing Rust tests and 9 default ignores. Children, repeated checks
and individual UI cases are not added to the parent Rust test totals.
Formatting, diff, hygiene, dependency/Pliron policy and DCO
checks pass. No full-workspace, strict-Clippy, hosted-CI, protected-proof,
simulation or GPU execution result is claimed for this checkpoint.

The completed full source matrix covers gfx942 and
gfx950, O0/O3, MIR0/MIR2, and five constant/capture/repeated/discarded cases:
40 observations, parent test passed in 84.35 seconds. Every observation has
one provider call, one normal provider return and one RustCall function.
All still stop at `execution capabilities require checked canonical KIR materialization`.

Matrix log SHA-256:
`3b3449c501ffa20562e6f54483eea9cb780ad173863f722494df984320aaac2f`.

Intermediate failures are retained: candidate A had two stale registry/golden
assertions; candidate B's matrix assumed a direct literal instead of a retained
callback call; its unsafe-callback fixture was rejected by the kernel macro
before reaching the intended collector check. These were corrected without
relaxing production admission or accepting an earlier refusal as a pass.

Two native read-only reviewers checked authentication, traversal and diagnostic
scope; the primary agent owns implementation and validation. No Qwen worker was
used. Exact invocation/scope roster custody and checked materialization remain
with the coordinated #275 owner.

## Resources

All three SSH hosts authenticated. Builds use mi300x, pinned
nightly-2026-04-03, the shared Cargo lock, one build job and test thread, a private
RAM-backed target and temporary directory, a 1,800-second command timeout,
8-GiB address-space limit, combined owned scratch below 4 GiB, and 40-GiB shared
disk/RAM reserves. Exact source files are checked before and after each gate.
GPU visibility is disabled for these source and CPU tests.

Local evidence directory:
`/home/harsh/work/fe2o3-issue272-provider235-evidence-20260918`.
All 22 remote log/exit files and four runner/source inputs were archived and
SHA-256 verified. Archive manifest:
`07030214d0e97644b8b1870e8a5e0d397f2920d32390fde3fae4b4e0dcecde35`.

After all jobs and reviewers were terminal, cleanup checked host, UID, private
directory ownership and live-process absence. It removed only our persistent
127,926,738 logical bytes and RAM-backed 2,255,030,046 logical bytes, then
verified both directories were absent. Other worktrees, caches and proof
installations were untouched.
