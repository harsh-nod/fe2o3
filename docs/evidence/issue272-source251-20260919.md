# Source251: Conditional Paths and Residual Ownership

This is a prerequisite for #272, not end-to-end qualification. All full M0-M7
milestones remain open; the qualified count remains zero. No protected proof,
GPU artifact, or GPU execution was produced by this batch.

## Exact Candidate

- Code candidate: `f2aa6ec909cf0f37042150fc205e68b674754947`.
- Base: `aac9bc09a147fd2b25e1ef1b44ef3bb81d289143`.
- Tree: `60166e96afb4b092f869f93203988560487c5fdb`.
- Frozen input: 5,556 files; roster SHA-256
  `f1deb62bfca275bec0bbf3ac56c793981c8eaf2a1f7b371ede8653eb6bb954d5`.

The frozen source matches the candidate's Git blobs and executable modes.
This record is a subsequent documentation-only change.

## Implemented Invariants

`check_ranked_coverage_v1` rederives the output extent against its retained
canonical/source owner, then checks the constructor-validated ranked recipe.
Both truth values of the exact global-X/length predicate must terminate normally:
the valid-index case executes exactly the selected write, the tail executes none.
Only literal comparisons or the exact selected predicate choose edges. Feasible
traps/cycles, unknown conditions, additional effects and unsupported computations
refuse. Unsupported operations also refuse in unreachable blocks.

The checker reuses constructor definition/scope/edge invariants. Two bounded
deterministic CFG walks need no heap scratch or copied graph; literal scans charge
the caller's inherited ledger. The result retains its owner, candidate and address
domain. It does not establish stored-value equivalence, source translation,
ownership, actual launch/length values or the runtime requirement `N <= G`.

The private PLIRON conditional ownership analysis retains the original V1 report
and a complete contract roster. It preserves mandatory bounds failures even when
the legacy dynamic-trace exit masks them, checks available effect-domain sites,
and runs finite unselected ownership checks when their prerequisites exist.
Each obligation is checked, rejected, inapplicable or explicitly blocked. Selected
coverage remains blocked on canonical coverage and source replay.

Shared preparation preserves V1 diagnostic order and early exits. New work, row
storage and diagnostic/name storage use the inherited metered analysis manager.
Context/operation identity is not mutation-epoch or transaction custody. No
clean-report conversion, unconditional coverage credit, old wire change or new
authority gate was introduced.

## Validation

The frozen candidate passed 3,584 library tests:

| Package | Passed | Ignored |
| --- | ---: | ---: |
| `fe2o3-host` | 90 | 0 |
| `fe2o3-lower-mir-kernel` | 968 | 0 |
| `fe2o3-pliron` | 1,319 | 0 |
| `fe2o3-verifier` | 146 | 5 |
| `rustc-codegen-fe2o3` | 1,061 | 31 |

Two explicitly invoked actual-source parent tests passed across three
configurations in 290.92 seconds: genuine reference fill on gfx942 and gfx950,
and the missing-annotation refusal. The observer exercises the full public query
with inherited/exact/one-short work limits and retained owner storage, then stops
before proof execution. All 91 doctests passed: 66 lowerer and 25 PLIRON.
Warning-mode all-target Clippy passed for the five listed packages; existing
warnings remain. This is not strict Clippy or whole-repository CI.
Formatting, both dependency policies, source-manifest validation, hygiene,
both repository DCO checks and whitespace checks passed. The manifest reports
`qualified=false`; content-address validation is not curriculum completion.

Candidate B's focused run passed 82 tests, including all 23 new ranked-CFG and
15 new ownership tests. Earlier candidate A failed two new test fixtures: removing
an operation with `unlink` retained a stale operand use, and the resource test
expected failure after rather than during metered initialization. Correcting the
fixtures preserved production checks and their failure conditions. The failed
run remains recorded. Read-only cross-review found no further production defect.

Follow-up `b90423e48dc4b20536eadbf16bfab4b47933af08` changes only the ranked-CFG
test file. It adds a work-limit interruption during the second literal lookup,
after the structural scan, with inherited work and unchanged retained/peak
storage. Its frozen 5,556-file roster is
`52d166965ce3e9ed69a7af1c05ebf3c4651616913178781b207683a9874010ca`.
All 24 ranked-CFG tests passed on this follow-up, including the new test's exact
limit 139 / attempted work 141 and sufficient-budget success. Production source
is unchanged. The broad and source results above belong to candidate C, not an
independently rerun broad suite on the follow-up; the overlapping tests are not
added again to the broad count.

## Next Integration

The actual-source observer invokes the full output/extent/path query using the
same owner and caller ledger, but still deliberately returns test-only
`Incomplete` before proof execution. Source/signature/effect preimage retention
and independent rederivation with #275 remain required; an opaque digest or
normalized argument ordinal is not that preimage.

The proposed typed boundary keeps a distinct pending PLIRON input and lets the
lowerer compose it with its genuine canonical/source owner. No callback returning
success can establish that relation. V6/V2 layout, aggregate replay, exact final
optimized/native output preservation and protected host discharge remain open.
The `GlobalLaunch` address-formation premise still applies at zero output length.

## Evidence and Resources

Raw logs, failed development runs, source manifests and policy results are in
`/home/harsh/work/fe2o3-issue272-source251-evidence-20260919`.
Native workers implemented and cross-reviewed disjoint leaves; the primary owns
integration, builds, publication and final evidence. Source248/249 archives were
not changed. The task-owned RAM cache remains bounded and retained for active
work. No remote scratch directory or GPU job was created.

Read-only host checks recovered the previously provisioned MI350 runtime image
`sha256:aae172a55c7dba4a96c2841320e3c5570037cc6b2febf7e05ded398fc1001742`.
The native host paths are absent, but that is not a missing-runtime blocker.
The earlier [Source245 protected proofs](issue272-source245-20260918.md) remain
historical evidence on their own candidate; they were not rerun or credited to
this batch. No shared host libraries or runtime pins were changed.
