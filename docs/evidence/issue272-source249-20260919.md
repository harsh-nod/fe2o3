# Source249: Checked Output Extent and Descriptor Correspondence

This is a prerequisite for #272, not end-to-end kernel qualification.
All full M0-M7 milestones remain open and the qualified count remains zero.
No protected proof or GPU execution occurred in this batch.

## Exact Candidate

- Compiler candidate: `4a3b76d2831c5a8391283e584376e21a791384e6`.
- Published base: `a58e4bc7da39c22de24881229315418e1217039b`.
- Frozen input: 5,550 files, roster SHA-256
  `249054d083adbef9937895d410e34cb0b417bc54cba8709fa7801f9202aa0615`.
- The frozen files and executable modes match the candidate's Git blobs.
  This page is a subsequent documentation-only change.

## Implemented Boundary

The checked-view producer now retains an inline, fixed-size extent proposal in
the existing source-access row. It records the original source argument and exact
ranked view, index and extent operands. It does not create a second graph, map,
wire record or authority token. Existing retained-capacity accounting includes
the larger row, and legacy reconstruction leaves the new proposal absent.

The borrowed output query can rederive that proposal against the exact canonical
owner's `SliceLength` facts. It checks source, view and index agreement, a unique
dynamic global-X index, supported extent uses, conflicting interpretations and a
matching index-less-than-extent success edge. Missing provenance refuses even
for `Argument(0)`; the operand number itself never means "output length".

That edge is not a dominance or complete ranked-CFG proof. The candidate remains
inert. This result does not authenticate translation, validate whole-candidate
SSA, establish reference ABI/value agreement, or discharge a runtime condition.
It retains `GlobalLaunch` versus `GuardedOutput`; whole-launch address arithmetic
is still a premise for GlobalLaunch even when the output length is zero.

A compiler-private descriptor query joins the sealed source-output binding to
the complete ordered typed-root roster using existing semantic ownership and
argument checks. It selects the generated field by the certified source position
and the physical type by its separately certified KIR slot, not by equal shapes.
Ignored, hidden, tuple-expanded and context inputs remain unsupported. Caller-
supplied matching roots are not authenticated as the transaction's own roots.

All new scans use the incoming work ledger and retained owner floor. The actual
source observer receives the live canonical owner and typed roots from the same
stage. The prepared-request observer still stops with an explicit test-only
`Incomplete` before proof execution and cannot produce an admitted artifact.

## Validation

The final candidate's library run passed **2,242 tests**:

| Package | Passed | Ignored |
| --- | ---: | ---: |
| `fe2o3-host` | 90 | 0 |
| `fe2o3-lower-mir-kernel` | 945 | 0 |
| `fe2o3-verifier` | 146 | 5 |
| `rustc-codegen-fe2o3` | 1,061 | 31 |

The final candidate also passed six explicitly invoked actual-source parent
tests across ten source/profile configurations: two prepared-request tests
(306.24 seconds) and four coverage-source tests (708.21 seconds). The latter
include same-typed descriptor arguments on both targets, fill and retained
Result-wrapper paths on both targets, and a retained-helper refusal on gfx942.
Twenty finite fill/wrapper scenarios run twice, including the N = G + 1 missing-
output counterexample. The prepared-request observer checks exact/one-short
work limits and the retained owner floor while still refusing before proof.

All 64 lowerer doctests and warning-mode Clippy for all targets of the four
listed packages passed. Formatting, dependency policies, source-manifest,
hygiene, DCO and whitespace checks also passed. This is not strict Clippy or
whole-repository CI. Earlier candidate B's passing coverage run is retained
separately, not relabeled as final-candidate evidence.

## Development Corrections

- Candidate A's new fixture tried to store an index as a semantic value. The
  existing ranked constructor correctly refused it before the extent query.
  The test now distinguishes constructor refusal from supported query negatives.
- Candidate B's nine new descriptor tests shared a malformed source helper:
  the second root reused the subtract helper's function identity. Admission
  correctly refused nondeterministic ordering. Distinct ordered fixture
  identities fix the tests without relaxing production validation.
- Cross-review identified missing budget tests on the full public extent query.
  Actual-source tests now measure inherited work, exact/one-short work, lost
  owner reservation, unchanged ledger identity and restored storage.

## Remaining Integration

No unconditional TotalView counter, clean-report gate or production admission
path changed. The next step is a bounded ranked CFG/path relation and a separate
conditional-aware ownership result. The current dynamic-trace exit skips
per-contract checks and later pipeline stages; deleting its diagnostic would
not establish them. Effect refinement also has a nested ownership dependency.

Root #271 allocated draft ownership of middle-end V6, semantic contract V2 and a
shared closed conditional obligation in comment 5737755031. Existing V5/V1 bytes
and meanings remain unchanged. Exact fields and APIs still require review, as do
reference-signature preimage retention and independent source replay with #275.
An opaque reference digest or normalized argument number is not that preimage.

Conditional coverage must survive verification of the exact optimized graph,
the native output chain and existing #209/#213 authority. Normal host preparation
must discharge the same output length, logical launch, dimensions, address-domain
and memory premises before effects, retaining artifact, target, context and
asynchronous borrow/completion checks. This batch provides neither value proof
nor that end-to-end route. Advanced kernels and full tutorial census remain open.

## Evidence and Resources

Logs, failed development runs, source manifests and reviews are retained under
`/home/harsh/work/fe2o3-issue272-source249-evidence-20260919`.
Source248's archive was not changed. Native workers implemented and cross-reviewed
the two leaves; the primary owned integration, source tests, fixes and publication.
Combined erased/expanded-prefix producer coverage is still a test gap despite
separate shifted-ordinal binding tests.

Builds use the bounded task-owned RAM cache and shared Cargo lock. No remote
scratch, runtime installation or GPU job was created. Two inactive task-owned
Cargo caches from September 5 and 12 were removed after ownership and live-use
checks, reclaiming 2,842,471,919 logical bytes; source and reports were preserved.
Separate Source250 tutorial binding and Source251 analysis work are not evidence
of completion of this candidate or any full milestone.
