# Issue 272: Source242 Graph Custody And Discharge

This is a partial M1/M7 implementation packet. All full M0-M7 milestones remain
open. No kernel moves to end-to-end qualification: `qualifiedPairCount=0`,
`requiredPairCount=null`, `inventoryComplete=false`, `qualified=false`.

## Exact Integration

The fresh production branch starts at published main
`f7137ab8caa1fc0c9223f5fb21203b843209707c`. It carries only the narrow source241
diagnostic packet (`9cc2d1f9e`, `996a9bd9f`), then normally merges the published
complete source-custody dependency
`e5d92f8fbfec5676f610842ae1f56942a87d269d`. The historical integration branch was
not imported wholesale.

The two new implementation commits are:

- `5c6c4bcb5c2986250b71fb2ff744407eef6acc96`: exact V15 graph ownership and checked
  execution erasure, with tests and the architecture note.
- `9ee36ffd15131e2ee6a60857c9ead0cfc93dbf47`: optional tutorial census comparison
  with bounded normalization and response-file refusal.

The latter is the exact tested code commit. Publication uses
`codex/issue272-production-20260918` on both repositories; this packet does not
advance main. Subsequent compositions require their own validation.

## Implemented Invariants

`VerifiedCanonicalKernelIrModuleV15` owns exact canonical V15 bytes and their
freshly decoded, semantically and lifecycle-verified inverse. Admission,
candidate copying, inverse equality and domain-separated hashing share one
resource ledger. The owner cannot be cloned, mutated or split. Transformation
candidates remain explicitly unverified. V12 byte counting and charging remain
unchanged; no grammar version or operation tag was allocated.

`ProductionExecutionDischargeV29` removes only `ContextIssue`,
`WorkgroupDerive` and `ScopeEnd` with an empty discard roster. It freshly admits
the physical V12 output and independently replays all retained graph fields and
the exact input-ordinal erasure roster. Ordinary calls, memory accesses,
operands/results, branches, parameters, metadata, ABI and ordering must remain
identical. Tiles, fragments and nonempty descendant disposal reject.

Exact and one-short work/storage tests retain incoming floors and denial
histories. Valid-but-altered output graphs and malformed erasure rows reject;
these tests do not rely only on the ordinary IR verifier rejecting bad IR.
An ordinary callback/store example runs in the existing simulator against an
independent Rust arithmetic oracle with an unchanged canary.

The complete contract and integration limits are in
[Checked Execution-Capability Erasure](../production-execution-discharge-v29.md).

The optional tutorial consumer accepts paired independent request/report files
only with `--emit-kernel-pairs`, after existing manifest validation. It compares
already-bound physical source members and complete original-byte identifier
tokens. Generated unavailable identifiers remain unresolved; names cannot stand
in for spans. Cargo intent stays caller-supplied, not compiler-observed. Default
feature intent is never inferred from missing cfgs. No qualification, inventory,
pending binding or runtime-census field changes.

Review found and fixed response-file tokens hidden in consumed `--cfg` values,
and repeated large-prefix scans during span normalization. The replacement uses
sparse, deduplicated endpoints and disjoint source slices. Tests check total
bytes traversed, not timing. See the
[consumer contract](../tutorial-kernel-pairs-v1.md#optional-diagnostic-comparison).

## Final Validation

All seven final stages returned terminal status zero on the same frozen
5,508-file snapshot. Every file hash was checked against the tested commit's
blobs; exact snapshot checks ran before and after each stage.

| Stage | Result |
| --- | --- |
| Kernel IR and lowerer packages, including integration tests and doctests | 2,094 passed |
| Backend library | 1,035 passed; 20 ignored by default |
| Analysis and optimizer libraries | 210 + 63 passed; 1 ignored by default |
| Actual rustc census parent | 1 passed, including its nine wrapper cases |
| Actual workgroup callback parent | 1 passed; 40 observations, 32 distinct configurations, 8 exact repeats |
| Manifest, scanner and identity Python suites | 80 + 12 + 24 passed |
| Mocked compile-matrix shell tests | Passed |
| All-target warning-mode Clippy for kernel IR, lowerer and backend | Passed |

Total: **3,404 Rust tests and 116 Python tests**. The 40 callback subprocess
observations are not added again to the Rust total. Default suites report 22
ignored tests in total, including one Kernel IR test; selected opt-in source
parents were executed separately. The earlier candidate run and workers' focus
runs are not added to final totals.

The new packet contributes 22 Rust unit tests, seven compile-fail doctests and
16 Python tests. Existing Clippy warnings remain; no new canonical-owner,
discharge or census-module diagnostics were reported. This is not a
zero-warning claim.

Formatting, workspace dependency policy, Pliron pin policy, manifest validation,
source-shape hygiene, DCO and diff-whitespace checks also passed on the tested
code. No Cargo dependency or lockfile changes were made.

## Evidence And Cleanup

Persistent evidence directory:
`/home/harsh/work/fe2o3-issue272-source242-evidence-20260918`.

| Record | SHA-256 |
| --- | --- |
| Tested source roster | `4400268e6411071f10cb0f874d36bcdda96b6c5a824f6e97480d94374b2ba7e6` |
| Final test summary | `8aba8a29f83c3061ef0e3041656cf8683c9ab03c0889591dc847367be758fa42` |
| Code-policy summary | `52581c5204052d3a0bcfddd936dc287ebf8f29095c691feaf43bedc3160d14ca` |
| 34-file evidence archive manifest | `7f1d6d0a7bbb7891ad416773dbec291b2864969e67e8768ef0ef9c0ba52cc512` |

Builds used the shared Cargo lock, two jobs, one test thread, the pinned nightly,
offline locked Cargo, a bounded private RAM-backed directory, resource reserve
checks and per-stage timeouts. All required processes were terminal before
cleanup. Archive hashes, ownership and live-process guards were checked before
removing `/dev/shm/fe2o3-source242-20260918.EmXdI6jk`, freeing 2,903,237,559 logical
bytes. Source worktrees and reports were preserved. Native workers were closed.
No SSH host, GPU or protected proof runtime was used in this packet.

## Remaining Production Work

The backend's execution-capability refusal remains closed. #275 owns complete
source/instance custody, producer emission, callback expansion and scope-end
materialization. The opaque discharge relation must be consumed at that actual
checked-owner boundary, not replaced by a cloned output labelled source-proven.
#271 retains ownership of the fixed final-graph pipeline and its policies.

This packet does not establish source refinement, global memory or barrier
safety, numerical error bounds, machine refinement, protected evidence,
artifact publication, generated safe GPU launch or the 47-kernel release gate.
It does not enable a fallback path or change any full milestone's status.
