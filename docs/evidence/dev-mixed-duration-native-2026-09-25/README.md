# Mixed-Duration Native Attempt

**Stopped during upload. No native test launched; zero native passes.**

Signed campaign helpers `15355be3943f4f12ae4bae118f2de167874dc03f`
stage the CPU-qualified static-musl ELF from signed runtime source
`ca1edd1423b6f094c75bc363b6d6f02004f1fa0e`. The executable SHA-256 is
`4e1b0379696d97443f0b75baae900b0a5b204afeba27c87712ed2927b08e400e`.
Four synthetic protocol calibration groups passed before staging.

The new adapter pins the existing process recorder, topology and strict endpoint
observers. It fixes GPU 1 / `0000:26:00.0` / `0xab83d2ffef0d3cdf`, two exact tests,
bounded disposable processes, independent 384-byte oracles and collection before
cleanup. Fresh topology, device ownership and pre/postflight checks are required
by the runner; none ran because upload did not finish. Earlier exploratory idle
observations are not native qualification or an exclusive reservation.

## Recorded Outcome

- Private staging creation succeeded.
- Upload ended with signal 15; its local process group was reaped.
- There is no `native` command or remote native-results collection.
- The later inventory command returned an empty results inventory.
- The cleanup command returned success and reported removal of exactly
  `/home/harsh/fe2o3-mixed-duration-20260925.153f809f9d250cd9`.
- The controller's original `collection.json` remains unchanged, including its
  broken-pipe errors and `owned_cleanup=false`. Command receipt success is more
  specific than that aggregate failure, but a separate post-cleanup absence
  observation was not obtained. SSH subsequently failed DNS resolution under
  the changed execution context.

```sh
python3 -I -B docs/evidence/dev-mixed-duration-native-2026-09-25/verify.py
```

Replay authenticates the signed helper source, joins the staged executable to
the signed CPU qualification, verifies original receipt/log hashes, and reports
only the interrupted-upload state. `raw/payload/runtime-tests.gz` losslessly
retains the original staged ELF; manifests retain its uncompressed identity.
No failed/refused native cell was retried. The next hardware attempt needs a new
packet and fresh device ownership checks, not mutation of this stopped packet.

Before that attempt, extend owner-profile parser calibration with a complete
positive and targeted lifecycle mutations, and restore the successful-campaign
replay's full command/topology/resource/endpoint joins. The current verifier is
deliberately for this stopped packet only. Physical overlap, high native depth,
formal machine-code refinement and HIP/HSA performance remain unverified.
