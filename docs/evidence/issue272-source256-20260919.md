# Source256: Retain Proof Custody Through Pending Source Analysis

This change connects actual protected proof admission to pending source
analysis. It does not complete a production kernel. All whole #272 M0-M7
milestones remain open; original-goal qualification remains 0/47, and all 50
current manifest fixtures remain unqualified. No GPU device or launch is used.

Scope follows [approval 5739906056](https://github.com/harsh-nod/fe2o3/issues/271#issuecomment-5739906056)
and [lease 5740037364](https://github.com/harsh-nod/fe2o3/issues/271#issuecomment-5740037364).
Publication is an independently tested feature-branch contribution, not main,
default activation or qualification of the root integrator's composed tree.

## Exact Candidate

- Base: `9494e347e4ef823e90387ef26c4427ef69365c2e` (Source255).
- Implementation G: `5b365b5e2f7da81ec480e7a3542f762a172b7f7a`.
- Final code H: `20f229333382a7e919ee41c1c9a1a47a0888ac6f`.
- H tree: `b9c3e25111903a33efa0e109a269ec556a6642ed`.
- H source roster: 5,588 files, SHA-256
  `21615865028fcc8ba2e5decf7666e71118a0e35c6c8bd99c34b3325c9a090370`.
- Pliron remains `7ebf6e6638c2a3bcec179423993b01211a9689b4`.
- Protected source/nightly/cache input inventory: 64,015 files, SHA-256
  `731f4a61972447474f93515741704b7257530b7264f7b8d6359a01e415ce97ec`.
- Freshly built backend test executable SHA-256:
  `af8dff410dfc5c1972f947b5c8ba9bef6f1a1fc80410b63e7655bd0e909478d6`.

H changes one negative test to explicitly drop its intentionally removed proof
receipt. Its production code is identical to G. H receives fresh qualification;
G results are not relabeled. This report is a later documentation-only change.

## Implemented Invariants

Normal compilation retains its original order: open the protected runtime,
execute and import proofs in sorted output order, check their common toolchain
and signer policy, bind exact requests, construct, admit receipts once, and run
the complete clean pipeline. Private move-only bound and staged states retain
the actual runtime lease, descriptors and mutation journal through their
continuation. Existing Proof versus Pipeline errors and precedence remain.

PLIRON exposes its unchanged admission body through the consuming, internal
proof-staging helper `stage_ranked_kernel_with_policy_checked_refinement_v2`.
The compile wrapper uses that helper and still runs the complete pipeline.
The shared `new_ranked_v1` session constructor retains kernel/GPU/proof dialect
registration order and error mapping. Neither an empty registered session nor
caller-policy receipt staging grants proof, lowering or launch authority.

The separately selected test observer identifies exact ownership-operation
occurrences in the actual bound recipe, consumes the staged construction into
the real session, and freezes its genuine pending owner. Its callback borrows
that owner and the original signed receipts; it cannot recover construction or
resume compilation. The runtime lease remains alive through that callback.

The original source owner, root, launch rank and correspondence rows feed
Source255's shared query against `pending.kernel()`. No source or recipe is
reconstructed, no proof or runtime is fabricated, and no replacement ledger,
fallback, retry or additional final runtime revalidation is introduced.

Every observer exit is a distinct `Incomplete` result. Successful source
correspondence preserves `TraceIncomplete`, zero proved unconditional TotalView
contracts, blocked selected coverage, bounds diagnostics and all nine pipeline
checks. The source parent rejects successful compiler continuation, missing
observations and unavailable runtime. Unannotated source has a separate early
refusal. The old pre-proof observer remains separate and unchanged.

## Validation

Final H passed 3,693 library tests: backend 1,137, PLIRON 1,339, lowerer 981,
host 90 and verifier 146. There were zero failures and 41 default-ignored tests
across those suites. Verifier tests ran natively on MI350; the others ran in the
isolated image. The exact backend executable was sealed after its successful
cold build and checked before and after every direct-binary run.

All 56 feature-enabled ranked-pipeline integration tests and 103 documentation
tests passed. The actual protected-reference parent passed gfx942 and gfx950
in 72.75 seconds, retaining one signed receipt, one matched memory effect, one
value expression, all nine pending checks and 471 live-ledger work in each
observation. Unannotated and missing-runtime refusal parents passed, as did
both unchanged pre-proof source parents. All six reference-binding parents
passed across 26 compiler invocations in 155.05 seconds.

Warning-mode five-package, all-target Clippy passed with no diagnostic on an
added code line. Its 71 inherited diagnostic locations remain; this is not
strict warning-free Clippy or complete workspace CI. Exact committed source,
formatting, both dependency policies, manifest, hygiene, both-repository DCO
and whitespace checks passed. Final inputs and executable hashes were unchanged;
all required qualification sessions are terminal. The complete H source roster
was verified again afterward.

Manifest validation still reports pending semantic oracles and policy checks
for all 50 fixtures, plus the unresolved `gemm-proof-plan` source binding. A valid
manifest and passing component tests do not qualify a tutorial kernel.

Focused controls cover original signed-sidecar allocation, order and bytes;
two-output receipt staging; eleven admission mutations through helper and
wrapper; admission-before-bounds precedence; source-root and launch mismatch;
missing retained storage; exact and one-short source/selection budgets; and
storage/observer restoration on errors and panics. Five selector refusals use
constructor-valid inert ownership rosters, not counterfeit imported proofs.

The measured budget covers selection payload and existing source-query
accounting, not whole-constructor allocation, diagnostic snapshots, native stack
or RSS. Descriptive source correspondence is not complete indexed-address,
operational or machine equivalence. A successful source observation proves no
unconditional total-output-coverage claim.

## Failure History

A failed on a test type mismatch. B passed 56 staging tests and backend checking.
C stopped at the shared resource reserve; D was refused before compilation.
D's first remote staging copied an unauthenticated build cache and was never
built or used for qualification. Later candidates start with empty target caches.

E built and passed 1,136 backend tests, but its protected parent failed a new
test's incorrect error-wrapper expectation after proof admission and source
correspondence. F corrected that assertion and also checked actual inherited
work exhaustion. Its two protected profiles and unannotated refusal passed.
G added selector negatives and passed the source, integration and documentation
gates, but its added-line lint gate found the intentionally discarded must-use
receipt. H makes that discard explicit without changing production behavior.

G's broad container run also failed four existing verifier tests: two lacked
Python, and two found unreaped descendant process entries. The unchanged
verifier binary passed all 146 tests natively on MI350. The failed container
run is retained, not reclassified as a pass. H uses that native environment for
verifier unit tests, with no protected-proof credit. Protected-source tests
remain isolated. Existing reference-binding tests deliberately require runtime
absence and do not establish real-prover semantic mutation rejection.

## Isolation And Remaining Work

Protected tests use the existing pinned MI350 build-tools image
`sha256:c91b418d0c07d05127e8ab6fccec0ea7a5f3aa5a99e5147f249bc859a2d33311`.
Containers run as nonroot UID 9661, without network, GPU devices or capabilities,
with read-only source/nightly and bounded CPU, memory, PIDs, time and scratch.
Inputs and sealed executables are verified before and after. Cargo dependency
contents are preserved across explicitly checked cache transitions. Container
absence is established after each run. After qualification, the entire private
MI350 scratch tree was removed under its execution lock and container absence
was checked again. Existing runtime images and peer data were preserved.

One rechecked inactive local Cargo deps cache was removed, freeing
2,449,776,640 allocated bytes. A separate inactive private RAM build cache was
removed after worker inspection and primary rechecks, releasing 3,852,722,176
allocated bytes. Source, Git, reports, toolchains, runtimes, the small dependency
cache and peer work were preserved. Some service process metadata was
inaccessible; the audits do not claim unrestricted process visibility.

The [approved next draft](https://github.com/harsh-nod/fe2o3/issues/271#issuecomment-5740384256)
joins exact pending output, canonical conditional coverage and authenticated
CPU reference roles. It still cannot turn `N <= G` or address representability
into an unconditional theorem. Typed conditional discharge, independent CPU
and helper replay, residual safety, aggregate proof, optimized/native-output
custody and protected safe host completion remain required.

Root's newer dependency pin and guarded entry-operation hook must be preserved
during composition. This scope changes no wire/schema, policy, dependency,
host/runtime implementation, website, main branch or production activation.

Raw evidence: `/home/harsh/work/fe2o3-issue272-source256-evidence-20260919`.
Native read-only reviewers covered staging, custody, source observation, resource
limits and integration dependencies. Primary owns edits, builds and Git. No
Qwen worker ran. Prior evidence archives remain unchanged.
