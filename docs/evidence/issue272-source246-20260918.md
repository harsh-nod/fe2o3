# Issue 272 Source246: Bounded Tutorial Configuration Selection

This is partial M0 hardening. It does not qualify another kernel or close a
milestone. The manifest itself is unchanged.

## Change

The restricted literal cfg evaluator now has explicit limits: 8 KiB UTF-8,
512 tokens, nesting depth 32, and a shared 1,048,576-unit work budget. Input
bytes are charged before lexing and tokens before parsing. Every predicate is
parsed, including branches whose truth value is already determined. Unknown
predicates and enabled unsupported item macros still reject.

The larger token bound accepts the existing 318-token attention feature
exclusion expression. It does **not** establish the three proposed ablation
bindings: complete module selection also encounters unsupported `cfg(test)`
and macro items. The initial three-binding candidate failed, was withdrawn,
and is recorded in the worker log. Negative tests now require those binding
claims to remain rejected. Matching only the ablation file is insufficient.

## Validation

Exact code commit: `447539e2e189db3b7ea89318599d9387c406e21f`.
The native worker passed 125 Python tests. The primary independently reran them
and the CPU-plan check, capturing raw logs: **126 passed** (83 manifest,
30 identity, 12 occurrence and one CPU-plan test). Dependency, manifest,
hygiene, DCO and whitespace policies passed. No Rust source or manifest bytes
changed; there was no GPU run.

Coverage includes exact and excessive byte/token/depth limits, shared work
budgets across expressions/modules/selections, malformed and unsupported
predicates, macro refusal, and all 256 feature subsets of the real exclusion
expression against an independent enabled-feature-count oracle.

## Remaining Inventory

The projection remains 50 fixtures, 60 known identities, 121 display occurrences
and 56 pending display bindings. `inventoryComplete=false`,
`requiredPairCount=null`, `qualifiedPairCount=0`, and `qualified=false`.
The original 47-case recovery set is unchanged.

Without a validated actual website census, there are 117 unresolved records:
56 displays, 60 selections and the missing census. Even with a matching census,
42 of the 50 fixture selections still lack exact display coverage. The
historical GEMM source gap remains. Neither protected-proof success nor a
diagnostic source census substitutes for these joins.

Evidence: `/home/harsh/work/fe2o3-issue272-source246-evidence-20260918`.
Its 13-file archive manifest SHA-256 is
`0a34dcf1aff591a7b4e08258594e67ae99397b0e908a37bfc703092d0497bc31`.
The worker log is a terminal-summary record; the primary's individual logs are
raw captures. Repeated runs and failed development candidates are not added to
the passing total. The normal merge with source245 preserves both histories.
