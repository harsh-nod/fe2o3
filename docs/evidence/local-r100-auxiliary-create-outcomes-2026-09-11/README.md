# R100 Auxiliary CREATE Outcomes: Local Evidence

Source parent: signed R99 `e6ac41c7fe61fbbb3e9a7003a8e9fd9a8a0c97a4`.

This record accepts **NATIVE-2B.5B-3A**, the named CPU/local-helper admitted
CREATE outcome matrix. It does not close 3B recovery/currentness, 3C roster/slot
composition, NATIVE-2B, A1/A2, issue #182, formal adapter correspondence, live KFD
qualification or HIP/HSA performance parity.

## Source Scope

Four test-fixture files change, including one new file and two new tests.
Production queue/runtime/model/accounting/completion mechanisms and Cargo inputs
are unchanged. The existing shared auxiliary driver executes after successful
primary construction with its original engine, foundation, memory, accounts,
data, ledgers and platform owners. R99's local gate/registration, mapping,
shadow protection and cleanup helpers remain in use; CREATE is scripted.

The separate admitted-CREATE oracle reuses exact pair-custody assertions without
weakening the existing successful-CREATE oracle. Seven scenarios run through
both original-runtime routes: fourteen auxiliary constructions, all failures.

| Scenario | Model phase | Retained returned ID | Engine authority poisoned |
| --- | --- | --- | --- |
| Definite no-effect, unchanged outputs | Planned | None | No |
| Indeterminate, unchanged outputs | Ambiguous | None | No |
| Indeterminate with returned outputs | Ambiguous | 8 | No |
| Input drift | Ambiguous | 8 | Yes |
| CREATE panic | CreatePending | None | No |
| Successful status with invalid doorbell output | Ambiguous | 8 | Yes |
| Failed-no-effect with changed outputs | Ambiguous | 8 | Yes |

Every row retains the complete terminal parent and poisoned outer ledgers/local
gate. The engine poison bit is not retry permission. No accepted typed CREATE
outputs, queue-live auxiliary registration, completed/installed lane, doorbell
or gate finalization is fabricated. Published shadows and every returned owner
remain rooted; unpublished cleanup does not run over published payloads.

Assertions preserve the exact original primary record and history prefix, then
compare every appended auxiliary history coordinate. Panic adds no fabricated
CREATE observation. Error stages and the original panic payload remain exact.
After authority transfer the root retains an empty resource-prefix container,
not a second authority. Before deliberate fixture disposal, both original and
auxiliary maps/files remain live; afterward only fixture-owned resources reach
zero. This is local cleanup, not a confirmed native queue teardown.

## Gates

Authoritative records are `raw/r100-final-source-gate.json`, its completion
marker, `raw/r100-auxiliary-results.json`, its completion marker, frozen/restored
records and all four `r100-mut-*` records. [test-summary.json](test-summary.json)
contains parsed totals.

| Check | Result |
| --- | --- |
| GNU / musl runtime all-target suites | Each 2,542 passed, 5 ignored, 48 harnesses |
| GNU runtime/host documentation | 108 passed |
| musl runtime / host documentation | 91 / 16 passed |
| GNU host / musl host | 258 passed, 4 ignored / 141 passed |
| Generated macro fixtures | 7 passed |
| New CREATE matrix | 2 test functions, 14 failure cases |
| Frozen / restored construction | 56 / 56 passed, identical rosters |
| Registration / ordinary / Linux helper / initialization | 2 / 1 / 20 / 4 passed |
| Transition / preparation / bind regressions | 20 / 23 / 4 passed |
| Final source gates / auxiliary checks | 17 / 10 passed |
| Compiled behavioral mutations | All 4 fail their intended dynamic assertion |
| Non-documentation source identities | 5,656 unchanged and exactly restored |

Filtered tests overlap the full suites; counts are not additional unique tests.
The collector requires all 54 existing R99 construction tests plus both new
tests in frozen/restored runs and both full targets. It compares gate commands
and unaffected totals to committed R99, allowing only the two new full-target
tests. Gates include both Clippy profiles with warnings denied, Python harness
tests, format/whitespace, dependency policy/tests, CI test-gate and standalone
lockfiles. Auxiliary checks include proof inventory and production dependency
closure. Inventory checking is not a solver run.

`raw/r100-environment.json` records WSL2, pinned nightly-2026-04-03 and cargo/rustc
binary hashes. Runs use locked/offline dependencies, four build jobs, disabled
incremental compilation and no `XDG_RUNTIME_DIR`. Wall times are test campaign
metadata, not CPU/GPU runtime performance measurements.

## Mutations And Preliminary Failure

| Mutation | Decisive behavioral failure |
| --- | --- |
| `create-result-propagation` | Ignoring the shared driver's CREATE error reaches a second runtime-created callback. |
| `returned-uncertain-id` | Ambiguous CREATE loses returned ID 8. |
| `invalid-output-phase` | Invalid successful output becomes Active instead of Ambiguous. |
| `failed-noeffect-outputs` | Changed failed-no-effect outputs avoid required engine authority poisoning. |

Each mutant compiles, runs its exact named test and exits 101 with zero passed/
one failed. The collector reconstructs the tested bytes, validates the sole
changed path and dynamic failure markers, then requires exact source restoration.
These are runtime behavioral negatives, not Verus negative proofs.

The first focused attempt compiled but both tests failed because the new oracle
incorrectly required `resource_prefix` to be absent. The existing transfer keeps
an empty container. Only that new assertion was corrected to require an empty
token-identity set; production did not change. The failed log, source inventory
and explanatory note are retained and reconstructed by the collector. Both
focused tests and the broader frozen/restored suites then passed.

## Swarm Handoff

The [current board](../../runtime-a1-a2-swarm-current.md#remaining-work-at-a-glance)
assigns Native 3B then 3C and replacement/adoption, Admission CO-2A then
descriptor/private-reply coverage and completion integration, and Resources
issuance/membership/settlement proofs before a real Context journal. It records
their reviewed dependencies, source boundaries, resource costs and acceptance
limits. Model-only proofs are not production consumption; metadata slot reuse
is not native incarnation qualification; descriptive identity is not a receipt.

Read-only workers reviewed source, evidence boundaries and the remaining
packets. Primary owns edits, serialized tests/mutations, proof pins, hardware
scheduling and signed publication. Queued packets are not unattended jobs.
No SSH session, MI300X process or remote staging was created, so no shared-GPU
cleanup was required. No new solver, live KFD or performance result is claimed.
