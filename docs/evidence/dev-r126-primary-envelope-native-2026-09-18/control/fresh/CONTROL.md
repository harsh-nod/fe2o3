# Fresh R126 Campaign Review

The original control directory remains unchanged. This fresh control copy changes
only `controller.py`'s `OWNED` constant from the refused `.oVqzCv3c` directory to
root's new marked directory:

`/tmp/fe2o3-r126-primary-8b0021ba-20260918.z3L69nX6`

The remote helper, controller tests, frozen bundle, source cohort, binary and
native protocol are unchanged. The frozen bundle's directory mode is now 0700;
all 20 file hashes and manifest identity remain unchanged. New local controller
qualification and native output directories must be used.

## Previous Attempt

The previous controller executed its local checks, remote-empty check and upload,
then refused remote approval because scp propagated the local bundle directory's
0755 mode. It never issued GPU admission or a native launch. Root approved a
separate prelaunch-only inventory, full collection and exact-owned cleanup.

The collected 22-file inventory hash was
`b987f3468f4b22f4d9f74afea62bb2abd60fcd98637aec7b07e9ca5c000874d0`.
Cleanup and a separate absence check both passed; no native PID or launch receipt
was fabricated. Original receipts remain in these separate local directories:

- `fe2o3-r126-primary-envelope-native-outer-20260918`
- `fe2o3-r126-primary-envelope-native-run-20260918`
- `fe2o3-r126-primary-envelope-prelaunch-collection-20260918`
- `fe2o3-r126-primary-envelope-prelaunch-cleanup-20260918`

All reside beneath `/home/harsh/.codex-tmp`. The earlier rejected argument-wiring
draft history remains in the original control directory's `CONTROL.md`.

## Prospective Command

After root verifies the one-line diff and corrected controller hash and explicitly
authorizes the fresh campaign:

```sh
python3 -B controller.py --root-ready-approved \
  --remote-directory /tmp/fe2o3-r126-primary-8b0021ba-20260918.z3L69nX6 \
  --output /home/harsh/.codex-tmp/fe2o3-r126-primary-envelope-native-fresh-run-20260918
```

Exactly one fresh campaign; no retry on the former directory. The three tests,
resource bounds, strict fresh admission, immediate/fixed-delay observations,
full collection-before-cleanup checks and separate absence rules are unchanged.
