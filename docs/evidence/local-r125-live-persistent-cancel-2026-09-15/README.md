# R125 Live Persistent Cancellation Evidence

Locally accepted CPU/test evidence after two independent read-only archive reviews.
The reviews bind prepared summary SHA-256 `a0e9873ebe1c637d0fc44a4e8771f2015542598e8d24303332e76ec5446c5bd1` and prepared README SHA-256 `645e98278dcad417e90e2a8f783a75806e9660990af7ef51dbb0774d0a8a1b97`. Finalization only changes acceptance metadata and this README; all 576 raw artifacts remain unchanged.
The archive retains 216,503,126 raw bytes, 92 qualifying prerequisites plus the closed collector, and 33 separate historical runs. All 126 owned groups were observed absent, and all 122 predecessor links were verified.
GNU and musl each pass 2,901 tests with five ignored across 48 libtest harnesses and one separately checked CSV target.
All seventeen source gates, ten auxiliary checks, restored 47-test KFD and 744-test runtime suites, and 207 fresh checker-calibration cases pass.
Eighteen compiled production mutations reach their declared behavioral assertions with normal Cargo 101 exits, then restore all 5,709 source identities.
The mutation inventory does not claim mutation coverage of the added boxed-ledger layout, owner-move tests or zero-check optimization.
The immutable source bundle retains the exact eighteen changed/new source files, including three added files; unchanged sources belong to the accepted R124 parent.
The GNU and full-musl interstitial checker records are explicitly validated and replayed, not accepted merely through predecessor hashes.
The collector replays exactly. Its four closed outputs are retained alongside every report-listed, hash-checked raw artifact.
Earlier interrupted GNU, stack-overflow and musl-timeout attempts, intermediate source cohorts, and development checks remain separate nonqualifying history.
The six-record paired CPU fixture diagnostic is independently checked and replayed; its test timings do not qualify GPU execution or matched HIP/HSA performance.
No recursive archive import is used. Explicit parser dependencies, including the retained Linux-helper calibration log, are archived; R124 baselines are bound to committed paths and hashes.
All qualifying records use one admitted boot and monotonic timing. Current-boot groups are observed absent; endpoint checks do not authenticate continuous process or source history.
The boxed-ledger measurements concern inline type sizes, not maximum call-stack or aggregate retained-memory bounds.
CPU fixtures and checker calibrations do not establish live Linux/KFD execution, authenticated formal refinement, total memory bounds, multi-device acceptance or HIP/HSA performance parity.
Cold three-binding output admission, A1/A2 and issue #182 remain incomplete.
