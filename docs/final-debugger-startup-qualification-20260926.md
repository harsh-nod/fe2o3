# Final debugger build: startup-only qualification — 2026-09-26

The final private R10 debugger completed one fresh, supervised MI2 startup on
mi350. Independent review of its actual loaded files, Python modules, transport
and owned-family cleanup passed. No inferior was selected, attached or run;
no queue, GPU dispatch, register sample or memory capture occurred.

This advances the prerequisites for #281's physical producer. It does not close
V4 or turn the disabled public controller/producer packages into an enabled
runtime. Accepted broad exits remain M1/V1/V2/U1/U2/U3 (6/18).

## What ran

The root issued a fresh finite R20 coordination lease, ending
2026-09-26T05:30:00Z, for the unchanged final build and a separately named R2
startup generation. Earlier generations and failed outputs remain retained.
This is task coordination, not operating-system-wide exclusion.

Before native execution, the unchanged static derivation and three actual
helper builds passed. Bound pure controls passed: 55 supervisor Rust,
13 process-reader Rust, 83 Node and 16 Python tests (167 total). The first
control run caught a source-pin parser's single-quote expectation for one
launcher hash literal; its corrected successor changed quoting only, not the
hash value, parser, runtime behavior or limits.

The deployed source/helper roster was independently reviewed. All 23 runtime
pins matched. Five fresh supervision controls then passed: normal completion,
owned timeout, double-fork adoption, a descendant holding stdout, and failed
execution. The two descendant controls reaped two and one adopted children.
Every case established its own cleanup, ECHILD and empty owned subgroup without
cleanup expiry or kill failure.

The final debugger startup consumed exactly two MI commands: source the fixed
collector and exit GDB. Fourteen complete MI records joined the raw output to
the collector frame. The debugger exited zero; its supervisor observed pidfd
exit, actual reaping/ECHILD, both stream EOFs and empty owned cgroup. No kill
attempt or cleanup expiry was needed. The manager was subsequently not-found,
inactive and dead; that is additional evidence, not a replacement for reaping.

## Actual loaded closure

Independent review checked the exact 25 successful output leaves: 1,182,958
payload bytes, within the unchanged 94 MiB consumer ceiling. It rejoined the
three outer requests/receipts, their input and stream hashes, the nonce,
request/release and manager identities, and all five benign results.

The actual collector observed 50 mapped ELF paths and 104 initial Python
modules, increasing to 112 after collector imports. The 193 observed paths
comprised 170 existing files and 23 absent build-local Python cache paths.
All existing files (398,511,109 bytes) were independently rehashed against the
retained observation. Artifact, candidate and generated/source Python records
also matched their separate pre/post checks.

System hooks including sitecustomize, apport_python_hook, _distutils_hack and
zope were present. This result does not prove complete import history, executed
cache provenance, system-hook exclusion or strong isolation. The only debugger
stderr was the exact reviewed warning that its index-cache directory could
not be determined.

The sampled process censuses before and after each checked six stable rows
with zero references to the task build tree. They are current refusal fences,
not global writer exclusion, historical process-family cleanup or lifetime
hardware isolation.

## Exact retained evidence

Private evidence lives under
`/home/harmenon/fe2o3-authoring-280-282-mi350.4VZ42zNr`.
These are historical content identities, not portable launch instructions.

| Record | Bytes | SHA-256 |
| --- | ---: | --- |
| Final R10 debugger ELF | 199494168 | `3980c1456940ca28c9024387ac8ca5547b25a7494d5f051ddcdfebdf6c842948` |
| Startup root receipt | 105448 | `9b83b1317fd668c90928446ec7a6892137b3c8db4dfa45d4ead35070f1bbaa68` |
| Startup request | 16128 | `3539239197579f51a7fd8b740ade84e07f481cdcab41d4d4a671c80d27741ac1` |
| Startup marker | 732 | `76381b1876f9b5b7fb1e5ca6795767b074d3ace9c38cae7e3d3789da5a02655f` |
| Runtime build pins | 5677 | `65f68dd555efdef71c170eba57c7ad694d2d0fdbf2e7dc574098082948b69cad` |
| Independent actual-closure review | 6631 | `a00831daa313b40bd724e29b5f8eaa20e64e12565c0f847e4af923c514857c43` |
| Independent audit | 48284 | `b8c3cb82daca00c8fbd8e7071d8509f84479794c1ece8fcc74becb564202cc9b` |

Startup nonce: `31f56cbaec52901a8251cefa51bce602`.
The command took 3,446 ms; the root gate completed at 04:08:30.058Z.
Review is retained separately in
`phase28-drafts/final-r10-startup-actual-review-r20-r1`.
The immutable producer marker still says `closure_review_required=true`;
the separate review must be explicitly joined by a future consumer.

## Next dependencies, not discharged here

A subsequent inert family/prerequisite successor passed 109 Node and 30 Rust
controls. Its first run retained a test expectation failure: a one-charge
over-limit read correctly produced an integer-bound refusal before the test's
expected aggregate refusal. The fresh successor corrected that expectation
and added a cumulative one-short aggregate test; runtime checks and ceilings
were unchanged. This does not qualify a bound runtime controller or family run.

An additional actual-startup prerequisite successor passed 128 Node tests.
It checks the final build's exact 25-file roster, the exact 170-byte index-cache
warning (and refuses all other nonempty stderr), the actual historical
deployment inputs, the original request/receipt equality, manager cleanup and
census caller identity. Its fixture is evidence for consumer checks, not a new
debugger run. Receipt: 90,799 bytes /
`be37a3c98804ee2181d502e11b539c7c7e40e2a69c2ba7d1b5f257cb1746eb22`.
All five runtime bindings remain null pending independent review and root binding.

A separate read-only running-driver audit joined the loaded module's build-ID
to the installed ELF, matched installed/DKMS module bytes and decoded relevant
CPU control paths plus the exact embedded gfx950 first-level trap image.
This narrows the source-versus-running-driver gap under a trusted-host model;
it is not live hardware setup or privileged-tamper resistance. Sampling
exclusion is per target process: actual debug attachment must succeed and
remain held through the later run. It does not require proving host-global
absence of unrelated sampling. Review: 13,163 bytes /
`8edb0de8223373bceff8834520b738ecb2046710f2c746875ec76b272ed8ac24`.

The runtime consumer must still be bound and tested against the exact startup
and review. Fresh work remains for actual VM/CWSR and TTMP setup completion,
per-target sampling exclusion, lifetime isolation, same-client attachment
before runtime, LoadedSuccess and sole successful event ACK, one-stop physical
capture and later target completion. Historical hashes and passing CPU tests
cannot substitute for those premises. Direct TTMP hardware readback is not
claimed by the reviewed setup path.

All debugger/runtime acceptance, runtime selection, physical capture and GPU
dispatch claims remain false at this checkpoint. The result is startup-only.
