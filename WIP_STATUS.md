# Issue 271 Integration Snapshot

Reviewable WIP, not default activation, tutorial qualification or milestone
completion. Do not merge this historical tree over current main: publish
selected changes against current main with fresh qualification.

## Exact Source

- Host: `XSJHARMENON01`.
- Checkout: `/home/harsh/work/fe2o3-issue271-canonical-inventory-v257-20260913`.
- Checkout HEAD: `10b190b8267c0c4011b4ef6bbb52680a3c44b391`.
- Source: 5069 files, manifest SHA256
  `b17c4d974b6c1c1ac007a2a0bcf32057e12c8b65236d0b91b0be170485f4a2aa`.
- Previous WIP: `d1100839cd5b6aed97b0a6056fcc00152668db39`.

This status file is outside the manifest. Snapshotting preserves checkout
HEAD/index and verifies every source Git blob.

## New Integration

The held Stage A and B1 callbacks now connect the supplied-source/native replay
and exact N/B target/catalog relation before their unchanged checked execution
receipt, source descriptor and native text replay. N is the original native
graph, B its actual target-bound graph, and O the actual checked policy-3 output.
The original input catalog is independently bound to N and B; native replay
still uses the separate transported O catalog. Both inventories, catalog views
and relation results remain live through the continuation on the original ledger.

Original source functional, aggregate and reference checks, per-root physical/R2
checks, fresh Complete formal-memory results and target geometry remain mandatory.
No gate was bypassed and no new source-proof owner was constructed. Emitter
replay is consistency, not an independent semantic proof or producer attestation.
The compiler documentation describes this order and the separate resource domains.

## Executed Checks

- Exact current source: all eight new chain tests passed, followed by all
  **831 backend library tests**, with no failures or ignored tests. The focused
  count is included in the full library count. Source/helper guards passed.
- The five edited Rust files passed rustfmt checks; diff checks passed.
- Previous snapshot `ad6f0bb8`: 390 lowerer library tests, 823 backend library
  tests, 28 lowerer compile-fail docs and 10 supplied-native integration tests
  passed. Those predate the five-file chain and its documentation addition.
- Current chain tests cover actual one/two-root components on both profiles,
  complete nonempty source catalogs, graph-valid metadata omission, relocated
  output catalog refusal, target mismatch, derived bookkeeping/header limits,
  callback errors, panic cleanup and postflight failure precedence.

These are scoped component/integration results, not full workspace, strict-lint,
default-production, LLVM-object or GPU qualification. Successful components still
do not demonstrate the genuine post-functional-Some production continuation.

## Separate Published Work

Both main branches contain `958a80a3d4c7d820df5ec51558b14a475edbb621`: the native
replay batch and subsequent facade test-dependency correction. The correction
passed 43 facade tests (one existing gfx1151 toolchain test ignored), all-targets
checking, 37 tutorial-manifest tests, workspace policy and 32 standalone locks.
Its dev-dependency/lock/manifest correction is not yet integrated into this
historical WIP. Main contains other peer changes absent here.

The CPU WIP `10598e5de1cd62dc1667804a9536438f2dcd15c5` records a real standalone
VecAdd observer pass at its parent `70a55ac76`: six reference tests, seven child
phases successful, 1,077.007 seconds within 1,200, stable inputs, private build and
driver directories removed, and no matching child process after cleanup. This
is CPU reference observation only; all qualification/authority flags remain
false. No real managed batch or speedup is claimed.

## Remaining Work

M5/M7 remain active and all M0-M9 acceptance boxes remain open. The next semantic
checkpoint is the generic identity DisjointSliceGetMut/Option source/N/O shape,
then exact Some-payload control and own-Store address joins. That design is not
implemented by this snapshot. A real rustc-collected post-Some positive still
requires original per-output and aggregate proof execution; approved runtime
qualification remains deferred, not passed.

Protected framing, source/output functional proof composition, wire/worker/host
integration, policy-4 dispatch, default activation, legacy retirement, full
tutorial/simulator/hardware qualification and release/website pins remain open.
The separate #272 WIP remains unchanged. Existing strict lint debt is not waived.
