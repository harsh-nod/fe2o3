[package]
name = "fe2o3-artifact-transaction"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Transactional compiler artifact publication for fe2o3."
readme = "README.md"

[features]
test-hooks = []

[dependencies]
fe2o3-build-authority.workspace = true
libc.workspace = true
rustix = { workspace = true, features = ["process"] }
sha2.workspace = true

[dev-dependencies]
sha2 = { version = "0.11.0", default-features = false }
