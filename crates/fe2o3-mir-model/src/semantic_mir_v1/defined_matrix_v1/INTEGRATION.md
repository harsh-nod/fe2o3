# Policy Matrix Original Constructors

Status after exporter26 freeze release: schema, source helpers and original
attachment/replay hooks are MOUNTED. Closed defined-contract tags 3/4 require
MIR V22 alongside Lagrange's existing BF16 tag 80. Existing tags and accepted
payload bytes are unchanged. No KIR schema changes were made. Central compiler
and model tests are requested; full source import is not yet established.
No worker messaging tool was available; owner-specific notices were relayed
through parent commentary before the narrow exhaustive-match hooks landed.

## Ordered Max Allocation Notice To Bern

Per the parent's resumed mount assignment, the subgroup owner is reserving
`ProductionExecutionTerminalV1::SubgroupPartitionReduceMaxF32` at terminal46,
current CombinedV5 identity172. The shared enum/tag inventory has no competing
allocation. This does not change matrix defined-contract tags3/4, MIR V22,
KIR borrowed25/Math26, BF16 intrinsic80, WG index tags, or the historical
terminal identity calculators. Max adds only nested partition subtag3 under
existing execution24 in MIR/KIR. Matrix and Math source branches stay untouched.
There is no direct worker-message tool in this session; this is the on-disk
owner notification, not a claim of a response from Bern.

## Max Allocation Acknowledgment

For Ram, checkpoint28: matrix ownership confirms no conflict with ordered-Max
execution terminal46 / CombinedV5 identity172. The mounted terminal roster ends
at partition broadcast45, and the existing CombinedV5 map is 126 + terminal tag
for tags >=42. Matrix Bind/narrowing use only defined-contract tags3/4, not an
execution terminal or terminal-identity allocation. These matrix changes do
not reserve46/172. Ram may use them for the reviewed Max specialization while
preserving historical mappings and all existing payload bytes. No Max hooks
were mounted by this worker. Direct worker messaging is unavailable in this
session; this acknowledgment is recorded for parent relay and shared-file review.

## Actual Source Evidence

Mixed24 retains the same six `PolicyGfx950MatrixIssue` first failures. A small
scratch-only rustc_driver probe queried the existing cached AMD provider MIR,
without the exporter DSO, Cargo, or any dependency rebuild. It compiled the
actual new `matrix/body.rs` and `matrix/body_tests.rs` children and passed three
positive body checks plus fourteen substitutions on each of gfx942 and gfx950.
This is original-body predicate evidence, NOT full-import or artifact evidence.

Observed original optimized MIR:

- Bind: 2 arguments, 3 locals, 1 block. Return aggregate fields are Copy arg0,
  Copy arg1, PhantomData brands, PhantomData private marker.
- Narrow: 1 argument, 3 locals, 2 blocks. Entry calls the original `matrix`
  field getter with Copy arg0 into the unique matrix-reference temporary. Its
  exact CallReturn block returns an aggregate containing Copy arg0 plus the
  private marker. No call-free alternative is accepted.
- Getter: 1 argument, 2 locals, 1 block. Return is Copy ((*arg0).field0).

The provider instances were the actual matrix::with_numerical_policy,
gfx950::gfx950, and matrix::matrix bodies. Generic arguments retained wave64
SubgroupBrand, KernelCapabilityBrand, CurrentTarget, RegisteredLaunch,
InitialEpoch and StrictIeee. The registered-source fixture below checks an
actual authenticated kernel root, not the probe's test-only nominal u8 brand.

## Typed Records

`SemanticPolicyMatrixBindV1` has its original function/source/ABI/body digest,
five type edges (matrix ref, matrix, policy ref, capability, bound), and the
complete provenance/policy/root brand/subgroup brand/epoch identity tuple.
It fixes reference arguments and fields to [0,1], width to 64 and mode to strict.

`SemanticPolicyGfx950NarrowV1` has its own original body record AND the exact
original projection function/source/ABI/body record, the five Bind type edges
plus bound ref and narrowed result, and the same full identity tuple. It fixes
receiver argument and captured field to zero. The getter receives no independent
authority annotation. The narrowing record does not invent a Bind occurrence;
later SSA must prove where the actual bound receiver came from.

Each body record is function u32 + three 32-byte source/ABI/body identities.
Body commitments reuse the exact unannotated V19 function-fragment encoder,
bounded to 16KiB per body and charged against remaining validation work. This
is a commitment format, not a claim that the new function annotation is V19.
Payload-only encoder/decoder lengths are Bind 444 bytes and Narrow 552 bytes.
The closed enclosing tags are 3 and 4, respectively. There is no
optional getter discriminator or extension of existing payload meanings.

Validators select locals by unique role/type and blocks by actual entry and
CallReturn target. They never assume canonical IDs equal raw rustc ordinals.
Module validation additionally requires matching existing MatrixAccess and
NumericalPolicyIssue callables for the same types and root provenance; matrix
access must retain width64, complete subgroup brand, root brand and epoch.
These are inert consistency checks, not dominance, liveness or SSA authority.

## Mounted Shared Hooks

1. `semantic_mir_v1.rs`: declare/reexport `defined_matrix_v1` and its six types:
   DefinedMatrixBody, DefinedMatrixIdentity, PolicyMatrixBindTypes,
   PolicyMatrixBind, PolicyGfx950NarrowTypes, PolicyGfx950Narrow (all Semantic/V1).
2. `defined_capability_v1.rs`: add PolicyMatrixBind and PolicyGfx950Narrow cases;
   delegate function/source/body/provenance accessors; minimum V22; encode tags
   3/4 followed by their payloads; delegate attachment and module validation to
   the new child. Preserve all epoch/Math methods and tags 0/1/2.
3. Its nominal claim dispatcher delegates to `defined_matrix_v1::record_claim`
   with each record's Bind types and identity. This reuses the policy binding
   and execution workgroup-brand/provenance maps. Complete matrix/epoch consistency is checked
   by the new child's issuer scan; no separate SSA authority graph is added.
4. `canonical_decode.rs`: mount `defined_matrix_v1/canonical_decode.rs` under a
   distinct child name. `canonical_decode/workgroup_borrow_v1.rs` dispatches
   tags3/4 only at version >=22 to the two new payload decoder methods. Existing
   V20/V21 dispatch and payload bytes must remain unchanged.
5. The epoch projection getter in `workgroup_borrow_v1.rs` and epoch-only filter
   in `semantic_direct_call_expansion_v1/workgroup_epoch_v1.rs` explicitly return
   None for the two new non-epoch variants. The common defined-occurrence mapper
   itself already carries typed contracts and requires no new identity fields.

Exact downstream exhaustiveness hooks mounted with the enum:

- Pauli: `adapter/math_borrows_v1.rs` and Math lowerer `occurrences.rs` exclude
  the two new variants from their Math-only index, without admitting any matrix
  borrow/issuer. Matrix SSA requires a later exact common-graph adapter.
- Lagrange: `execution_source_occurrence_01/defined.rs` returns false for these
  variants until an exact matrix statement adapter exists. Preserve rev5.
- Compiler Math-only `math/import_tests/defined_sources.rs` has three exhaustive
  matches; new matrix cases must be explicit unexpected-fixture failures.

## Mounted Source Hooks

Children live in numerical_policy_v1/defined_body_v1/matrix*. All operations
derive types from the actual constructor inputs/result fields and retained
original MIR, never from an MFMA/Global consumer. Both helpers stay defined.

1. `matrix` is mounted in `defined_body_v1.rs`, visible to the importer parent.
2. In importer original function validation, call
   `numerical_policy_v1::defined_body_v1::matrix::validate_source(tcx, instance)`.
3. After the COMPLETE canonical function/callable loop, beside existing Math
   attachment, call `matrix::attach(tcx, plan, &types, &mut functions,
   &callables, kernel_contexts)`. Reuses the existing exact Roster/source/root
   converter checks, prepares all clones before applying any attachment.
4. `validate_execution_terminal_carriage_v1` calls
   `matrix::validate_carriage(tcx, plan, kernel_contexts, semantic_mir)`.
   This rejects missing, conflicting and non-reviewed-source attachments.
5. `is_traversed_reviewed_helper_v1` adds ONLY PolicyGfx950MatrixIssue (and its
   exact roster test). No Current alias or terminal expansion is introduced.

The scoped annotation does not own the legacy root-only specialization of
`MatrixCapability::with_numerical_policy`. `KernelContext::matrix` returns
`MatrixCapability<KernelCapabilityBrand<...>>`, so its Bind has equal matrix
and global brands, with no subgroup width or epoch. The source classifier
leaves ONLY this exact reviewed specialization unannotated after the existing
strict-policy, original signature and ordered-reference validator succeeds.
Malformed root/policy specializations remain candidates and fail validation.
`PolicyGfx950MatrixIssue` still requires the complete subgroup chain; the root
case cannot obtain a narrowing annotation or a synthesized epoch. This fixes
the BF16 source boundary without changing any BF16 terminal or schema.

## Tests And Remaining Gates

Eleven new model tests: eight `defined_matrix_` body/identity/permutation tests,
two payload codec tests and one closed-tag/minimum-version test. Parent central
checkpoint27f reports all eleven passing.

Two new ignored actual callbacks:

- `defined_body_v1::matrix::tests::policy_matrix_full_import_gfx942`
- `defined_body_v1::matrix::tests::policy_matrix_full_import_gfx950`

They use cached FE2O3_CORE_TRY_* metadata, authenticated registered source,
repository cwd and scratch rustc out-dir. They assert V22 canonical roundtrip,
V21 rejection, exact original source/ABI/type/reference edges, checked defined
occurrences, idempotent attachment, all fourteen original-body mutations,
generic substitutions, coherent hostile policy/root/subgroup/epoch claims and
atomic rejection of changed canonical rosters. Parent AMD27f compiled and ran
both callbacks, but their source macro rejected a context-only kernel before
import. The fixture now has an `enabled: u32` runtime argument controlling an
early return. New pre-import source-boundary checks retain the live plan,
validate the root-only Bind specialization, reject malformed policy/root
substitutions, and reject root-only narrowing. Central rerun is pending.

The live plan is built from the actual inventory and authenticated closure
types before the original collection is consumed. Tests compare its canonical
transcript and inventory hash with the imported owner and retain all three
terminal/source/contract validation calls. Repository cwd and scratch output
directory are unchanged. No worker Cargo or dependency rebuild was run.

Whole-document combined epoch/Math/matrix codec validation still needs the
central source tests. No MFMA terminal ABI, FP4/FP8 Global
view/load contract, Context/matrix SSA borrowing, numerical target proof,
backend or simulator support is granted here. Six production kernels may
advance to those next explicit blockers; no successful bundle is claimed.
