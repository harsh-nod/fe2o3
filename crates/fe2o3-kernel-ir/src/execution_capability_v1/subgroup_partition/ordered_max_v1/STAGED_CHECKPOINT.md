# Mounted Ordered Max Checkpoint

The complete 71-edit batch is MOUNTED after the parent's resumed assignment.
Terminal46/current CombinedV5 identity172 is recorded in Bern's matrix handoff
(`semantic_mir_v1/defined_matrix_v1/INTEGRATION.md`) before the shared hooks.
There is no direct worker-message tool and no claimed acknowledgment from Bern.
Current inventory has no competing allocation. All repository edits are held
for the central core28/28b build and sweep checkpoint.

Patch: `integration.patch`, SHA256
`708232b8c5b4e867ab09912da15e77f5687e335a95dfb0b4cf9429c2af451a5b`.
The archived patch mounted 71 edits across 26 existing Rust files; all nine
referenced child Rust files exist and are mounted. Do not reapply the patch.

## Contract And Routing

- MIR V22, execution24/partition3; signature `[partition_reference, f32] -> f32`.
- KIR execution24/partition3, payload2 or existing occurrence payload5. No new
  fields, work ceilings, source records, arbitrary policies or execution authority.
- Existing partition0/1/2, borrowed25/revision3, Math26/revision4, WG27/28,
  legacy WG18 and BF16 intrinsic80 remain unchanged.
- Mounted terminal46/current V5 identity172 preserves the existing allocations.
  Both producer/importer tag calculators are unchanged; existing historical
  identity arrays remain asserted alongside the new current-schema tag.
- Existing source reconstruction authenticates only the actual Wave16 max item,
  its nominal shared receiver, scalar type, width64/tile16, root, brand and epoch.
- Borrowed dispatch uses the existing exact partition receipt and live owner loans.
  KIR verifies the original Workgroup/Subgroup/partition SSA issuer chain.
- Backend and simulator reuse Wave Maximum, ascending XOR1/2/4/8, ordered less
  and left-bit retention on equality/unordered. No fmax, sum, reassociation,
  NaN canonicalization, fast flags or tile-leader broadcast.
- Target requirements remain full participation and IeeeStrict. Max uniformity
  is varying because NaN payloads and signed-zero ties can differ by lane.

## Existing File Groups

The manifest lists exact before/after anchors. Shared hooks cover the MIR
partition schema/version/decoder; KIR partition schema/verifier/catalog;
analysis convergence/uniformity; Pliron exact receiver classifier; lowerer
mapping/borrowed receipt dispatch; AMD target/operational/physical lowering;
simulator family/physical projection; importer terminal/contract validation;
and the existing actual-import harness, retaining its original V20 case.

Pascal's two added catalog families are preserved: expected catalog after Max
is 29 families and 46 cases. Original catalog tests and mutations are not erased.

## Complete Child Files

- `fe2o3-kernel-ir/src/execution_capability_v1/subgroup_partition/ordered_max_v1/fixture.rs`
- `fe2o3-kernel-ir/src/execution_capability_v1/subgroup_partition/ordered_max_v1/tests.rs`
- `fe2o3-mir-model/src/semantic_mir_v1/subgroup_partition_ordered_max_decode_tests.rs`
- `fe2o3-lower-mir-kernel/src/production_semantic_kir_v1/subgroup_partition_01/ordered_max_tests.rs`
- `fe2o3-amdgcn-model/src/lowering/v13/subgroup_partition/ordered_max_tests.rs`
- `fe2o3-kir-sim/src/execution_capability_v13/subgroup_partition/ordered_max_tests.rs`
- `rustc-codegen-fe2o3/src/collector/production_importer_v1/subgroup_partition_v1/workgroup_source_v1/canonical_transport_v1/ordered_max_source.rs`
- Same canonical_transport_v1 directory: `ordered_max_import_tests.rs`
- Same canonical_transport_v1 directory: `ordered_max_lowering_tests.rs`

The inert KIR fixtures test structure and numerical projection only, not source,
machine or launch authority. The separate registered AMD source uses the existing
production macro and exact session-derived binding/cached metadata harness.
Its two max calls must survive full import, V22 canonical roundtrip, replayed
SSA, ranked/context checks and KIR lowering with exact observed occurrence fields.
AMD/simulator projections must retain both calls. Negative-only source replay
kills the observed owner before derivation and before consumption separately.

## Verification

Before mount: 71 unique/nonoverlapping anchors and git apply --check passed.
After mount: all 35 actual Rust files parsed with pinned rustfmt (emit stdout,
no formatting edits), and git diff --check passed. The Node spawnSync syntax
mode times out here; direct tool invocations were used. Exhaustive partition
consumers and catalog were inspected after mounting. These are not semantic
or typecheck results. No Cargo, dependency rebuild, SSH, network or GPU launch ran.
Parent's Catalog23 8-pass/27-family/43-case result predates this Max extension.

Parent AMD27f reports actual borrowed source and KIR lowering pass, followed by
the AMD target closure incorrectly requiring hardware SubgroupSize(16). The
mounted target dependency fix retains logical collective participants16 and
derives physical width from explicit graph root requirements (Wave64 here).
Four new target tests await central execution. No final callback success claim.

## Cleanup After Integration

Parent may move these worker-owned staging artifacts to ART after the mount:
`integration.patch`, `integration.edits.json`, `check_staged.mjs`, and this
`STAGED_CHECKPOINT.md`. The same four filenames under the lowerer's
`production_semantic_kir_v1/borrowed_workgroup_01/` are obsolete staging artifacts;
that directory's `BB6_CHECKPOINT.md` is historical diagnostics for ART too.
Keep Rust source/tests and both directories' architectural `INTEGRATION.md`.

Parent commands for the mounted checkpoint:
```text
cargo test -p fe2o3-mir-model --lib ordered_partition_max
cargo test -p fe2o3-kernel-ir --lib ordered_max_tests
cargo test -p fe2o3-kernel-ir --test execution_capability_catalog_v13
cargo test -p fe2o3-lower-mir-kernel --lib ordered_partition_max
cargo test -p fe2o3-amdgcn-model --lib ordered_max_tests
cargo test -p fe2o3-kir-sim --lib ordered_max_tests
```

Compiler child filter `subgroup_partition_v1` retains the existing nominal,
ABI, brand, epoch, scalar and root mutation suite, extended to Max. The new
ignored actual callback is:
```text
collector::production_importer_v1::subgroup_partition_v1::workgroup_source_v1::canonical_transport_v1::import_tests::ordered_max_import_tests::ordered_max_full_import_gfx950_v22
```
Run with the same FE2O3_CORE_TRY_* cached metadata environment as the original
Workgroup callback, using --exact --ignored --nocapture --test-threads=1.
No first-kernel qualification, final functional/machine proof or launch success
is claimed from this sidecar.
