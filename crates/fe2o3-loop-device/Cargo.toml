[package]
name = "fe2o3-loop-device"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Narrow Linux read-only autoclear loop-device boundary for fe2o3."
publish = false

[dependencies]
rustix = { workspace = true, features = ["process"] }

[dev-dependencies]
tempfile = "=3.21.0"

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"
