[package]
name = "fe2o3-collected-wave64-collectives-v1-fixture"
version = "0.0.0"
edition = "2024"
publish = false

# The fixture compiles the public Phase A source directly. There is no copied
# or rewritten kernel body whose authentication could drift independently.
[lib]
path = "../../../../../examples/wave64_collectives_v1/src/kernel.rs"

[dependencies]
fe2o3-device = { path = "../../../../fe2o3-device" }
fe2o3-host = { path = "../../../../fe2o3-host" }

[workspace]
